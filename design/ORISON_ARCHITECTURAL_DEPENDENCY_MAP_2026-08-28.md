# Orison architectural dependency map — 2026-08-28

**Task:** ADMIN-ARCH1. **Evidence level:** source-only audit (no Godot, no
Blender, no Git metadata). **Baseline:** origin/main after `d248c6c`
("Complete Orison two-root parity audit") and `9969bee` — the v2 reversible
composition foundation is in scope (§12). **Companion machine artifact:**
`tools/orison_spatial_dependency_manifest.json` (3,500 records), regenerated
and drift-checked by `tools/audit_orison_spatial_dependencies.py`, tested by
`tools/tests/test_orison_spatial_dependencies.py` (48 self-tests).

This document maps every repository dependency on the *current* Orison
building's spatial identity so the architectural rebuild owner knows what may
move freely, what needs a named-anchor migration, and what must remain
stable. It does not propose the new floor plan and does not touch production
code, layouts, scenes or checkpoints.

## 1. Identifier universe (current authorities)

Primary authority `art/data/building_layout.json` (mirrored at
`game/data/building_layout.json`, generated by `art/data/gen_layout.py`):

| Domain | Count | Notes |
| --- | --- | --- |
| floors | 8 | `B1, F01..F06, ROOF`; `meta.levels` carries elevations −2.8..19.2 (3.2 m storey) |
| rooms | 127 | `F01_LOBBY`, `F02_A_MAIN`, ... with `rect` + `kind` |
| units | 22 | `meta.residents` keys `1A..6D` (2–6 full rows, 1A/1D only on F01) |
| markers | 631 | props, doors (`F02_DOOR_05`), site shop doors (`SITE_SHOP_DOOR_*`), stations |
| furniture | ~7,300 | mostly generated set dressing; a few semantic (`bed` on F04, `*_couch`, TVs) |
| sockets | 237 | display sockets, e.g. `2A_FRIDGE_FACE` |
| vantry points | 119 | top-level list, `F02_A_MAIN_VANTRY_POINT` style |
| ventilation registers | 23 | riser-keyed |

Secondary authorities discovered during the audit (the scanner loads both):

- **`game/data/acoustic_graph.json` — 550 nodes**, each with id, raw
  position, `room` (room or unit id), `network`, riser and an id-keyed
  adjacency list. The single strongest foreign-key file in the repo
  (~2,500 spatial references). Service-system endpoints
  (`B1_BOILER_01`, `B1_ELECTRICAL_HUB`, `*_VANTRY_TRUNK`, `*_V_A_RISER`...)
  live here, not in the layout.
- **`game/data/fixture_light_map.json` — ~248 fixture ids** in
  `<ROOM>_<KIND>_<N>` grammar with floor, room and raw position;
  `light_provenance.json` is keyed BY those ids and must stay in sync.

**Runtime-created stable identifiers** (in no data file; authored by
`orison_detail_pass.gd`/`building_root.gd` at build time and then treated as
contracts by props, saves and tests): `F01_NIGHT_REGISTER`,
`F01_SIGNAL_REGISTER`, `F01_TOUR_KEY_GUARD`, `F01_WATCHMAN_DETECTOR`,
`F01_HOUSE_TELEPHONE_BOARD`, `F01_FIRE_LINE_STAIR`, `F03_EXTINGUISHER_STAIR`,
`B1_HOUSE_PANEL`, `B1_STATION_BOILER`, `B1_WATCH_STATION_01`,
`F02_WATCH_STATION_01`, `F02_STATION_2A_LANDING`, `ROOF_DOOR_CHECK`,
`ROOF_TANK_BALLCOCK`, `F04_B_BED`, `F01_ENTRY_SCONCE`, `LobbyPorterBoard`,
`LobbyMailBank`, `LobbyPostTray`, `LobbyMailChute`, `LobbyServiceDumbwaiter`,
`F01LandingInterlock`, `StreetEndHoardingFaces`, `AtriumShaft`,
`AtriumSkylightPool`, `F04_B_DOOR_ANOMALY`.

## 2. Live totals (manifest snapshot at the two-root baseline)

3,500 records: production 564, data 1,724, scene 4, test 1,208.

| Authority | n | | Spatial binding | n | | Disposition | n |
| --- | --- | --- | --- | --- | --- | --- | --- |
| DATA_FOREIGN_KEY | 1,724 | | SEMANTIC_ANCHOR | 2,199 | | PRESERVE_OR_ALIAS | 2,086 |
| TEST_CONTRACT | 1,208 | | ROOM_MEMBERSHIP | 495 | | UPDATE_TEST_FIXTURE | 1,178 |
| RUNTIME_LOOKUP | 461 | | FLOOR_MEMBERSHIP | 271 | | REGENERATE_CONSUMER | 77 |
| GENERATED_IDENTITY | 262 | | ASSET_PATH | 231 | | MUST_PRESERVE_ID | 71 |
| SCENE_NODE_PATH | 206 | | CAMERA_STATION | 133 | | REPLACE_WITH_NAMED_ANCHOR | 53 |
| SAVE_CONTRACT | 16 | | RAW_GODOT_COORDINATE | 113 | | UNRESOLVED | 34 |
| DEBUG_ONLY | 12 | | RAW_PLAN_COORDINATE | 70 | | OWNER_DECISION | 1 |
| | | | UNKNOWN_DYNAMIC | 35 | | | |
| | | | CONTAINMENT_ASSUMPTION | 10 | | | |
| | | | DERIVED_LAYOUT_LOOKUP | 18 | | | |
| | | | DISTANCE_THRESHOLD | 4 | | | |

Vector3 literals: 3,115 sighted, 2,914 deliberately excluded as
local/presentation (mesh building, prop-internal offsets). ~200 recorded as
gameplay-binding. Derived `b2g(<layout data>)` conversions are counted, not
recorded — they follow the layout wherever it goes.

## 3. Save boundary (P0)

Authority: `RealityState` (`game/scripts/game/reality_game_state.gd`),
version-4 JSON at `user://reality_maintenance_save.json`, per
`design/SAVE_RELOAD_TRANSACTION_MODEL_2026-08-27.md`. Spatial identity that
actually reaches disk:

| Saved fact | Value domain | Read-back behavior | Rebuild requirement |
| --- | --- | --- | --- |
| `organism_incidents` ledger keys + `ORG-<unit>-NNN` order ids | unit ids | actively read to re-arm conditions | **units must keep ids** (or save migration) |
| `organism_incidents[unit].at` | **raw world Vector3** | nearest-appliance pick after load | migrate to anchor or accept mis-pick after geometry move |
| `organism_incidents[unit].prop` | **scene node path** | `get_node_or_null`; missing ⇒ silently skipped ⇒ **ORG- work order stranded open forever** | the sharpest save risk in the repo |
| `core_loop.safe_return_anchor` = `"F04_B_BED"` | runtime anchor id | *write-only for position*: wake placement re-derives from the unique F04 `bed` furniture record | keep the id AND a unique F04 bed furniture record (or alias both) |
| `waking_residues[*].anchor_id` = `F02_2A_FRIDGE_01`, `.display_socket_id` = `2A_FRIDGE_FACE` | acoustic node + layout socket | currently provenance-only (consts win; socket miss falls back to acoustic node) | preserve or alias both ids |
| `building_personality.favorite_floor`, `.floor_dwell` keys | floor ids | read directly; stale keys elected happily | keep floor ids `B1/F01..F06` |
| job/order/report ids (`vantry_chirp_2a`, `lena_radiator_round_2b`, `WO-*`, `first_shift.report_id`, `night_register.lines[].job_id`) | unit tokens inside ids | validated against job library on restore; unknown id refuses the whole restore | job ids are frozen strings; unit renames must not rename job ids |
| `dream_seed` / `dreams_had` / `dream.night_index` / `dream.spawn_anchor` | seed + integers | deterministically regenerate dream geometry; `DreamAtlas.spawn_path()` mix is a **golden vector** | do not touch the mix; dream world is otherwise decoupled from the building |

Never saved: player transform (arrival is the `ARRIVAL_POSITION_B` curb
constant in `first_shift_director.gd` on every boot/reload), counters,
prompts, maze geometry, station marks (deliberately transient).

Floor-of-Y for the dwell ledger is derived from a **hardcoded 3.2 m storey
model** (`building_personality_director.gd`), not the layout — a rebuild
with different storey heights silently mis-files dwell time (and
`ambient_soundscape.gd` has a *second, different* envelope model:
roof y>18.5, outside z>9.72, basement y<-1.2).

## 4. Golden-route and case dependencies (P1)

- **Case one (Mina)**: `mina_case_gameplay.gd` places evidence props,
  calibrator, voice source, shift clock and the 4B letter at ~10 raw plan
  coordinates inside 2A/lobby/4B; `mina_caption_manifestation.gd` adds four
  caption positions plus the fridge anchor/socket pair. Job binding
  (`repair_target_id: F02_A_MAIN_VANTRY_POINT`, `anchor_ids`) lives in
  `maintenance_jobs.json` — derived, but the ids must survive.
- **Golden loop** (`golden_loop_test.gd`, K5/K6): two ~20-waypoint literal
  routes (2A→lobby descent, street outbound/return to the shop), door
  find-by-proximity `Vector3(-5.33, 3.2, 2.105)` (2A entry), named nodes
  `F01_DOOR_06`, `SITE_SHOP_DOOR_HARDWARE_PAINT`, and `EXPECTED_CHECKS := 87`.
- **Route probes**: `route_probe.gd` shape-cast legs against shop aisles
  (x 18.67 vs gondolas 17.95/18.97), `passage_nav_test.gd` portal plane
  `z > 28.316` and per-shop anchors, `final_map_route_test.gd` asserts the
  Passage portal plane **to the millimetre** (28.316 vs 28.317).
- **Resident movement**: `resident_nav.gd`/`resident_routines.gd`/
  `schedule_director.gd` carry corridor core extents, F01 ring envelope
  (|x|<5.33, |y|<9.65), full dog-leg stair flight literals (x ±2.31),
  passage aisle spine, STREET_ROUTES polylines, 18 hardcoded HAUNTS and the
  ANCHORS table (lobby, mail bank, laundry, roof, bodega, Harukiya). All are
  independent copies of built geometry; only a runtime raycast validates the
  stairs.
- **First shift**: arrival constants, `F02_STATION_2A_LANDING`,
  `F01_TOUR_KEY_GUARD` and the night-register `HOOK_DOORS`
  (`F02_DOOR_03`, `F02..F06_DOOR_04` by `find_child`).

## 5. Duplicated scalar authorities (the quiet contracts)

These are single floats/tables below token-scan granularity; the audit tool
carries them as `manual_contract` records so they cannot silently vanish.

1. **Floor elevation table** `{B1:-2.8 .. ROOF:19.2}` duplicated in
   `light_rig.gd`, `wayfinding_signage_pass.gd`, inline in
   `orison_detail_pass.gd` (five call sites), `walk_test.gd`; none read
   `meta.levels`.
2. **Street stage envelope** (`STAGE_W -20.10 / STAGE_E 20.60`, walk and
   road centrelines) authored four times inside `exterior_detail_pass.gd`
   and mirrored by `street_traffic.gd`, `weather_fx.gd` road mist,
   `light_rig.gd` core AABB and `street_containment_test.gd`.
3. **Zone-envelope law** in `building_root.gd`: passage boxes duplicated in
   `_envelope_side` and `_point_is_in_passage` (comment demands lockstep),
   Harukiya core, atrium eye, roof plane, weather exposure bands, transit
   shelter footprint.
4. **Elevator shaft half-depth** `FRONT_Z 1.1` + core `WALL_T 0.18` in
   `elevator.gd` (stops themselves are derived).
5. **Corridor/stair nav geometry** in `resident_nav.gd` (see §4).
6. **Wall-art law mirrors** of `build_orison.py` (RAIL_TOP 1.355 etc.) in
   `wall_art_law.gd` — must move in lockstep with the generator.
7. **Whole-room literal fit-outs with no layout consult**:
   `maintenance_headquarters.gd` (~20 literals bound to F01_OFFICE),
   `harukiya_interactables.gd` (bar interior), the `orison_detail_pass.gd`
   east-lobby service wall (x 5.24 family), `room0.gd` (pocket room at
   (-7.2, 90, -4.4)), `prop_warehouse.gd` (shed at x=400).

## 6. Id-grammar dependencies (naming conventions as code)

- Floor-from-unit: `"F0" + unit.left(1)` in `building_root.gd` (×4),
  `orison_detail_pass.gd`, `schedule_director.gd`,
  `domestic_witness_system.gd`, `building_personality_director.gd`,
  `flue_breast_prop.gd`.
- Unit-from-furniture-id: `fid.split("_")[0]` (`broadcast_director.gd`,
  `resident_routines.gd` couch/TV logic).
- Acoustic node anatomy: `<floor>_<A-D>_<device>` parsing in
  `acoustic_graph.gd`; case-manager fallback *synthesizes* marker ids
  (`"F0%s_%s_RADIATOR_01"`).
- Room-id suffix `_MAIN` = main room; riser letter math `V-A..V-D`;
  glTF buffer naming (`_walls`, `_finish_`, `F01_` prefixes) classifying
  materials in `surface_pass.gd`/`heightmap_pass.gd`; shop-hours marker
  suffix must equal light-fixture name suffix (`passage_hours_director.gd`).

A rebuild that changes any naming scheme rewrites these implicit contracts.

## 7. Test and capture coupling (P2/P3)

1,060 test-tier records across 334 test scripts. Three styles, in
decreasing brittleness:

- **World-space literals** (~79 files of `b2g` camera stations; walk/sweep
  waypoints; door-by-proximity literals) — silently mis-frame or hard-fail.
  Sharpest: `walk_test.gd` (3,735 lines: slab probes, B1 doorway rays,
  waypoint chains, count contracts of 24 sinks/23 showers/etc., storey
  datum table), `first_minute_test.gd` (spine plate pinned to 1 cm),
  `street_containment_test.gd` (stage envelope consts).
- **Name contracts**: 161 distinct quoted structural ids; most-referenced
  `F02_STATION_2A_LANDING` (30), `F01_NIGHT_REGISTER` (23),
  `F01_WATCHMAN_DETECTOR` (22), `F01_TOUR_KEY_GUARD` (16),
  `F02_WATCH_STATION_01` (12), `F02_A_MAIN` (10), `B1_BOILER_01` (8).
- **Layout-derived (resilient)**: the minority reading `root.layout` or
  asking the building (`warehouse.viewing_stand()`, `nav.passage_anchor()`)
  — the pattern the rebuild should make universal.

Room reconstruction checkpoints (`design/ORISON_*CHECKPOINT*.md`, 26 docs)
pin object id + 2D plan position + yaw + room membership at 0.05 m/1.0°
tolerance — historical evidence of the *current* building; they become
HISTORICAL_EVIDENCE_ONLY the moment the rebuild lands and must not be
"fixed" to match new geometry.

## 8. Stale, ambiguous and unresolved references (33 records)

- **Pseudo-rooms**: `B1_NAVIGATION`/`F01_NAVIGATION` used as `room` values
  by `fixture_light_map.json` (49×) and `light_provenance.json` (21×) but
  absent from the layout's room list. Also `*_ATRIUM_FRUIT_2` (8 floors) in
  `light_provenance.json` with no matching fixture id — apparent
  provenance/fixture-map drift. **Owner decision**: bless as pseudo-room
  vocabulary or reconcile.
- **Referenced but nonexistent**: `B1_DRYER_01`, `F04_B_EXHFAN_01`
  (`walk_test.gd`) — stale or misspelled test expectations.
- **Dynamic prefixes** (correctly unresolvable): `F01_BAR_LT_`,
  `SITE_SHOP_HOURS_`/`SITE_SHOP_LT_`/`SITE_SHOP_IN0..2_`,
  `F01_STREETLAMP_`, `F06_A_MONITOR_`, `F02_DOOR`, and `F04_B_` (v2
  blockout test id assembly) — assembled at runtime; each is a
  prefix-family contract with the generator/blockout naming.
- Resolved since the first pass: `F02_A_TELEPHONE` is a telephone endpoint
  registered live by `OrisonV2RuntimeRoot` — now classified as a
  runtime-created identity, not a stale reference.

## 9. Risk table

| Level | Dependency | Consumer | Failure mode | Migration requirement | Static test | Runtime proof still needed |
| --- | --- | --- | --- | --- | --- | --- |
| **P0** | `organism_incidents` node path + raw `at` + unit keys in save | organism re-arm after load | silent skip ⇒ ORG- order stranded open forever | replace path/coords with named anchor or write save migration | audit drift on `manual_contract` + unit records | K3 reload at an organism boundary |
| **P0** | `core_loop.safe_return_anchor` + unique F04 `bed` furniture | wake placement | player not moved on wake (warning only) | keep `F04_B_BED` id + one `bed` furniture on the new F04 | smoke test in suite | dream wake on new geometry |
| **P0** | `waking_residues` anchor/socket ids | residue reconstruction | label relocates to acoustic fallback without error | preserve/alias `F02_2A_FRIDGE_01`, `2A_FRIDGE_FACE` | smoke test in suite | residue visible after reload |
| **P0** | `DreamAtlas.spawn_path` mix + seed/night ints | dream reconstruction | every save's waking room silently moves | do not change the mix | manual_contract record | — |
| **P0** | floor ids as save keys (`building_personality`) + job ids with unit tokens | dwell ledger, job restore | stale keys / refused restore | keep floor ids and job-id strings verbatim | drift on floor_reference records | reload with existing save |
| **P1** | Case-one raw coordinates in 2A/lobby/4B | mina_case_gameplay / manifestation | evidence and captions strand outside the room | named anchors (see §10) | REPLACE_WITH_NAMED_ANCHOR records | case-one playthrough |
| **P1** | `maintenance_jobs.json` `anchor_ids` + `F02_A_MAIN_VANTRY_POINT` | chirp hunt, service round | anchor missing ⇒ fault loop never starts, progression blocked | preserve/alias anchor ids | data FK records | golden shift |
| **P1** | resident nav/routines/schedule literals | resident movement | residents walk through walls or stall | anchors or full layout derivation | manual_contract records | schedule live probe |
| **P1** | golden-loop routes, portal plane 28.316, shop aisle legs | K5/K6 proof | golden route test fails or silently passes wrong geometry | restation waypoints from named anchors | test-tier records | human golden shift |
| **P2** | runtime station/register ids | props, watch network, first shift | find_child misses; stations dead | keep the 26 runtime-created ids (§1) | runtime_id MUST_PRESERVE records | first-shift live test |
| **P2** | acoustic graph node ids/positions/rooms + fixture map/provenance sync | audio, lights, cases | sound routing and provenance orphaned | regenerate both files with the new plan; keep node ids referenced by code (§ BAND_ORIGINS, `B1_BOILER_01`) | data FK + PRESERVE records | audio policy live test |
| **P2** | glTF buffer/material naming, floor asset paths | surface/heightmap passes, building_root FLOOR_SCENES | materials silently unclassified; floors fail to load | keep export naming discipline and `res://assets/building/*.gltf` keys | asset_path records | visual audit |
| **P2** | duplicated scalar envelopes (§5) | weather, zoning, traffic, elevator | rain indoors, traffic through walls, mis-zoned audio | derive from layout or update in lockstep | manual_contract records | containment tests |
| **P3** | ~126 camera stations, shot suites, checkpoints | capture evidence | shots frame empty walls | restation after rebuild; checkpoints become historical | CAMERA_STATION records | re-render shot suites |

## 10. Named-anchor migration candidates (prioritized)

The rebuild owner chooses coordinates; these name the anchors and their
consumers. "Fallback": behavior today if the anchor is absent.

| # | Proposed anchor | Current consumer(s) | Ownership | Fallback today | Migration test |
| --- | --- | --- | --- | --- | --- |
| 1 | `PLAYER_ARRIVAL_CURB` (+look target) | `first_shift_director.gd` ARRIVAL_POSITION_B/LOOK | layout marker | none — constant | spawn lands on walkable surface, faces entry |
| 2 | `PLAYER_SPAWN_LOBBY` | `building_root.gd` `_ready` b2g([0,-9,0.1]) | layout marker | none | walk test first step |
| 3 | `SAFE_RETURN_BEDSIDE` | `core_loop_director.gd` (bed-rect derivation) | already semi-derived; formalize as marker | warning + no move | wake places player at bed on new floor plan |
| 4 | `CASE1_EVIDENCE_A/B/C`, `CASE1_CALIBRATOR`, `CASE1_VOICE`, `CASE1_SHIFT_CLOCK`, `CASE1_LETTER_4B`, `CASE1_CAPTION_1..4` | `mina_case_gameplay.gd`, `mina_caption_manifestation.gd` | layout markers in 2A/lobby/4B | none — strand | case-one interactables reachable |
| 5 | `RESIDUE_DISPLAY` (socket) | manifestation socket lookup | keep socket domain | acoustic-node offset | residue renders on fridge face |
| 6 | `HQ_BENCH`, `HQ_GEAR_WALL`, `HQ_CASE_WALL`, `HQ_INTERACT` | `maintenance_headquarters.gd` | markers in F01_OFFICE | none — strand | HQ interactable reachable |
| 7 | `PASSAGE_SPINE_1..3`, `SHOP_AISLE_<venue>` | `resident_nav.gd` passage_route/spots | markers on F01 site | none | passage_nav test |
| 8 | `STREET_ROUTE_<venue>[n]`, `RESIDENT_HAUNT_<slug>` | `resident_routines.gd` | markers or generated route data | none | schedule self-test |
| 9 | `SCHEDULE_ANCHOR_<place>` (lobby, mail_bank, laundry, roof, bodega, harukiya_bar, light_court) | `schedule_director.gd` ANCHORS | markers | none | schedule resolution test |
| 10 | `BAR_*` fit-out + `HARUKIYA_FLOOR_Z` | `harukiya_interactables.gd` | markers or bar-room derivation | none | bar interactables |
| 11 | `STREET_STAGE_ENVELOPE` (W/E/N/S walk/road lines) | exterior_detail/street_traffic/weather_fx/light_rig + tests | one layout site record | none — 4+ copies | street containment test |
| 12 | `ZONE_PASSAGE`, `ZONE_HARUKIYA`, `ZONE_ROOF`, `ZONE_STREET_BANDS` | `building_root.gd` envelope law | layout-derived boxes | none | passage visibility + weather tests |
| 13 | `UNCANNY_MARK_<floor>`, signage stations (directories, fire signs, landing plates, spine plate) | atmospheric_decal/wayfinding passes | derive from corridor walls | none | presentation audit |
| 14 | Camera stations (test-only) | 79 shot files | regenerate from room rects + eye height | n/a | shot suite re-render |

Presentation-only offsets (pane insets, decal standoffs, rail heights) stay
constants — they ride on derived positions and need no anchors.

## 11. What is already safe

- Everything that reads `layout`/`root.layout` and iterates floors, rooms,
  rects, markers, furniture, stairs, elevator stops: `railing_polish.gd`,
  `door_glow.gd`, `window_glow.gd`, `switch_system.gd` placement,
  `vantry_point_network.gd` (minus Teresa's id), `heat_balance.gd`,
  broadcast/arcade/domestic-radio/furniture-interaction placement,
  `weather_fx.gd` reflections, elevator stops, `wall_art_law.gd` inputs.
  These REGENERATE_CONSUMER records follow the new building automatically —
  *provided the ids they filter on survive* (marker kinds, `asm`
  vocabulary, unit ids).
- The Dream world: generated from seed; only `D00_4B_THRESHOLD` (scene
  marker) and the spawn-path mix touch it. No building geometry is
  serialized.
- The ~2,800 stats-only Vector3 literals (prop-internal geometry).
- Dead code: `building_root._build_original_orison_ad_board`,
  `exterior_detail_pass` car details/rideshare, `window_glow` own-window
  path — cleanup opportunities, not contracts.

## 12. Two-root rebuild status (v2 composition, landed `5f1bd8e`..`d248c6c`)

The rebuild is no longer hypothetical: a reversible two-root composition now
exists in production and the audit classifies it as follows.

- **Selector (composition dependency):** `BuildingRootSelector` is the one
  non-persistent authority for waking-scene choice —
  `res://scenes/building/orison_root.tscn` (v1, committed default) vs
  `res://scenes/building/orison_v2_runtime.tscn`, override via
  `ORISON_BUILDING_ROOT`. Both scene paths are PRESERVE_OR_ALIAS/HIGH;
  cutover and rollback change `DEFAULT_ID` only. `GameBoot.BUILDING_SCENE`
  and `CampaignShell.waking_scene_path` both route through it.
- **Parity anchor contract (preserved semantic identity):**
  `OrisonV2AnchorAdapter.REQUIRED` — `F01_DOOR_06`, `F02_DOOR_02`,
  `F04_DOOR_03`, `F02_A_MAIN_VANTRY_POINT`, `F02_A_MONITOR_01`,
  `F04_B_MONITOR_01`, `F04_B_BED`, `F04_B_BEDSIDE_RETURN`, `LobbyMailBank`,
  `LobbyPorterBoard`, `F01_HOUSE_TELEPHONE_BOARD`, `LobbyServiceDumbwaiter`
  — each must resolve to exactly one node in the selected root. All are
  MUST_PRESERVE_ID/HIGH in the manifest, in the adapter, in
  `OrisonV2RuntimeRoot`'s mounts and in `orison_v2_blockout.json`.
- **Save/wake dependency:** `CoreLoopDirector.resolve_return_anchor()` now
  prefers an injected resolver — v2 answers `F04_B_BED` with the explicit
  `F04_B_BEDSIDE_RETURN` stance — and falls back to the **v1-only
  anonymous-bed scan** (`furniture id == "bed"` on F04). Both records are
  curated: the fallback is PRESERVE_OR_ALIAS with a V1-ONLY rationale, so
  its removal is exit-4 drift until the manifest is deliberately updated at
  cutover. `core_loop.safe_return_anchor` stays `"F04_B_BED"` in the save
  in both roots.
- **Acoustic dependency:** the adapter installs scoped acoustic-position
  overrides for parity ids and must restore originals; the encroachment
  service beats still pin `F02_B_RADIATOR_01` and `B1_BOILER_01`
  (`radiator_evidence`/`basement_comparison` in
  `apartment_encroachment.gd`) — PRESERVE_OR_ALIAS acoustic node ids that
  the v2 world must answer.
- **Second layout authority:** `game/data/orison_v2_blockout.json`
  (`development_only`, `production_default: false`) defines the v2
  vocabulary — 38 spaces, 16 doors, 31 anchors, 45 envelopes, 11 capsule
  stations, service/passenger circulation. The scanner treats it as an
  authority: ids it shares with the v1 universe surface as the parity
  records above; v2-only vocabulary (envelopes, capsule stations,
  thresholds) stays out of the inventory by design.
- **Interaction/job/case dependency:** `OrisonV2RuntimeRoot` mounts real
  props (mail bank, porter board, telephone board + runtime endpoint
  `F02_A_TELEPHONE`, dumbwaiter, monitors) and rebuilds a one-point vantry
  network on `F02_A_MAIN_VANTRY_POINT`; job/case data files are unchanged
  and keep their v1 foreign keys.
- **Test/evidence-only:** the two-root matrix and v2 blockout/review suites
  are TEST_CONTRACT; the four `orison_v2_*_review.tscn` scenes are
  scene-tier asset-path records.
- **Debug-only:** selection is environment-driven and session-pinned;
  nothing v2 persists a root choice (verified: no selector state reaches
  `RealityState`).

## 13. Handoff summary for the rebuild owner

**IDs that must survive verbatim (or ship a save migration):**
unit ids `1A..6D`; floor ids `B1, F01..F06, ROOF`; the 27 runtime-created
ids (§1, plus `F02_A_TELEPHONE`); the twelve
`OrisonV2AnchorAdapter.REQUIRED` parity anchors (§12) including
`F04_B_BEDSIDE_RETURN`; `F04_B_BED` + a unique F04 `bed` furniture record
while the v1 root remains selectable; `F02_2A_FRIDGE_01`, `2A_FRIDGE_FACE`;
job-id strings (`vantry_chirp_2a`, `lena_radiator_round_2b`); the
`DreamAtlas.spawn_path` mix.

**Preserve-or-alias (rename allowed only with an alias layer):** all room
ids referenced by data files (fixture map, provenance, found art, hallway
art, dream modules); door marker ids referenced by name
(`F01_DOOR_06`, `F02_DOOR_03..F06_DOOR_04`, `F02_DOOR_05/10/11/12`,
`SITE_SHOP_DOOR_*`); acoustic node ids referenced from code
(`B1_BOILER_01`, `F02_B_RADIATOR_01`, BAND_ORIGINS set,
`F02_A_MAIN_VANTRY_POINT`, `F01_D_BED_VANTRY_POINT`); floor asset path keys
(`res://assets/building/floor_0N.gltf`); the resident→unit map (authored in
≥5 places — treat `reality_cases.json` as canonical).

**Top named-anchor migrations:** rows 1–4 of §10 before any geometry
lands; rows 5–12 before the golden shift re-run.

**Regenerate, don't preserve:** acoustic_graph.json,
fixture_light_map.json + light_provenance.json, all `[D]`-style consumers
(§11), the checked-in spatial dependency manifest itself.

**Test/evidence-only coupling:** 1,060 test records + 26 room checkpoints
are UPDATE_TEST_FIXTURE / HISTORICAL_EVIDENCE_ONLY; budget a restationing
pass, do not let them constrain the design.

**Unresolved owner decisions:** pseudo-room vocabulary
(`*_NAVIGATION`, `*_ATRIUM_FRUIT_2` provenance drift, §8); elevator shaft
scalar policy (derive vs preserve, §5); whether id grammars (§6) are kept
as law in the new naming scheme or replaced by explicit fields;
stale test ids `B1_DRYER_01` / `F04_B_EXHFAN_01`.

## 14. Tool and v2 reconstruction workflow

```
python tools/audit_orison_spatial_dependencies.py            # drift check vs manifest
python tools/audit_orison_spatial_dependencies.py --json     # machine output
python tools/audit_orison_spatial_dependencies.py --production-only
python tools/audit_orison_spatial_dependencies.py --update-manifest  # explicit only
python tools/tests/test_orison_spatial_dependencies.py       # self-tests (48)
```

Exit codes: 0 clean; 1 new unclassified production dependency, vanished
target, authority/domain/coordinate-contract change, or unresolved save
contract; 4 stale preserved records (a MUST_PRESERVE / PRESERVE_OR_ALIAS /
SAVE_CONTRACT record left source — including the v1 anonymous-bed
fallback); 5 both; 3 malformed manifest, duplicate identity, or a manifest
path aimed at `game/`, `art/` or `design/` (the tool refuses to write
there); 70 internal. Record identity is line-independent
(kind, file, token/symbol, tier), so pure code movement never drifts.

**Workflow for every v2 spatial milestone:**

1. **Before** touching geometry/anchors, run the drift check — it must be
   clean; if it is not, the previous milestone left undocumented contracts.
2. Make the change (blockout, anchors, mounts, cutover step).
3. Run the drift check again. Read every non-zero bucket:
   - *NEW unclassified* — a spatial dependency you just created; classify
     it (extend `KNOWN_CONTRACTS`/`RUNTIME_CREATED_IDS`/
     `MANUAL_CONTRACTS` if heuristics get it wrong).
   - *stale preserved* — you removed a contract; deliberate only.
   - *targets vanished / class changes* — an id changed meaning; either
     restore it, alias it, or accept it explicitly.
4. Only after the deltas are understood, `--update-manifest`, and commit
   the manifest with the change that caused it (same commit).
5. `python tools/tests/test_orison_spatial_dependencies.py` — smoke tests
   pin the parity contract (selector paths, adapter REQUIRED ids, bedside
   return, v1 bed fallback) and fail if a milestone silently rewrites one.

The recommended single command before and after every spatial milestone:

```
python tools/audit_orison_spatial_dependencies.py && python tools/tests/test_orison_spatial_dependencies.py
```

## 15. Limitations

- Heuristic textual scan, not GDScript semantics: dynamically assembled ids
  are enumerable only as prefix families; scalar-float contracts are carried
  as hand-authored `manual_contract` records (13 of them), which cover the
  known cases but not unknown future ones.
- `b2g` calls whose argument mixes literals and variables
  (`b2g([0, -8, z + 0.1])`) count as derived; the hardcoded x/y inside them
  is under-reported (the teleport stations are covered by a curated record).
- The Vector3 gameplay gate (strong context, or weak context with ≥8 m
  magnitude) is deliberately conservative; borderline presentation offsets
  in building space (~7 m) stay stats-only.
- Data-tier JSON foreign keys are matched by exact string equality; prose
  mentions inside display strings ("2C — JUNO KELLS REPORTS") are not
  separately enumerated (noted in §3 as part of the work-order records).
- No Godot run: nothing here proves runtime behavior; K2/K3 route evidence
  remains the human matrix in `TASKS.md`.
