# V2 continuous shift and conversation readability

Evidence class: **INERT**

REPORT - V2-CONTINUOUS-SHIFT - 2026-09-26

Branch / base / origin/main at start / merge-base: main /
a6b862b92558ec5c53032a9492d8bfcb5e32bcb2 (all three references).
Implementation HEAD is the commit introducing this report.

## Change and ownership

The rendered first repair conversation exposed overlapping service-wire text
behind Mina's translucent dialogue choices. TelegramHud now hides the field
copy while an existing attention-people owner claims the conversation. Its
typewriter and reading lifetime pause and resume without reissuing the card.
A newer field copy waits behind the conversation; a dismissed copy stays
dismissed. A caller-hidden HUD retains its previous visibility. No input,
conversation, maintenance or campaign authority moves into the presenter.

The full-shift fixture composes the production CampaignShell and reuses the
existing physical route drivers. CampaignShell performs automatic onboarding;
the standalone repair fixture retains its explicit onboarding call. The joined
run earns the repair and both conversations through ordinary collision movement
and interaction rays, then lets SleepPressureDirector initiate the dream on its
normal clock. Dream movement uses the ordinary controller and the autonomous
outcome returns through the same shell. The fixture does not reload an
intermediate save, inject resolved case facts, teleport, disable collision, or
request the dream outcome. Dialogue choices are driven programmatically.

The existing saved-boundary played-dream fixture now also uses normal onset;
the separate boundary-reconstruction fixture retains its explicitly scoped
public transaction test. The new full-shift fixture waits for actual player
floor contact instead of using fixed settling delays. One new V2 scene-path
test dependency is inventoried; previous spatial classifications are unchanged.

## Validation and evidence limits

Before the UI fix, the windowed recurrence route passed 164 waypoints but its
conversation capture showed the overlap. After the fix the same route passes,
including visibility, retained reading time and restoration of the same card.
The isolated field-copy fixture checks replacement during conversation,
dismissal, caller-hidden visibility, pointer ownership and unchanged campaign
facts. All seven checks pass.

The earned-save played-dream run with normal onset passes 92 checks, including
autonomous outcome, V2 reconstruction, bedside movement, retained case facts
and real disk reload. Windowed conversation, Dream and bedside frames are
inspected. The joined campaign run passes 162 waking waypoints, traverses
56.7 metres in the autonomous dream, receives its capture outcome, returns to
the actual bedside and saves the retained closed job and waking residue.
Logs and adjacent suite-run receipts live under tmp/v2-full-shift;
the committed continuous run is recorded separately as committed-full-shift.
Candidate verification includes two imports and the clean baseline gate
comparison. These are scoped regression observations, not schema-2 runtime
contract promotion or a blind-player K2 acceptance claim.

The initial continuous fixture wrongly attempted to start onboarding twice;
the production shell had already started it. Its adapter now verifies the
automatic arrival. An exploratory ServiceWireResponseTest run crashes at an
existing removed shop-audio field and times out before these checks. That
legacy suite was not changed or counted as passing; the new regression has an
independent fixture. No historical test result or acceptance evidence is rewritten.

## Remaining scope

This completes neither the broader six-case campaign nor the blind-player
golden-shift and eleven-boundary save matrix tasks. The scripted route knows
the authored destinations and conversation choices. Existing H23 lighting
seams and historical gate debt remain. The owner's four render files are
preserved outside the change. No owner decision is required for this slice.
