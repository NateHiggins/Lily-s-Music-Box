# Wardrobe floor admission — reviewed proposal, engine unrun

The actual full02 probe establishes twelve wrongly admitted low wardrobe leaves: F03/3A belongs to Mina2A rows, F05/5A to Peter4A rows, and F06/6B to Cal5B rows. Material source links and case markers are true; later field rebinding makes the stored row material differ from the installed draw. F02/F04 extra registry IDs exactly equal their stale foreign-floor row IDs. This is a first-case admission error, followed by the binder conflict described below; no MatLib mutation or callback defect is required to explain it.

The authored elevations are F02 3.2, F03 6.4, F04 9.6, F05 12.8, F06 16.0 metres. The 3.2m step is smaller than reach_props' +3.6m ceiling. FurnitureInteractionPass35–42 places each real mechanism beneath its exact floor provider. BakedFurnitureInteraction272–306 gives FramedLeaf world bottom floor+0.10 and RaisedPanel floor+0.22. Both overlap the prior ceiling; the BrassKnob conservative bottom is floor+0.929, beyond that ceiling (an earlier informal approximation was +0.939). Authored X/Z records stack inside the prior units. source_evidence.json binds the source files, raw probe and all eight original observations; derived dimensions are source evidence, not newly measured runtime transforms.

floor_ancestry.patch is against exact repaired owner7c54c. It captures the complete floor_nodes provider at build, creates an identity set once per reach, and refuses a different nearest declared floor ancestor before metadata and world-AABB admission. B1/F01/roof are included although they have no case. Existing manual36/171 fixtures explicitly supply unit.floor_node identities; these populate the same provider set without scene-name guessing. Root-level new props retain the old bounded AABB test; existing root-level metadata and horizontal case-sharing behavior stay intact. No SurfacePass, MatLib, geometry, vertical bound or one-census code changes.

The secondary binder loop says root-level but visits all root descendants. Current field order is F02,F04,F06,F05,F03, so the F05 case-row pass can overwrite a real F06 binding; earlier floors also retain stale registry IDs after later replacement. Correct fresh admission prevents these foreign case rows in this static composition. This proposal deliberately does not define copying/carrying an already case-owned material across floors. That policy and any defensive root-only binder restriction remain unresolved and must not be claimed fixed by the fresh-producer proof.

The focused scene uses the actual owner build with authored room records and all eight floor provider identities, then real FurnitureInteractionPass/BakedFurnitureInteraction/MatLib for nine selected authored wardrobes. Floor containers are synthetic, with no glTF surfaces or substitute collision proof. It runs both normal SurfacePass.apply_props and the exact queued tier callback. It covers six correct case mechanisms, three incorrect prior-floor admissions, real current field/registry identity, unchanged root-level inside/outside bounds, and explicit no-case floor provider controls. The latter intentionally place synthetic test draws in an overlapping AABB; they establish provider authority, not authored physical geometry. Two repeated sweeps check identity stability. It holds field ticking/ecology outside scope and disables DREAM_FIELD only during the focused build; persisted facts must remain unchanged and the actual root must retire.

expected_contract.json declares63 exact ordered checks. Original7c54 and the guard-only omission must reproduce exactly the same four foreign leaf paths per each of the three records, in both normal and queued phases. Only the specifically listed consequences may fail; setup, real callbacks, correct same-floor groups, root AABB controls, facts, retirement and readable receipt must pass. Any parse/type/native error, warning or unexpected assertion is a new failure, never a declared red. Candidate/restored require63/63 and no foreign rows. The omission preserves provider/helper code and changes only admission plus the unused-local underscore spelling.

Proposed sequence after explicit source/engine handoff: original7c54 red → candidate green → guard-only omission red → exact candidate restoration green → existing36 material-binding regression. Preserve full raw logs/receipts, selected raw sources, complete hash manifests, actual runner/native versus gate exits, installed engine/PID binding and fresh APPDATA via the unchanged serial runner. The successful171 equivalence package and its four adapted copies remain frozen; one-census must be rebased only after this owner is validated.
