# Narrow foundation status amendment proposal

Prepared outside canonical reports at `e72320288d256e4d20386e8f28c116fe962788fc`. Nothing in this package has been applied. No game, tools, master ledger or current report changed; no engine run, generator or whole-tree hash was performed. `proposal.json` binds the five exact input reports and inspected evidence. `originals/` retains their bytes, `proposed/` contains reviewable replacements, and `foundation_status.patch` is the corresponding unapplied change.

The five proposed destinations are `design/astra/reviews/production_obligations.json`, `design/astra/LIVE_STATE.json`, `design/astra/DECISION_LOG.md`, `design/astra/PLAYTEST_FINDINGS.md` and `design/astra/RISK_REGISTER.md`. There are no master-ledger output files in this proposal. Root should regenerate those only after selecting the evidence and reconciling any later changes.

The decisive stale claims are:

| Current claim | Proposed correction | Remaining boundary |
| --- | --- | --- |
| CLOCK still names eight host-Unix writes and hearing that ignores absence | Record the five timestamp owners, archival/protection controls and presence-gated apartment hearing with actual red/green receipts | Ambiguous older facts, interrupted porter recovery, embodied perception, ordinary-day and human schedule proof |
| OPENSHIFT hearing correction/reconstruction is pending | Record focused 13/13 and actual-two-root 23-check proof, authority and save matrix | Full four-disposition physical route and dramatic-boundary proof; no human listening |
| AMBIENT navigation controls are still underway | Record 40/40 twice, independent portal/wait omissions and PassageNav 26/26 | Separate F01 haunt remains inside shaft; actual-Body clearance and complete arrival/dwell/return remain prepared only |
| PERF says no final broad candidate exists | Record the first full V1 candidate 305/305, native/gate 0, source stability and retirement | Matched independent controls, applicable V2 scope, no-op native-call count, content-comparable frame acceptance and new visual review |
| Historical timeline treats its old renderer signature as wholly unresolved | Preserve the noisy historical notice artifact while pointing to scoped current candidate progress | No retroactive artifact cleanup, no resident historical-event delivery or V2 notice, no human acceptance |
| F01 row names only C1/C2 quarantine | Record that a clean, source-bound reconstruction plan now exists | No export/provider executed or adopted; C1 human-pending and dirty C2 provenance remain unchanged |
| LIVE_STATE still selects save-commit audits | Select the completed foundation audit by exact receipt/output hashes and source HEAD | Latest actual production commit remains 9548301 until root commits; applied source overlay is named explicitly |

Obligation status enums remain unchanged. A focused navigation or perception success does not make the full ordinary-day obligation runtime-proven, and a full diagnostic composition does not satisfy release or performance acceptance. Fixed EST remains the authored policy; the proposal describes seasonal-offset coverage as a scope limit, without introducing automatic host DST or a new calendar decision.

Decision entries are appended as D023–D026, explicitly superseding pending portions of D020/D021 while preserving earlier historical failures. Playtest findings retain the original mixed/crashing renderer trials and add current outcomes; no human playthrough is invented. Risk rows move from the repaired symptoms to the specific remaining scope.

The first full V1 result inspected is `evidence/vulkan_composed/runs/candidate_v1_foundations_01/result.json`, with `frames/probe.json`: 305 checks, all passed; native/gate 0; full V1 Forward+; 80.919 seconds whole process; actual shell and world retired. It has zero unpair, soft-shadow-underflow, retention and non-inherited error headers. It still retains five inherited loader errors, two loader warnings, 1,038 RGB8 conversion warnings and one found-art placement warning. This is not empty stderr. `no_op_native_call_count_proven` remains false. Its frame samples are teleported rendered-transition measurements, not accepted steady p95 or complete human route performance.

The navigation review's later capsule correction is material: the former fixture used radius 0.33 m, height 1.524 m and center offset 0.80 m, while the actual resident Body is 0.28 m, 1.55 m and 0.775 m. Wider is not uniformly conservative when the shape is also shorter and differently centered. Preserve the old ray/graph/audit-shape evidence without upgrading it to actual-body or full swept-path clearance. The separate haunt proposal has not run.

The hearing original focused receipt remains actual exit 5 versus expected 6, with 8/13 checks. It is a semantic red, not a matched expected-exit control. Hearing green full digests precede only the two subsequently revised job/register fixtures; their selected production/hearing source copies still match. Earlier timestamp regressions precede hearing and fixture changes; final timestamps/jobs/register match the final reviewed full runtime manifest. These inherited-source limits stay in the aggregates rather than being collapsed into one undifferentiated green.

The selected static evidence retains five safety exits 0, completeness exit 2 and data consumption exit 1; all seven selftests and period audit/selftests exit 0. This changes neither baselines nor broad completeness/debt. Harukiya's old `>250` remains red and unchanged; 247/247 indexing and the 36 eligible source groups do not resolve the historical 264 discrepancy.

Recheck the five original report hashes before applying. Root/branch are still completing renderer controls; this proposal freezes only the first full V1 candidate. Incorporate later results as new evidence, not by overwriting the already inspected run or promoting unfinished sequences. No selector, accepted M11B boundary, last actual production commit, or release status is changed here.
