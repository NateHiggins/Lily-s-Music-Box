Execution is complete for the scoped resident navigation repair. The final source-bound fixture passed40/40 twice; original portal and waiting-point omissions failed independently; unchanged PassageNav passed26/26. See design/astra/evidence/resident_lift_waiting/validation.json and design/astra/reviews/resident_lift_waiting_review.md for all raw receipts, limits and outstanding work. The engine/source lane was released to root with an empty native process census; no further game writes or Godot runs are planned by this agent.

The immediate F04 cause was an actual graph disconnection. Generated door markers store hinges, but navigation treated them as portal centers. Real masonry rejected those jamb paths. A retained center-only candidate then showed sparse diagonal approaches still clipping an actual wall switch and masonry. The final portal spec matches hinge/width/orientation to an authored wall aperture and adds two normal body-clear approach nodes, without forcing edges or changing collisions. Clearance is wall.t/2 + radius0.33 + margin0.08; PROBE remains independent. All15 separately authored exterior anchors are preserved. The second fix derives a public lift waiting point from layout.elevator.shaft, retaining actual request/readiness/boarding rules.

Final reproduction and review inputs are resident_portal_approach.patch, resident_lift_wait.patch, the two game/tests fixture files, run_waiting.py, run_omissions.py and run_existing_nav.py. The wrappers also use the unchanged repository tools/run_godot_serial.ps1 and separately tested design/astra/work/vulkan_pairing/run_case.py diagnostic parser, with the installed engine bound by each receipt. Complete final proposed source copies live under game/scripts/characters in this package; the real tested files are in the repository game directory. Original byte copies resident_nav.before.gd.txt and resident_routines.before.gd.txt drive independent omissions. Current expected candidate and fixture hashes are deliberately explicit in run_omissions.py; a different checkout must fail this historical byte binding rather than be called the recorded proof.

On an explicitly granted exclusive engine/source lane, the final fixture can run through `python design/astra/work/resident_lift_wait/run_waiting.py lift <fresh_name>`. The omission transaction temporarily restores one exact original product file at a time and restores the complete tested candidate in finally. It must not overlap another agent's engine run or source edit. Each run uses a fresh non-overwriting evidence directory and APPDATA. Actual process exit, diagnostic gate and functional assertions remain separate. The unchanged PassageNav suite uses run_existing_nav.py passage_nav <fresh_name>. These are reproduction instructions, not a queued request for more runs.

Historical preparation and failures are preserved: resident_portal_center.patch and resident_nav.center_only.gd.txt are the incomplete center-only candidate, not the final patch; applied_candidate.json records that earlier candidate. installed_diagnostic_manifest.json and fixture_parser_repair.json explain the initial original-source diagnostic and the fixture-only explicit WeakRef repair. Their hashes must not be rewritten to imply later source was tested. door_marker_contract.json records all117 eligible marker forms (102 matching hinges,15 independent exteriors,3 additional cabinets excluded).

The proof covers the actual4D spawn, forward and reverse geometric path, selected capsule endpoints, existing production wall/ray checks, fixed-step production path-following logic and live source-ready elevator boarding. It does not claim human traversal, whole-path swept-body clearance, a completed return/day/haunt lifecycle or performance acceptance. Existing Transient Guests' F01 haunt is still authored inside the shaft and is recorded as separate follow-up. Initial hypothesized closed-elevator graph pruning was disproved; the shaft goal node remained connected while4D was disconnected. No geometry, selector, authored door marker, collision exemption or error suppression was changed.
