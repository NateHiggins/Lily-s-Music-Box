Current status: root applied the ledger correction and focused/composed fixtures. Original focused was 8/13 with actual native 5 versus mistakenly configured expected 6; that mismatch is preserved. Original composed had 4 failures among 23 checks. Corrected focused 13/13, actual-two-root composed 23, authority and save matrix 17/17 all returned native 0/gate 0. The composed found-art warning remains. See `design/astra/evidence/hearing_presence/validation.json` and `design/astra/reviews/hearing_presence_review.md` for exact source bindings and same-process scope. No new engine runs are requested here. Historical preparation text follows unchanged; its earlier not-installed/not-executed statements describe that earlier stage.

---

Prepared outside the live game while the renderer lane is occupied. Nothing in this package has been installed, parsed by Godot or executed. `git apply --check` passed as a packaging check only.

The existing observation ledger resolves the apartment reached by the acoustic graph, but its hearing loop ignores the presence provider already used by sight. A resident away from that apartment therefore earns an `in_home_hearing` or `heating_riser` memory through an empty room. The mandate requires observations to have real provenance. The proposed change uses the same home-presence authority for these two apartment hearing routes. Explicit actor-reported direct observations remain separate. Locating listeners in corridors, visited flats or outdoor venues is wider embodied-perception work, not proved here. Existing stored beliefs are preserved.

`hearing_presence.patch` contains one production ledger edit, a 13-check owner fixture and an update to the existing V2 presence fixture. The composed fixture checks a real authored corridor absence, continued hearing by the present riser neighbor, no retrospective learning on return, and a new properly dated observation after return. It also repairs its old reconstruction helper: that helper accepted a root ID but only reloaded the same global store. The proposal actually constructs each root and reads the root-owned ledger after reload. This changes what the test proves; it does not retroactively upgrade earlier receipts.

`originals/` retains the exact pre-change ledger and composed fixture. `proposed/` holds all four target paths. `prepare.py` records this first preparation and refuses to overwrite its original inputs. `run_case.py` invokes the unchanged serial runner with fresh APPDATA and source copies, and reports native exit independently of its diagnostic gate. Python AST parsing of the runner passed; no runtime claim follows.

Independent static review found two fixture weaknesses before execution. The final prepared fixture removes its unnecessary radiator offer and asserts an unoffered situation initially and after each root reconstruction; it must not manufacture a later autonomous sound while pretending to replay only the injected ledger events. No production coordinator is paused. Timestamp reconstruction now uses a fixed absolute tolerance and rejects a one-minute shifted input; relative float tolerance is much too loose at Gregorian absolute-minute magnitudes. `review_before/` preserves the earlier proposal and a failed Windows text-encoding packaging attempt. `preparation.json` and the patch bind the corrected proposal; the first-generation script is historical.

After source and Godot lane handoff, install only the two new focused fixture files plus the revised composed test. Run the focused and composed fixtures against the original production ledger to retain meaningful assertion reds. Apply only the ledger edit and run both again. Run the existing Open Shift authority and save-matrix consumers. Preserve native errors and all first failures, restore any temporary source mutation in `finally`, and keep source stable throughout each process. Finish by binding final production bytes, actual-root evidence and known perception limitations in the live ledger. No new human, listening, movement or full-game acceptance is granted by this package.
