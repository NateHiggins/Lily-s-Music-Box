This is a strictly stronger synchronous observer revision of exact c264.
It is outside game and unrun. The actual c264 full02 evidence remains intact:
eight material failures,with restoration of recorded state and failed
consumer propagation. That run did not prove all-registry uniform restoration.

The candidate fixture is
`proposed/game/tests/vulkan_composed_root_test.gd`, SHA256
d67ce1ac8b19d51f6d4ccc88bc3fe11599dd6068afaccea6abb1d9ec50712508.
Its helper SHA256 is
aec59c518d9c08714b30a69b5f9682dd8cc213e17d495719602d5635d815530c.
`originals` preserves the immediate c264 fixture/helper bytes. Live cleanup
still restores the parent package's original5ec63 fixture,not this revision's
immediate source parent.

Only `_exercise_actual_material_refresh` and one new scalar/ID serializer
change. All other source,including the eight explicit bool repairs,original
assertions,captures,transition/no-op timers,safe case/beachhead precondition and
actual retirement,remains unchanged. Declared check counts remain314 for full
V1 and305 for V1 root_retirement. These expectations are not runtime results.

Every valid finish/prop row now independently evaluates installed identity and
expected immediate state before combining them with the aggregate result.
Earlier failures cannot skip later comparisons. Invalid draw/slot/material
rows record an unevaluated failure and do not call an invalid API. The case
lifecycle comparisons likewise run independently for every touched unique
case material.

Immediately before the actual lifecycle push,the observer reads every live
registered material in the exact fields/registry iteration that the writer
will visit. It deduplicates resources by exact signed instance ID and records
all write-floor memberships. For case materials already poisoned,it uses the
original value actually captured before poisoning; unpoisoned registered
materials use their current original value. This reconstructs the complete
pre-probe value map without inventing defaults. A live non-ShaderMaterial
registry entry is an explicit failed precondition for the writer; it does not
produce a passing partial census.

The real production `_push_living_lifecycle` then runs for every actual field.
Each registered resource is evaluated against the last field that writes it
in that source order. The observer restores the deduplicated union of case
materials and every registry target,before restoring force/refresh state and
returning to the next frame. Full registry before/after uniform maps must match
in the same aggregate assertion. Local material references expire with the
synchronous call; returned evidence contains only IDs,scalar values and string
floor memberships.

The returned `comparison_contract` is
`independent_rows_and_all_registry_lifecycle_v2`:

| Field | Meaning and required green relationship |
| --- | --- |
| case_comparisons | One row per actual finish/prop row; identity_matches and state_matches are independently computed |
| case_rows_planned / case_rows_evaluated | Planned count equals current actual case-row population; green evaluates every row |
| case_comparisons_evaluated | Twice evaluated rows: one identity and one state comparison each |
| case_materials_touched | Unique actual installed case resources poisoned/restored; not a pass count |
| case_lifecycle_comparisons / case_lifecycle_materials_evaluated | One independently evaluated lifecycle result per unique touched case resource |
| registry_live_slots | All live registry slots the actual writer would visit,including aliases |
| registry_unique_materials | Deduplicated valid ShaderMaterial write targets |
| registry_lifecycle_comparisons / registry_lifecycle_materials_evaluated | One evaluated post-push result per unique registry target |
| registry_lifecycle_before / registry_lifecycle_after | Complete pre-probe/restored values and write-floor memberships for all those targets |
| registry_lifecycle_restored / registry_restore_failures | Exact map equality and explicit differing target IDs; green requires true/empty |
| lifecycle_restore_union_size | Unique union of case-poisoned and registry-written targets |
| tested_current_materials | Retained legacy field,explicitly documented as unique case materials touched |

Counts depend on the real admitted/current materials; do not pin them to the
960/967 case resources from the failed c264 world. Full case comparison and
registry arrays permit exact checks against the per-stage actual census.

Use this revision's `gate/proposed/classify_result.py`. Its copied classifier
remains byte-identical,while the material gate extends the exact signed-ID
parent gate with `observer_gate.validate_v2_refresh`. All earlier owner,
native,facts,threshold and retirement predicates remain in force.
The additive validator requires exact per-case row coverage and signed IDs,
both independent flags and actual/expected numeric agreement,all planned and
evaluated counts,case lifecycle coverage,and complete registry target/floor
membership. It rejects missing or unexecuted case comparisons,case lifecycle
comparisons and registry lifecycle/restoration payloads with separate reasons.
The full registry before/after map must cover every current registered ID and
match exactly,including non-case targets. Plausible reduced counts cannot
conceal a dropped row or target. All earlier raw classifications are retained.

Five pure source controls verify the exact parent/narrow method delta,
unchanged assertions/timers,independent evaluation ordering,complete registry
capture/union restoration ordering,and synchronous refusal/cleanup. They are
static preparation proofs,not Godot parser/runtime clearance. The final
admission/ancestry owner hash and explicit engine lane handoff are still
required before the next actual-building run. No new invocation scaffold or
production file is installed by this package.

Twelve additive-gate controls cover complete signed-ID payloads,missing
contracts/arrays,plausible truncation,unexecuted or failed comparisons beneath
a passed aggregate,wrong state/path/count,duplicate lifecycle records,
non-case restoration drift,missing target/value/floor memberships,and invalid
zero/bool/out-of-range IDs. They use controlled synthetic gate metadata and
do not claim runtime success. The root still reviews the actual raw rows and
images after execution.
