The full02 run remains a material-composition failure. It completed314 checks
with eight aggregate material failures,18 captured images and actual root/
material retirement. All305 original renderer checks plus the added material
retirement check passed. The engine returned1 after93.451seconds. No project
error header, native unpair/soft-shadow error or retention diagnostic was
reported. Raw loader,RGB8 conversion and found-piece placement warnings remain
in the unchanged evidence; this review does not suppress them.

The repeated ownership failures are concrete installed-consumer mismatches.
In the initial and six cycle observations,four F03/Service_3A_w0_wardrobe leaf
draws appear in Mina's F02 case rows and four F05/Service_5A_w0_wardrobe leaves
appear in Peter's F04 rows; their row materials are no longer installed.
Four F06/Service_6B_aw_wardrobe leaves sample the wrong/unbound field. F02 and
F04 registries each retain four extra resources. F06 has four extra and four
missing resources. Registry slot counts remain unique and contain no invalid
resources, so the previous duplicate-slot growth is not the observed failure.

The final observation differs: the four F06 leaves now appear as stale Cal
rows, yielding12 stale rows and only F02/F04 registry mismatches. F06's
registry is then correct. The complete per-stage paths and exact signed IDs
are preserved in runtime_analysis.json; this temporal difference must not be
flattened into a constant twelve-row or three-registry claim.

All six actual case finish sets are live,guarded and correctly marked in every
observation. The production SurfacePass cache consumers remain installed with
the current governor budget. Actual private-world and actor populations are
nonempty with no observed field/owner contamination. These are scoped positive
observations within an overall red run.

The material census reads the actual floor descendants and current full/surface
overrides. It then compares them to the real owner registry. It does not invent
wardrobe row ownership,manufacture markers,reset case facts or reuse another
root. The reported stale rows test direct installed resource identity. Thus a
negative signed ID does not explain away their live=false value or the exact
registry set differences.

Production source review supports competing floor ownership: reach_props scans
the whole root and permits the authored vertical interval to overlap adjacent
floor geometry; existing encroachment_case metadata is retained without new
spatial eligibility checks. The later _bind_storey prop_rows pass,described as
handling root-level props,also accepts descendants of another floor owner.
Independent review found field order F02,F04,F06,F05,F03: later actual ancestor
binding can replace an earlier case-row material; the Cal pass can likewise
overwrite a material after F06's registry rebuild. The exact capture lacks a
per-leaf world-AABB receipt,so this is source-supported ownership attribution,
not a measured claim about the first geometric boundary crossing.

Two instrument limitations remain distinct from those production failures:

- Both refresh probes passed their real safe-threshold precondition and
  restored recorded case material snapshots,forces,intensities,facts and all
  beachhead identities. The grouped gate text `actual state/lifecycle/identity
  restoration incomplete` also covers failed propagation; it does not prove
  restoration was lost in this run.
- `okay = okay and ...` short-circuits subsequent comparisons after the first
  mismatch. tested_current_materials counts deduplicated resources touched by
  poisoning/restoration,not independently passing comparisons. Furthermore,
  _push_living_lifecycle writes every storey registry material,while uniform
  snapshots cover case materials; all-registry uniform restoration is not
  directly measured. The aggregate red is valid,but per-material failure
  attribution and a broad restoration claim would exceed this instrument.

All1,048 observed unique case material WeakRefs were released at actual root
retirement. This review did not inspect the18PNG and adds no visual acceptance.
It makes no release performance claim and proposes no live source change.
