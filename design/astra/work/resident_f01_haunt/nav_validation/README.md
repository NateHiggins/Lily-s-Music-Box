Prepared nav validation orchestration. Root authorized execution only after exclusive lane handoff; no command in this package was run against Godot during preparation.

Use these commands in order from `C:\PleaseRemainOnTheLine-astra`. Every command stops on an unexpected result; review retained evidence before a new attempt. Do not rerun the same phase over existing results.

```powershell
python -B design/astra/work/resident_f01_haunt/nav_validation/execute.py focused nav_direct_01
python -B design/astra/work/resident_f01_haunt/nav_validation/execute.py lifecycle nav_direct_01
python -B design/astra/work/resident_f01_haunt/nav_validation/execute.py nav-control nav_direct_01
python -B design/astra/work/resident_f01_haunt/nav_validation/execute.py coordinate-control nav_direct_01
python -B design/astra/work/resident_f01_haunt/nav_validation/execute.py regressions nav_direct_01
```

`execute.py plan nav_direct_01` prints the structured command/run plan without installing or launching anything. `test_controls.py` runs only offline parser/filesystem/PowerShell stubs.

| Phase | Exact run name | Required result |
| --- | --- | --- |
| focused | 01_original_focused | original nav362de8; 25/33, native8, diagnostic1, exact declared control0 |
| focused | 02_candidate_focused | nav9e0840; 33/33, native0, diagnostic0 |
| lifecycle | 03_candidate_lifecycle | actual V1 owner arrival, dwell, real return/home and retirement; all31 names; native0 |
| nav-control | 04_nav_call_omission_lifecycle | remove only two-line shortcut call, retain all other nav fixes; actual arrival/retirement,20/21, sole swept-public-route failure; native1/diagnostic1 |
| nav-control | 05_nav_restored_lifecycle | exact nav9e0840 restoration; all31 actual lifecycle names; native0 |
| coordinate-control | 06_coordinate_omission_lifecycle | revert only transient guest haunt coordinate; actual arrival/retirement,15/21, six exact unsafe-target failures; native1/diagnostic1 |
| coordinate-control | 07_coordinate_restored_lifecycle | exact routines2a3433 restoration; all31 actual lifecycle names; native0 |
| regressions | 08_waiting_regression | unchanged40 named historical waiting assertions; native0 |
| regressions | 09_passage_regression | unchanged26 named PassageNav assertions; native0 |

The first phase installs only the two new direct fixture files after verifying every existing owner. The original935fde fixture is preserved in `fixture_revision_02/935fde_original.gd`. Revised d1985e50 adds exact1.25 positive admission,1.2501 refusal,>.08 foot offset and radius-shrunk outer slab refusal in both directions; nav9e0840 is unchanged. Original direct red must fail exactly eight names in contracts.json; its actual world-retirement check must pass. Expected outcomes are requirements, not claimed observations.

Each run uses fresh APPDATA, the unchanged serial runner and authority's byte-identical isolated `runner_bridge.ps1` (bb727e17…), passing JSON plus real ExtraArgs array splatting. Continue is scoped inside that separate child process, preserving runner Write-Error→124. CmdletBinding stubs verify array fidelity, timeout124 and absent-exit refusal. No direct `-File runner -ExtraArgs --verbose` binding shortcut is used. All runs use windowed Vulkan Forward+, Dummy audio and the same explicit1280x720 settings. Runtime overrides are cleared and recorded. No fixture pose, speed, lift readiness or travel timer is altered.

The canonical runner owns the fresh process census/mutex and180-second ceiling. Exact child PID ancestry is also recorded and required. Runner73/124/78 and absent-completion refusals have separate classifications; an actual fixture exit is reported only when completion is structurally proved and the runner did not report refusal/timeout. Unexpected native/project/engine errors stop the next phase. Exact reviewed warnings remain named/countable debt, including inherited host warnings, RGB conversion and found-art placement; unknown warnings, pairing and retention are not admitted. Simple durations confer no performance acceptance.

The strict assay requires an existing readable JSON for direct/haunt/waiting, exact unique named rows, raw assertion order/content equal to JSON, exact footer/count and the actual retired-world/root check where that fixture claims it. Passage retains its existing26 exact raw names/footer and has no invented JSON or explicit-retirement claim. Silent focused JSON-write failure is rejected outside the fixture. It never becomes green just because its native exit was0.

The batch freezes all tracked/nonignored game bytes after installing the direct fixture. Across phases only the exact nav/routines variants are allowed. Each run stores full raw authored text/test copies before and after, complete game and runtime manifests, binary asset hashes, installed console/engine hashes, bridge/wrapper/assessor/parser/runner copies, structured invocation, PID observations and raw logs. Consumed artifact bindings are mandatory. Phase receipts bind each result hash, re-evaluate raw checks, and enforce the original batch engine/source census before dependent runs.

Omission writes are restored in finally. A failed omission still restores exact candidate bytes before returning for review; a failed restoration or unexpected foreign owner edit cannot yield a completed phase or launch a dependent run. Restoration controls exercise the actual controller against a temporary filesystem, never game. There is no promise of a restored runtime proof if the omission itself produced an unexpected error: bytes are restored, then work stops for review.

Offline validation is in `offline_validation_01`:18 controls pass. The same18 tests against a separate copy with the required-artifact guard removed fail the omitted-log-binding test. These controls do not establish GDScript syntax, actual Body travel, V1 rendering or runtime performance. Historical original_haunt_01 launch refusal, original_haunt_02 unsafe target, candidate_haunt_01 nav detour, and first strict warning rejection remain unchanged outside this package.

Execution amendment: first batch nav_direct_01 retained a GDScript parse error at weakref inference and canonical timeout124, with no JSON or accepted original red. fixture_revision_03 changes only that local declaration to explicit WeakRef; current fixture is f1207275a4761ef8319547f0b753d0d12f27d3575aa1885b7984a3c4cff760ba. All33 labels and8 declared original failures are unchanged. Its18 offline controls passed again. The next fresh batch is nav_direct_02; prior rows above remain expected outcomes until that run completes.

Second execution amendment: nav_direct_02 retained synthetic room kind/F01 access errors and timeout124; no candidate nav was installed. fixture_revision_04 adds only the required kind:room key. Current fixture bda5bf9892c756ebf31448b11e4138762027dacd9712bf86553ff55253c45f0a retains33 checks and8 declared original failures; next fresh batch nav_direct_03. All earlier sources and receipts remain unchanged.
