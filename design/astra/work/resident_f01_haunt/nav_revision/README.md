This is an **unapplied, unrun proposal**. Live ResidentNav remains SHA256 `362de8a26f67741960e0721a9da7ae95418eb5904db5c8deaf389d834b5baec8`. Live routines retain the authorized F01 destination candidate `2a34334b157d0c895437b03d9d88c6a21cfc5645329bb0104bee248540642d3a`. No Godot process was started to prepare this directory.

The retained actual V1 result `design/astra/evidence/resident_f01_haunt/candidate_haunt_01/result.json` is native 1 / diagnostic gate 1, 20 passed and 1 failed. Its new public endpoint has real slab support and no actual Body overlap. The failing path is `(1.925,.03,7.4) -> (1.925,0,6.75) -> (2.2,.03,8.3)`: the nearest-pair graph unnecessarily visits a lift-door anchor before returning south to the public destination. The actual Body sweep hits landing panels. Actual lift arrival and root retirement passed; dwell/return are deliberately unproved after that unsafe path.

`resident_nav_direct_segment.patch` changes only ResidentNav. The proposed scene and fixture add synthetic admission controls. Root review and a fresh engine/source handoff are required before any live application or runtime claim.

The direct leg is limited to 1.25 metres, one floor, equal endpoint Y within 1 mm, and a foot offset 15–80 mm above the authored flat slab datum. The established leg is about 0.9411 m. The existing graph remains the fallback whenever a new check refuses admission; its existing door policy and endpoint selection are unchanged. That fallback is not newly certified as capsule-safe.

Admission requires all of these checks:

- The navigation node completed collision validation in its current viewport's World3D. A WeakRef does not retain the world. Build, validation entry and tree exit clear admission; another world, null validation and freed world cannot authorize a direct leg.
- The existing authored wall test passes. Both endpoint centres lie inside the same radius-shrunk rectangular slab. Because that rectangle is convex, the complete segment's envelope footprint remains inside it. Segment clipping against each radius-expanded hole excludes the complete segment, including narrow holes and boundary contact.
- A conservative capsule of radius .33 m, height 1.65 m and centre +.825 m has no start/end overlap and `cast_motion` returns exactly `[1,1]`. The query uses all body layers, a 1 mm margin and no collider or DoorProp exemptions. It does not collide with nonphysical Areas. Ordinary layer filtering still applies: production resident Body nodes have layer 0 and are invisible to these queries. This is not resident-to-resident overlap or crowd-avoidance proof.
- Real downward support rays at the centre and eight rim points, every at most .20 m, hit upward-facing surfaces at the authored slab height. This is sampled physical support; it is not a proof against every possible unmodelled sub-sample floor hole. Authored holes are excluded continuously. At most 72 support rays are made for a qualifying leg, plus two overlap queries and one cast. No performance acceptance is inferred.

The conservative capsule is not the actual resident shape. The actual constructors `game/scripts/characters/npc_placeholder.gd:58` and `animated_resident.gd:99` create radius .28 m, height 1.55 m and centre +.775 m. Their cap centres are .28 and 1.27 m; the envelope's are .33 and 1.32 m. Each centre separation .05 plus radius .28 is at most .33, proving containment of the entire upright capsule by convexity. The existing F01 test independently obtains and queries its actual owner's shape. Its 0.03 m foot offset is maintained by the ordinary route follower; no actor movement or elevator state is changed here.

The Godot API specifically documents that `cast_motion` ignores existing overlap; that is why independent endpoint overlap checks are mandatory. API references: [PhysicsDirectSpaceState3D](https://docs.godotengine.org/en/stable/classes/class_physicsdirectspacestate3d.html), [PhysicsRayQueryParameters3D](https://docs.godotengine.org/en/stable/classes/class_physicsrayqueryparameters3d.html). Current installed-engine behavior still needs the focused runtime controls.

The proposed `ResidentNavDirectSegmentTest.tscn` uses an isolated SubViewport World3D, an actual StaticBody floor and one-node graph. It checks admission before/after validation; direct and reverse endpoints; length/height/foot bounds; envelope containment; a side jamb whose centre ray is clear; start/end overlap; an actual DoorProp type with construction disabled and a synthetic leaf collider; wall-only obstruction; narrow/off-centre authored holes; missing authored support; removed/restored physical floor; a sampled physical gap; build/null/wrong-world revocation; tree exit; and non-retention of retired World3D. This fixture makes no actor, elapsed travel, visual, renderer retirement or performance claim. Its original-source execution must fail positive direct-admission controls without unknown errors, and the candidate must pass every control.

The next actual consumer sequence, subject to source/lane handoff, is:

1. Install only the new focused fixture/scene and retain original-nav controls. Apply reviewed nav only, then run focused candidate. Any parse/lifecycle red is retained before a narrow fixture repair.
2. Run unchanged actual `ResidentF01HauntTest.tscn` with candidate routines/nav, fresh APPDATA, unchanged serial runner. It must reach actual F01 arrival, pass actual Body sweep, dwell on real physics time, return on the real lift, and retire the actual V1 root. A direct-admission helper omission and restored candidate establish the nav repair; the earlier coordinate-only red remains preserved.
3. Run existing `ResidentLiftWaitingTest.tscn` and `PassageNavTest.tscn` with all existing assertions. The historical lift fixture uses its disclosed audit capsule, not actual Body identity. Do not weaken either regression.
4. Run the independent old-haunt omission only after the current full candidate is actually green, preserving exact restored bytes and current final fixtures. Its strict assay must retain the known warning debt separately and reject unknown diagnostics. No retrospective green can replace original launch/strict-warning rejections.

The wrapper's inherited PowerShell Stop/runner timeout interaction is being prepared as a separate offline boundary repair. Do not use the old wrapper's timeout exit as an exact native-engine exit. Current completed raw receipts and source hashes remain historical, unchanged.

Independent read-only review by authority_debt found no definite source/API, weak-world or rectangle-clipping blocker. It explicitly identified and this proposal now records the layer-zero resident-body scope limit. This is source review only, not GDScript parse or runtime acceptance.

The proposal preserves the original ResidentNav CRLF bytes. An initial LF review patch could not apply to the raw archived copy without whitespace normalization; that preparation failure is retained in patch_validation.json. The final byte-preserving patch passes ordinary git apply --check against the archived original. No patch was applied to game.
