# Independent named-stage review

**Clear** at `77dc5f75cd7cb68be6d1f3d0be4f57ff1b06c3d4`: the actual 154 staged paths exactly match the pathspec and staging receipt. All 152 selected raw hashes match, as do the selection/pathspec's own staging rows and every staged clean-filtered Git blob.

The ten live art/tools files equal clean C1 `c34ad283df148496fbd98e68638470835c930c5c` Git blobs. There are zero staged game paths. All 17 protected files match their raw hashes and HEAD/index/working/C1 Git blobs. Optional provider and V2 proposals are excluded.

No export was rerun or reinterpreted. Generated geometry and texture files remain in their external output directories and are bound by retained receipts; this review does not claim generic recursive receipt-schema completeness. C1 remains human-pending, with no production provider, default or selector adoption. Exact bindings are in `review.json`; verification was read-only and performed no staging or commit.
