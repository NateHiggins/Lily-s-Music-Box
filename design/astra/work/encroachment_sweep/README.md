# One-census encroachment sweep preparation

Not installed or executed. This is a separate candidate on top of the reviewed material ownership/registration repair, which must first be exercised with its focused original-source red.

The composed phase probe attributes roughly 523 ms to `reach_props` and its nested work, but does not establish how much duplicate registration versus mesh classification contributes. Measure the ownership repair alone before deciding to apply this candidate.

The candidate takes one transient mesh census per normal call. Existing case metadata remains authoritative; an unclaimed mesh goes to the first matching authored case in the same dictionary order as the original. Shader, bounds, case masks, living-field parameters, late material discovery, governor behavior and final storey refresh remain. No callback is skipped and no long-lived topology or transform cache is introduced.

Required comparisons include exact per-case installed materials/parameters and material-sharing relationships; overlapping case bounds and reversed authoring order; already-owned and unknown-case materials; late meshes, moved unclaimed meshes, replaced materials and the existing world/actor boundary. After focused equivalence and adversarial controls, use an uninstrumented real-root transition measurement and inspect actual material/state presentation. A faster scan that loses live consumers is a failure.
