# Floor-aware sweep equivalence — prepared, engine unrun

This preserved revision compares exact floor-admission owner
`2a440504ab80404f4d9f4ab16138df7db9255801ca879ff91aabafe3c7f4d0ca`
with one-census candidate
`1a1b067ff5b95c03fea739747c97deaaf9621e03a635b318b3379cef07bd3776`.
The production proposal is in `../../revisions/ownership_2a440_01`.
Only `reach_props` differs. Its original floor-provider helpers are preserved;
known case metadata and every first-claim candidate retain the real-floor
admission check. Floor owners are captured once, and the mesh census is fresh
on every call. Authored priority, unknown owners, root-level moved metadata,
normal callbacks, state updates and final registry refresh remain unchanged.

The six test files reuse the historical 171-check specimen and its exact
ordered gate contract. Only four adapted source copies change. Each removes
the global class declaration and normalizes line endings; all production
methods and preloads remain intact. `priority.patch` reverses only first-claim
case order; `drop_late.patch` caches only the first mesh census. Both controls
retain the corrected floor checks. They must complete all 171 checks with the
already-declared selective failures and clean retirement/diagnostics.

The specimen exercises actual SurfacePass-produced materials, field/state
consumers, ownership/sharing, both authored orders, late/moved/replaced draws,
private-world and actor exclusions, detachment and immediate lifecycle. Its
floor containers and case bounds are synthetic inputs. It does not build the
actual apartment markers or execute the queued governor callback. Texture
class/path/size normalization is scoped source equivalence, not arbitrary
texture-pixel equivalence. Actual full-root ownership has a separate baseline
receipt; candidate composed rendering and matched cost remain pending.

No live files are installed by preparation. After an explicit lane/source
handoff, reuse the preserved runtime controller in `../runtime_plan_01`,
rebased in a new sibling controller revision to this sealed package and fresh
evidence batch. Install only these six test files, then run candidate,
priority, drop_late and exact restored candidate. Preserve the actual engine
and diagnostic exits, source snapshots, fresh profiles and all failed runs.
Do not replace live production ownership for this focused comparison.

The historical 7c54 and 69e28e packages remain unchanged. This is new source
preparation, not an adoption or a transfer of their runtime results.
