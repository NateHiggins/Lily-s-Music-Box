# Focused one-census execution plan — prepared, engine unrun

Only six test files are installed from the sealed sibling ownership_7c54c_01 package. The live production owner stays 7c54c18154ccf8de8c3bce13a356329344dec19411adb2f1237425ae99eb90db throughout. All four source variants already coexist as separately loaded test copies; priority/drop_late select a child-process environment value, not a production source swap. The final restored phase selects the exact same candidate bytes and requires both intervening selective reds and all preceding source/artifact bindings.

Run each command from C:/PleaseRemainOnTheLine-astra after exclusive lane/source handoff, inspect its result before advancing:

```powershell
python -B design/astra/work/encroachment_sweep/equivalence_revisions/runtime_plan_01/execute.py install ownership_7c54c_01
python -B design/astra/work/encroachment_sweep/equivalence_revisions/runtime_plan_01/execute.py candidate ownership_7c54c_01
python -B design/astra/work/encroachment_sweep/equivalence_revisions/runtime_plan_01/execute.py priority ownership_7c54c_01
python -B design/astra/work/encroachment_sweep/equivalence_revisions/runtime_plan_01/execute.py drop_late ownership_7c54c_01
python -B design/astra/work/encroachment_sweep/equivalence_revisions/runtime_plan_01/execute.py restored ownership_7c54c_01
```

Each command must return controller 0. Candidate/restored require actual runner/native fixture 0 and diagnostic gate 0. Priority/drop_late require actual runner/native fixture 1 and diagnostic gate 1 while the selective-red controller returns 0. All require the exact 171 ordered JSON labels, raw stream-specific assertions, 12 comparison payloads, complete four-root/eight-owner retirement, unchanged source/engine/instruments and observed wrapper→console→engine ancestry. Expected failure counts are not guessed: the sealed selective label contract requires both orders' named change, prohibits unrelated/baseline failures, and requires first/repeated phases unchanged for drop_late.

The unchanged tools/run_godot_serial.ps1 runs headless --verbose with a 60-second ceiling via the exact tested isolated JSON-array-splat bridge. No warning/error allowlist is imported. All stderr and stdout remain raw. Wrapper 73/78/124 are lane refusal/cannot-run/timeout, never expected assertion reds; incomplete/parse results do not establish a completed native fixture exit. Record failures in their original fresh folder; never overwrite or retry a name. A fixture/contract correction needs a new sibling revision/batch.

Evidence is created under design/astra/evidence/encroachment_sweep/equivalence_runtime/ownership_7c54c_01. install.json binds the six target bytes, previous absent/exact states, complete post-install game hash manifest, HEAD and package. Run receipts live at runs/01_candidate/result.json, 02_priority/result.json, 03_drop_late/result.json, and 04_restored/result.json. Each retains command/invocation/environment, fresh APPDATA, raw Godot/runner streams, scene shots/equivalence.json, PID observations, before/after complete game and runtime-text manifests, selected raw source copies, game diffs, instruments and engine fingerprints. All recorded artifacts except APPDATA are hash-mapped; required artifact omission, stale copied source and log mutation refuse progression.

The complete game manifests bind tracked/nonignored game bytes including binary assets; ignored .godot imports/cache are excluded. Runtime-text manifests include shaders and tests. The exact six tests and eight direct owner/dependency inputs are copied before/after; unchanged unrelated sources remain reconstructible from the observed Git HEAD plus explicit working overlay and full file hashes. This avoids duplicating a full authored source archive each run. Binary pixels/renderer output are outside this focused headless proof.

No game source is rewritten between runs. Failed runs therefore leave the same candidate test bytes installed for review. Optional explicit cleanup `python -B design/astra/work/encroachment_sweep/equivalence_revisions/runtime_plan_01/execute.py restore-preinstall ownership_7c54c_01` restores exactly the prior six test states. It first refuses any modified target or damaged backup, checks each resolved target is inside game/tests, and never removes directories or changes production. Existing unknown files are refused before installation; old absent targets are individually unlinked only during authorized cleanup. No automatic cleanup obscures a failed run.

Independent review: the live owner is exact 7c54c, its candidate differs only inside reach_props, and fixture-adapter/control bytes match the sealed source receipt. No definite current GDScript/API blocker found. Queued nodes stay live throughout synchronous exercise and each specimen root frees before deferred retirement. Shader nullable checks and Variant-sensitive assignments are guarded. Minimal classifier dictionaries are consumed only by lifecycle classification, with no field tick. Root and authority reviews agree on this bounded reading.

Coverage limits: the specimen uses SurfacePass.surface_for plus real reach_props/_bind_storey/_apply_prop_states/_push_living_lifecycle, synthetic units and manual full-override changes. It does not invoke build(), real glTF/world assembly, queued governor callbacks, real material tier transitions, rendered pixels or sampled performance. The separate material-binding 36-check proof owns the queued callback seam; later composed ownership/material/render measurements remain required. Shared textures are compared through direct pointers and class/path/dimensions, not arbitrary pixel identity. At this preparation no runtime or rendering result is claimed.

Offline validation uses synthetic receipts and temporary non-Godot PowerShell stubs. The copied sealed gate remains byte-identical; assess.py adds raw assertion/stream, type, variant, source/PID/engine and runner-condition checks without loosening its selective failures.
