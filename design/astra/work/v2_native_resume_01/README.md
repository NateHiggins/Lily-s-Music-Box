# Native V2 verification resumed

Owner explicitly reauthorized Godot. All launches used the unchanged shared
serial runner; the process lane was empty before the first and after the last
run. No foreign process stopped. Existing test launch scenes are unchanged.

Repaired numeric drain-side validation: JSON floats failed integer-array
membership. Numeric type and magnitude now enforce left/right, alongside the
integral-setting guard. Five variants per reconstruction exercise this repair.
The fixture also narrows environment ownership to the waking viewport, allowing
the service display's separate subviewport environment, and waits 250 ms after
freeing each world so AudioServer can retire stopped decoders. Both fixtures
disable persistence before resetting campaign data. No disk-save proof claimed.

Final rendered connected world: 212 checks, zero failures, empty stderr.
Shop simulation: 19 checks, zero failures, empty stderr. Verbose headless
connected-world run also finished without leaks or stderr. These results cover
bounded composition, standing capsules/floor support, direct WC/wardrobe actions
and simulation. They do not establish actual controller targeting or full routes.

Five native 3B views were inspected; the final kitchen view was rechecked.
Geometry is present, but bedroom/wardrobe/bath are too dark, surfaces remain
blockout finishes and bench dressing is incomplete. Visual acceptance fails.
Fixed diagnostic cameras retain the actual service-device overlay. These are
not a walkthrough or a controlled time-of-day lighting comparison.

Evidence and source hashes: ../../evidence/v2_native_resume_01/receipt.json.
The before manifest includes initial persistence guards; only two game files
changed thereafter. Three game files differ from HEAD. Final rendered evidence
binds after.json; earlier logs are preserved diagnostics without individual
source seals. Historical source-only and native receipts remain unmodified.

Next: room/task lighting at fixed campaign time, then controller routes and
targeting. Full cutover remains open; V1 remains default. F01 provider native
permission has resumed but its old continuation baselines remain stale.
