# Actual-root Vulkan boundary fixture

Status: **installed; two mixed-cause failed V1 runs retained; revised viewport guard and fixture not yet run**. See `../../reviews/vulkan_composed_diagnostic_review.md` and `../../evidence/vulkan_composed/failed_runs_visual_review.json` for exact source-bound results and inspection of all 29 retained PNGs. Raw crashed; helper-only candidate completed with no native pairing/underflow diagnostics but exposed a real arcade viewport-indexing error and a separate resident-route error. Neither is an intended-only renderer red or current-source green. The new 25 synthetic parser/source-transform controls passed with actual exit0; exact sources/logs are retained in `../../evidence/vulkan_composed/viewport_boundary_repair/parser_controls`. Earlier 13/14-control receipts remain intact. Dream owns the engine lane for focused route diagnosis; game inputs are frozen until explicit handoff. The unchanged serial runner's 180-second cap is retained.

The live consumer boundary is asymmetric. `BuildingRoot` (V1) owns `_apply_visibility`, `_zone_toggle`, StreetCore and Passage indices, and the F04 actor registry. `OrisonV2RuntimeRoot` owns none of those gates: its floor map currently points to one blockout. The V1 case therefore proves the actual zone path. The V2 case constructs the actual selected root through CampaignShell, resolves its real F04 semantic stance/terminal, renders it and its shared-world phone view, and retires the real shell. It explicitly records that V2 StreetCore/Passage applicability is absent. Nothing transplants V1 visibility logic into V2 or describes this as two-root zone parity.

Prepared source paths:

- `game/tests/vulkan_composed_root_test.gd` and `game/tests/VulkanComposedRootTest.tscn`, relative to this work directory, are the eventual test installation paths.
- `run_case.py` runs a selected root/declared raw/candidate/omission/viewport_omission source through the unchanged serial runner, binds both installed engine binaries using PowerShell resolution, and hashes all tracked/untracked game/tool inputs including binary assets before and after. Changed inputs are copied. It refuses a variant inconsistent with the live helper operation and viewport boundary, makes no game edits, isolates APPDATA, retains exact stdout/stderr and actual child exit, and applies the independent diagnostic gate after process exit.
- `variant_case.py` requires the explicit current candidate SHA256, derives each control from those exact current bytes and restores them in `finally`. Raw removes only the helper and restores the two original mask assignments; omission removes only the same-scenario rebind. Both retain the new viewport boundary and all unrelated current changes. Viewport omission removes only the new guard while retaining the helper.
- `gate.py` reports unpaired-light errors and the independent `geom->softshadow_count==0 - BUG!` underflow separately. Only exact previously identified host loader-manifest errors are classified as inherited. Every other ERROR, retention diagnostic, missing receipt/capture, incomplete retirement, renderer fallback, or source/engine change fails. Functional exit0 never overrides this gate.
- `test_gate.py` and `test_variant_case.py` contain 25 passing synthetic log/receipt and exact variant-transform controls. The viewport red requires a still-live baseline mask change in its own scenario; missing IDs, unrelated route/native errors, source drift, incomplete retirement or missing captures cannot validate it. Every expected red still has diagnostic gate1. These controls are not rendering evidence.
- `summarize_cost.py` separates warmup/teardown from synchronous apply cost, no-op script cost, next rendered-frame waits, measured viewport CPU/GPU times and render populations. It reports n/median/p95/max without inventing a release threshold.

V1 uses the actual root inside CampaignShell and the production player camera. STREET, PASSAGE, ORISON, F04 corridor and Harukiya coordinates come from existing StreetCore/Passage tests, `perf_probe.gd`, and `coverage_shot.gd`; it preserves distinct player/zone probe points and camera eye heights. Six matched cycles execute STREETâ†’PASSAGEâ†’STREETâ†’ORISONâ†’F04â†’ORISON, with four rendered frames per transition. Cold station warmup and a final blocked Passage stance are separately labeled and excluded from the 36-transition measured set. The production clock is explicitly frozen at November10,1928 03:00; schedule dispatch is disabled and authored night presentation is selected. Only player physics/input is stopped to keep explicit viewpoints authoritative. Root physics remains active because it also owns SurfacePass.govern; the fixed player supplies the same visibility zone to the production cache. Lighting/shadows, actor processing, rendering backend, scene geometry and hierarchy remain in production form. These camera teleports are controlled composition, not walking.

Each measured frame records the live surface governor's parallax budget, prop tier, step count and queued material work. The wrapper clears inherited PERF_/SURFACE_ experiment overrides instead of silently pinning the governor. These recorded adaptive material states and render populations must be considered when comparing timings; fixed script source alone does not imply identical adaptive visual detail. Arcade geometry is first censused after the actual Harukiya near-cabinet warmup and eight further rendered frames, preserving production distance-triggered lazy boot.

The real StreetCore/Passage overlap must exist and preserve its exact original mask when both gates release. Repeated physics requests must hit the existing visibility-signature cache. Sixteen explicit same-region scans must preserve tables and masks; their script cost is measured after the rendered transition window. This observes no-op semantics and the cache, **not a native scenario-call count**; the candidate's unchanged-mask guard needs separate source review or disclosed instrumentation before that stronger claim.

F04 is checked through the actual `functional_props_by_floor` population and `F04_B_MONITOR_01` SignalTerminalProp, not newly invented proxy actors. From STREET the exterior owner rule keeps F04 actors available; PASSAGE/lobby hides them; F04 restores them without forcing F01 actors visible. Immediate actor/property checks are distinct from frame submission census and agent image review. Existing StreetCoreVisibilityTest and PassageVisibilityTest retain their fuller excluded-glass, joinery, neon, shell and late-builder assertions in the prior smoke sequence; this fixture does not replace them.

During preparation the smoke owner reported StreetCore's existing Harukiya `>250` population assertion failed (27 pass/1 fail) with native unpair/underflow counts zero. Root subsequently reported the same one StreetCore/two Passage failures on matched unmodified source and is coordinating an explicit03:00 test-time pin without relaxing assertions. This fixture records the actual total and Harukiya subset populations rather than changing that existing threshold; a green narrower fixture cannot clear the outstanding broader assertions. At root direction this fixture now also pins03:00; its first completed candidate census has247eligible/indexed, matching the Street smoke count. The inherited1.6-second delay plus eight rendered frames is a settling heuristic, not an authoritative asynchronous-builder completion signal. Matched cases must inspect readiness/population differences before attributing a performance or architectural change to the layer helper.

Four harness-owned geometry controls call the actual V1 `_zone_toggle` for both blocker/release orders: authored mask33, authored zero mask, self-hidden geometry, and an invisible parent. They test repeated requests, exact mask restoration, an owner visibility change during blocking, table drain and an unrelated neighbor. These are direct helper-boundary controls, explicitly separate from real architectural populations. Their blocks are drained before free.

Relevant SubViewports have different contracts:

| Consumer | Fixture proof scope |
|---|---|
| PhoneCamera | Production implementation mounted by the harness through setup/bind_world/set_active/track. Its main-world view is captured through transitions. This is not the physical handset UI flow. |
| PlanarMirrorRenderer | Actual V1 owner and actual F04 MedicineCabinetProp. A camera pose derived from its real mirror plane must activate the production selector; the borrowed world/cull mask and both main/reflection PNGs are checked. |
| ArcadeMachine | Actual own-World3D consumer census. Surviving baseline geometry masks/world scenarios must remain unchanged and outside all V1 spatial indices. Legitimately retired gameplay identities are not claimed preserved. This is ownership isolation, not arcade gameplay acceptance. |
| Separate owned World3D | Explicit synthetic helper boundary with real lit geometry, nondefault mask33, repeated production `_zone_toggle` calls, rendered orange-pixel present/restored/absent controls, light deletion while blocked, table drain, and viewport retirement. It tests use of the node's own scenario independently of actual arcade content. |

After captures, the actual CampaignShell is queued for retirement with its production `_exit_tree` paths intact, four rendered frames and an additional short settle. It does not pre-clear renderer pairs, turn off shadow ownership, or call `shutdown_for_tests` to obtain clean output. Weak references verify actual shell/world destruction before writing the probe. External stderr still governs engine-side resources and delayed native errors. Stdout phase markers and separate stderr are retained; buffering prevents an invented exact native-error phase attribution.

The minimal decisive execution sequence after handoff is: install only the two prepared fixture files; preserve a fresh actual V1 raw run; apply the reviewed candidate unchanged; V1 candidate plus V2 actual-root control; omit only the candidate's pre-change `instance_set_scenario` operation in the same V1 consumer; restore and rerun candidate. All changes and helper restoration belong to the coordinated source owner. Each run starts in a fresh process/profile and must complete within180s. The wrapper itself never creates a variant or changes an engine. A raw/omission run remains diagnostic exit1 when its native red is reproduced; its observed expected-red field is not a green acceptance label. If candidate retains the independent soft-shadow underflow, the result remains red and the two defects must be diagnosed separately.

Before game execution, review/compile the prepared fixture under the newly granted lane. The parser controls already passed without using that lane. The actual arcade population, F04 medicine-cabinet registry, mirror selector and capture angles are runtime assertions, not assumed observations; missing consumers should fail visibly rather than be silently dropped. Image files still require direct inspection after capture. Compare matched source-bound distributions and draw populations before judging transition cost; six cycles are a smoke sample, and no human acceptance, release performance or full-game lifecycle clearance follows automatically.

Raw/omission can emit heavy native diagnostic traffic while candidate is quiet. That logging asymmetry itself affects wall/frame timing; preserve it rather than muting errors, and do not attribute the entire timing delta to the helper's scenario work. A second same-source candidate run can expose A/A variation if needed after the first bounded set. The script-only apply/no-op measurements and measured viewport times are reported separately for this reason; they are not interchangeable with end-to-end release frame cost.
