# Queued execution scopes after stable-source handoff

No live source was changed by this preparation. The queued GDScript is
`game/tests/vulkan_composed_root_test.gd` under this work directory, SHA256
`5ec63c36c6181b33c02bd2255039e9bf4e8db0c4465a45ac641d1481f422d6d3`.
The currently installed fixture remains `7651dce0…`. Exact queued sources,
pending diff and 31/31 Python controls (actual exit0) are in
`../../evidence/vulkan_composed/queued_scope_controls/receipt.json`.
This is not GDScript compilation or renderer proof.

Full candidate V1 remains the required complete case: all real root/zone/F04
checks, both independent blocker orders, actual mirror and PhoneCamera,
actual arcade lifecycle and two raw arcade viewport captures, the separate
own-world light/mask/deletion case, and actual shell/world retirement. Its
capture gate requires 18 exact names. V2 remains the actual selected-root
two-image composition/retirement control; StreetCore/Passage remain absent.

Two new images use the same actual cabinet's persistent ArcadeMachine viewport.
Selection is deterministic by authored path among currently live nearby cabinets
after the existing Harukiya warmup. The second capture follows the repeated
Passage sequence and return to the real near-camera activation radius, with eight
rendered frames to refresh the viewport. Neither boot nor update mode is forced.
This avoids treating a disabled cabinet's cached texture as fresh rendering.
Current boot/live state, population and world isolation accompany both images;
direct visual inspection remains required. The effective own world is resolved
with `SubViewport.find_world_3d()`, rather than its assigned `world_3d` property.

Raw/helper omission first retain their broader failed run exactly if they crash.
An access violation, missing final receipt, missing retirement, or unrelated
diagnostic can never validate an intended-only red. In that situation a fresh
`--scope root_retirement` case is available on the same actual production root
and same source variant. It executes the identical 36 measured transitions and
all actual-root consumers, plus direct blocker controls, then explicitly skips
the separate-world case and retires the actual shell. It retains a final blocked
Passage stance and 15 required captures. The receipt and stdout must both name
the excluded case. This scoped run is not evidence for separate-world teardown.
If it also crashes or reports unrelated errors, it too remains failed; the gate
will not reinterpret it as a completed red.

The intended-only unpair field requires engine exit0, all applicable functional
checks/captures/retirement, unchanged source/engine, the exact known unpair
signature and no other unexplained error. Independent soft-shadow underflow is
reported separately and prevents that intended-only classification. A valid
observed diagnostic red still returns diagnostic gate1. Full candidate clearance
has its own field and cannot be obtained from the smaller scope.

Both helper omission and raw source derive from the exact newly handed-off
candidate bytes and retain the corrected SubViewport boundary and unrelated
fixes. The separate `viewport_omission` restores only the missing viewport guard,
with the helper intact; it must expose a still-live own-world mask change while
all other checks and diagnostics remain clean. Every variant restores candidate
bytes in `finally` and records the exact restoration hash.

Only identical completed cycle/station/frame signatures from the 36 transitions
before the separate-world case are potential timing comparators. Warmup, image
capture, standalone controls and teardown are excluded. A crashed broad case's
walltime or partial progress arrays are never a completed cost comparator.
Native diagnostic volume, asynchronous actors, render population and recorded
adaptive SurfacePass settings remain confounders; no release-performance
threshold or human acceptance is inferred.

## Separately declared paired-bookkeeping negative contract

`pair_bookkeeping_case.py` is a pending, explicitly invoked sequence wrapper.
It does not change `gate.py`, `run_case.py`, `variant_case.py`, or their strict
unpair-only behavior. It runs one exact helper-operation omission and then the
exact restored candidate on the same actual V1 root and selected scope. Each
child retains the unchanged serial runner and its own 180-second cap. Invoke
only after exclusive lane/source handoff, using a new sequence name and that
handoff's exact candidate SHA256:

```powershell
python design/astra/work/vulkan_composed/pair_bookkeeping_case.py <fresh_name> --candidate-sha256 <handoff_sha256> --scope root_retirement
```

The full candidate remains independently required. The smaller negative scope
does not cover the deliberately excluded separate-world deletion case. A broad
crash is retained as failed evidence and is never repaired by relabeling its
diagnostics. If a new bounded attempt also crashes, lacks its final probe, fails
any actual check/capture/retirement, or emits unrelated diagnostics, the pair
remains incomplete or invalid.

The contract is declared before execution and binds the retained a13da4feb
scene-cull source by SHA256. It requires both exact native headers: stale light
set at `instance_set_base:616` or `instance_set_scenario:844`, and soft-shadow
underflow at `_instance_unpair:348`, all in `renderer_scene_cull.cpp`. Missing,
different or extra source locations are rejected. Counts remain separate;
underflow remains fatal to the original strict unpair-only predicate.

Per-run `pair_bookkeeping_omission_evidence_complete` is provisional.
`expected_pair_bookkeeping_red_observed` becomes true only after an actual
engine0 restored candidate has clean diagnostics and equivalent completed
functional/capture/retirement scope. The aggregate verifies exact helper-only
source transformation, retained viewport guard, both transaction restoration
hashes, unchanged other game/tool files, matching runner/engine bindings, run
order, and artifact hashes. The paired aggregate always remains diagnostic1 and
never grants candidate clearance. Synthetic parser controls do not prove engine
behavior; no paired runtime observation exists until this sequence is run and
its images are directly inspected.
