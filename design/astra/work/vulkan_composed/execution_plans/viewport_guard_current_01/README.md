# Next viewport boundary control â€” execution plan only

Use the existing `variant_case.py`, `run_case.py`, strict gate and unchanged serial runner. No new runtime framework or live edit was made for this plan. Run only after root grants the exclusive lane and final source handoff. Root's active d67 material run must finish and restore its fixture first.

The recommended isolated pair uses the unchanged base fixture `5ec63c36â€¦`, scene `b9508089â€¦`, current root `cde44cd1â€¦` and **full V1 scope**: 305 checks, 18 captures, 36 measured transitions and actual shell/world retirement. The stronger d67 material observer is a separate source-bound proof. Reusing it here would require declared314-check full or305-check narrow totals and its additive v2 payload gate; do not silently substitute it.

`variant_case.select_variant` derives guard-only bytes `5308026936229c5728ed93691c24f7f48afb45dc2810788869eca723fe13a665`. It removes only the exact SubViewport ancestry exclusion in `_late_owner_is_dynamic` at1881â€“1883. The same-scenario rebind in `_set_zone_layer_mask` at2884 remains present exactly once. CRLF and unrelated source are preserved. The driver saves candidate/selected bytes and restores the exact134,673-byte candidate in `finally`.

## Preconditions

Check the small exact source bindings in `plan.json`; get the final apartment owner hash from the handoff. The observed2a440 owner is not a promise about a later one-census candidate. Freeze game/tools across both runs, require a fresh name and empty engine lane, and keep the same renderer, engine binaries, environment and wrappers. The existing runner creates fresh APPDATA and retains both engine hashes, source/assets, changed-source copies, command, native streams and real process exit. No source hashing should overlap another measured run.

The ordinary gate does not enforce exact total labels or explicit retirement assertions independently of the final phase. It also records warnings without rejecting them. Therefore run the read-only artifact/label/retirement review below; `expected_viewport_guard_red_observed=true` alone is insufficient.

## Existing commands

Run the omission first, then inspect its completed result and exact restoration before the restored run. The first command returns diagnostic1 for a valid intentional red; that is not a clean diagnostic result.

```powershell
Set-Location -LiteralPath 'C:\PleaseRemainOnTheLine-astra'
& 'C:\Users\nate_\AppData\Local\Programs\Python\Python312\python.exe' -B 'design/astra/work/vulkan_composed/variant_case.py' v1 viewport_omission viewport_guard_final_01_omission --candidate-sha256 cde44cd16b9ebdfedde0bcda34b404c2a675adffe106ba4ac0d576d26e8e1fd4 --scope full
```

```powershell
& 'C:\Users\nate_\AppData\Local\Programs\Python\Python312\python.exe' -B 'design/astra/work/vulkan_composed/variant_case.py' v1 candidate viewport_guard_final_01_restored --candidate-sha256 cde44cd16b9ebdfedde0bcda34b404c2a675adffe106ba4ac0d576d26e8e1fd4 --scope full
```

Both names were unused when this plan was written. A reused name must be replaced in both the declared plan and commands before execution. An invalid omission can still be followed by restored green for cleanup evidence, but the sequence then remains invalid as the intended control.

The expected failing assertion family is actual arcade isolation after stations, the final arcade mask check, or the actual near-cabinet reactivation check. Require a recorded `still_live_baseline_geometry_corrupted` whose expected/actual masks differ while the cabinet remains booted and the geometry retains its own scenario. Index pollution alone, an unloaded/rebooted owner, an absent cabinet, or a copied synthetic viewport is not sufficient. Failure counts may depend on legitimate owner timing; do not invent an exact native assertion count. All non-isolation checks, captures and real retirement must pass. Engine exit1, diagnostic1 and precisely the two functional-failure reasons are required; native pairing errors, project errors, retention, timeout or a crash invalidate the red.

The restored run must have engine0/diagnostic0, exact complete passing labels, no native pairing errors and the same scope/non-root bytes as the omission. Use existing `pair_bookkeeping_case.load_run()` and `validate_artifacts()` for raw hashes and strict-gate reproduction. Compare exact ordered labels to `candidate_v1_foundations_01/frames/probe.json` for full or `foundations_pair_01_restored/frames/probe.json` for root_retirement: both references used base fixture5ec63. Require exactly one passing `actual shell retired` and `actual selected world retired`. Compare before/after maps across runs after removing only `game/scripts/building/building_root.gd`, and require all four engine manifests and both wrapper-input maps equal. Inspect every PNG and all raw diagnostics; the known loader/RGB8/found-art categories do not grant blanket warning acceptance.

`root_retirement` is an allowed smaller contract only if selected before **both** runs: 296 checks and15 captures, explicit exclusion marker, `separate_world_case_executed=false`. It still runs actual arcade and root retirement. It omits only the synthetic independent-world sequence and its three PNGs; it cannot replace final full V1 acceptance. V2 has no matching V1 guard: only `v2 candidate --scope full` is applicable, with two real semantic stance/phone captures and retirement.

## Historical pairing operation proof

No new paired-bookkeeping omission is needed solely because apartment material ownership changes. The historical `foundations_pair_01` already binds the **same current cde44 root**, the same operation-only omission `ead71c74â€¦`, engine sourcea13da4feb and exact restored candidate. SurfacePass, base fixture, CampaignShell, ArcadeMachine and ArcadeCabinetProp also match that pair. The retained native red/green establishes the operation's causal role in its declared historical root_retirement composition.

Apartment ownership and resident navigation have changed since that full-tree snapshot. They can change material/geometry timing, so a final-current complete V1 green with native and retirement inspection is still required. Do not relabel the historical pair as current full-tree evidence. Repeat the pair if the layer operation/callsites, indexing/blocker ownership, engine, or relevant scenario/light/shadow/geometry teardown contract changes; if final-current green emits a native signature; or if exact-final-source necessity is explicitly required. A fresh viewport-only control remains necessary because it establishes a different ownership boundary.
