# Reservation display: independent pre-execution review

**Needs a small controller/seal revision before execution. No production rebase is needed.** The reviewed HEAD is `02fcb18a209624c9b609b65fb44b05b45c65de09`. All 19 declared current dependency hashes match, both proposal copies agree, and the two original owner files match HEAD content. The preparation remains uninstalled and unrun. The read-only check passed 14/14; its receipt is `static_checks.json` (SHA256 `d3953ce8304c559228368530f02af2d6edb0957c4ddec3d0c7347b41bbdfbf25`). This is not Godot compile clearance.

The production patch is bounded: default-true construction flag, visibility assignment for 70 envelope meshes and eight landing clearances, and one production false assignment before `add_child`. It retains the terminal repair, geometry, material construction, collision, anchors, selector, risers and fixtures. The production omission is exactly that one false-assignment line. No definite source or fixture API blocker was found. The new fixture checks actual generated records and actual V2 composition; the unchanged terminal assessor retains the accepted 39-check input/origin oracle.

Required before sealing:

1. **Bind the new execution revision.** `preparation.json:4` is still null, so installation deliberately refuses. Set the reviewed execution HEAD to 02fcb; preserve historical af9c provenance and all earlier preparation/control receipts. Prefer a sibling execution revision so existing package hashes remain attributable. Keep the accepted terminal preparation and assessor unchanged.
2. **Use the requested 180-second cap.** `runtime/run.py:86` and all four request files currently specify 60. Change those together, update the README and exact request controls, then refresh their leaf hashes, preparation hash, and runtime package hash. The canonical PowerShell runner remains unchanged.
3. **Enclose the entire source transition in restoration handling.** `runtime/run.py:181` copies both production owners before its `try/finally`. Failure on the second copy can leave mixed owners without a restoration receipt. Move the transition inside the protected region and track the exact pre-transition/desired bytes per path. Validate restoration inputs against sealed expectations, verify every restored hash immediately, and retain a failure/refusal receipt even when an unexpected source edit prevents restoration (`:194` currently raises before writing it). A failed first candidate should restore original 0bdb/03d; subsequent phases restore the exact candidate 6646/ff25. Never overwrite unrelated edits or an active process.
4. **Make observed overlap an explicit failed lane contract.** `:140–148` proves the fixture child belongs to the wrapper, but does not reject other sampled Godot PIDs. A foreign engine can start and finish during the run and leave a green functional assessment and empty final census. Retain the functional/native result, record the foreign rows, and stop phase progression pending review; add an unrelated-PID control and an owned launcher/child control. Do not claim absolute exclusivity from a sampled census. The native Windows census also cannot see Linux-container engines: root must finish/drain the separate Docker batch before this native window. No native guard edit is proposed.

The existing diagnostics gate correctly separates ordinary red from an accepted negative control: exact 28 labels/footer, exact three failed labels, native exit 1, geometry witnesses and real retirement must all agree. Unknown diagnostics, retention, timeout/refusal, and missing artifacts remain failures. Keep that predicate and its exact known loader/RGB8 header/site pairs; do not widen it to obtain a green. Existing offline evidence is 13 synthetic tests, not runtime evidence; refresh it for the controller changes without overwriting the old result.

After sealing, use the existing sequence: `install`, `01_candidate` (28/28, native/gate 0), `02_production_flag_omission` (25/28, native/ordinary gate 1, control contract 0), `03_restored_candidate` (28/28), and `04_terminal_regression` (39/39). Inspect the first actual candidate before advancing. Preserve each fresh profile, complete raw streams, 17 source copies, full before/after game manifests, exact engine/runner hashes, PID observations and independent restoration receipt. The install baseline records the current full game map; it is not an assertion that every unowned file equals HEAD.

The optional operator/phone capture remains a separately reviewed step after terminal 39. Its current fixture/runner/gate hashes still match. Require the two hidden reservation observations, final pose and actual front oracle from the terminal run, exact 21 checks, source restoration, and direct review of both PNGs. The earlier display proposal lists more omissions and lobby/lift captures; those are unrun broader plans, not results of the authorized four phases. This batch establishes construction/display parity and the inherited terminal input regression, not traversal, collision navigation, art acceptance, furniture completion, audio listening, or performance.

Only this new review folder was written. No game/tools/proposal edits, engines, imports, containers, broad source census, or offline test executions were performed.
