# F01 provider and consumer reconstruction review

Read-only preparation at `e72320288d256e4d20386e8f28c116fe962788fc` plus current uncommitted foundation repairs. Exact reviewed raw hashes are in `consumer_review.json`. No adoption, source edit, engine, generator or exporter run was performed.

There is no live F01 owner-cell provider/configuration/registry. The current V1 composition consumes the imported F01 scene, protected gameplay layout, exact marker names and render-name classifications together. An exporter replay can establish attributable source ownership, but cannot establish this production consumer seam.

The retained M11 review places the last traceable human-accepted boundary at M11B `a9e455bfede9f89193c9acd0796eb8fc5a0c3548`. C1 `503465defa24d19d55b53c9a17bf8a4affdfb5eb` is an isolated technical rehearsal with human acceptance pending. The committed C2 tip is `46d40e9a916ffa7c7cc2ae5031fa6bcbcdeb7777`; its separately captured dirty working-tree production consumers remain quarantined. Their owner-first default and deferred lifecycle must not be copied.

| Contract | Current production dependency | Required reconstruction boundary |
| --- | --- | --- |
| `provider_selection` | `game/scripts/building/building_root_selector.gd:5`: Default v1 selection is distinct from F01 geometry selection. No live Floor01CellRegistry or Floor01GeometryConfiguration exists. | Keep selector/default unchanged; design an explicit root-owned provider seam with a legacy F01 implementation and reversible opt-in. Do not import dirty C2 default-owner cutover. |
| `floor_parent_and_order` | `game/scripts/building/building_root.gd:333`: Protected layout loads once; FLOOR_SCENES instantiates floor_01.gltf into the direct child named F01 and floor_nodes[F01]. Passage indexing precedes late builders; SurfacePass follows height-map setup. | Retain the F01 parent/transform and floor_nodes contract even if render children change; mount provider before indices/material pass, then classify late production children separately. |
| `passage_and_street_aliases` | `game/scripts/building/building_root.gd:1469`: Passage depends on _retail_shop_, _retail_passage_shell_, _retail_passage_proxy_ names. Street indexes measured bounds but exempts facade glazing/stone_trim/sash/blind batches and F01_DOOR_06 boundary ownership. | Provide explicit batch-to-owner/zone aliases preserving all current memberships and exceptions; compare actual source-derived populations and bounds. Renaming meshes or flattening them into one cell silently changes rendering ownership. |
| `late_owner_and_private_world` | `game/scripts/building/building_root.gd:1877`: Late/root geometry classification excludes registered props, moving actors and any ancestor SubViewport. The current paired-bookkeeping helper changes masks through same-scenario retirement and both blocker tables remain independent. | Preserve this current foundation code when introducing providers. Cell traversal must stop at viewport boundaries; do not classify cabinet private-world geometry as F01 or Passage foreign. |
| `surface_class_and_import_owner` | `game/scripts/building/surface_pass.gd:259`: apply traverses floor descendants and substring classes. _is_imported (381) requires a glTF/glb scene ancestor and node.owner == that imported scene root. _class_for (391) is name based. | Preserve class tokens and imported ownership or add an explicit equivalent provider classification. A new tscn wrapper or renamed owner mesh can lose surface treatment or enter prop passes. Preserve governor physics and release all cached material/prop references before provider destruction. |
| `marker_props_and_door_registry` | `game/scripts/building/building_root.gd:1270`: Protected marker records instantiate root-owned functional props and doors. Exact IDs, marker transforms and functional_props_by_floor / doors_by_floor drive lookup and visibility independently from imported floor children. F01_DOOR_06 selects LandmarkEntryDoor. | Keep unique marker IDs, root discovery and coarse floor registries. Avoid duplicate semantic anchors plus live consumers of the same name. Geometry replacement alone must not remove props, rewrite door ownership, or fabricate marker transforms. |
| `apparatus_and_first_shift` | `game/scripts/building/building_root.gd:667`: FirstShift binds F01_WATCHMAN_DETECTOR, F01_NIGHT_REGISTER, F01_SIGNAL_REGISTER, F01_TOUR_KEY_GUARD. OrisonDetailPass authors those names and telephone/fire-line consumers under F01. | Retain those aliases, signals and scoped service ownership; validate actual first-shift/M08F reconstruction, not only node existence. F01_D_BED_VANTRY_POINT, F01_ENTRY_SCONCE and F01_CORRIDOR_DOME_03 remain literal witness/light/case identities. |
| `shop_and_room_records` | `game/scripts/game/maintenance_shop_service.gd:108`: build_counters resolves stock counter_anchor_id through layout furniture; mount_counter (128) is an existing duplicate-rejecting semantic seam and unmount APIs exist. Cases/art use floor IDs, unit rooms, room rects, walls and furniture beyond visible geometry. | Keep stock IDs and all consumed furniture/room records; reuse one service-owned counter mount per shop. Do not substitute a render-only owner manifest for the gameplay layout. |
| `navigation_schedule_and_acoustics` | `game/scripts/characters/resident_nav.gd:324`: Current nav matches door hinge+width+yaw to actual wall apertures, uses wall thickness for capsule-clear approach nodes and keeps collision pruning. Schedule resolves real passage anchors and unit-room centers. | Preserve marker conventions, aperture/wall data, room IDs and shared Blender-to-Godot frame. Validate actual routes and acoustic positions after provider changes. Rebase from the latest navigation fixes, not old C2 root/routine assumptions. |
| `accepted_semantic_adapter_scope` | `game/scripts/building/orison_v2_anchor_adapter.gd:17`: resolve requires exactly one matching name. mount_consumer (75) renames the semantic anchor to *_Semantic and keeps the live consumer on root; unmount restores it. Acoustic overrides are scoped and restored. | This is a useful consumer ownership contract, not a live F01 geometry provider or a batch alias registry. Reuse intentionally only with unique names and explicit cleanup. |
| `accepted_spatial_resolver_scope` | `game/scripts/building/orison_v2_exterior_spatial_resolver.gd:10`: Accepted resolver reads orison_v2/exterior/regions.json with frame/region/template/surface/instance/threshold/route semantics. The isolated exterior cell is dependency-injected and is not the default V1 F01 floor provider. | Keep its coordinate/provenance contract separate from a new legacy F01 adapter. Its acceptance does not accept C1 owner-source artwork or authorize a selector/whole-route cutover. |
| `reconstruction_and_release` | `game/scripts/dream/campaign_shell.gd:117`: Current shell detaches waking services, removes active_world and frees it immediately before loading/rebinding destination; startup_failed roots are refused. There is no current F01 provider teardown hook. | Define root-owned idempotent release for provider indices/material caches/consumers without copying C2 deferred deletion. Prove repeated actual shell destroy/reconstruct and both-direction root/save continuation with final calendar, protected-save and audio ownership behavior. |

Surface class tokens currently include `_walls`, `_finish_`, `_floors_`, `_wainscot`, `_ceiling`, `_stairs`, `_slabs`, `_stone_trim` before `_trim`, `_sash`, `_furnish_`, `_furniture_`, `_retail` and `_transit`. Imported ownership and class aliases are separate requirements; satisfying one does not satisfy the other.

Harukiya has additional live named consumers: `F01_BAR_LT_` lights, `F01_BAR_SIGNAGE` and `F01_BAR_STAGE_SIGN`. The current 247 eligible/indexed census does not satisfy the unchanged legacy >250 assertion. Preserve that unresolved gate and the separately prepared source-coverage contract; do not convert a new export count into acceptance.

Minimal next proof sequence: attributable C1 source/export replay with protected-path equality; explicit off-by-default provider and full alias census; source-derived collision/material/owner coverage; real zone transitions including private viewport isolation; actual apparatus/shop/room/nav/acoustic consumers; repeated root retirement/save/calendar continuity; source-bound captures for subsequent human review. Each seam needs an omission or refusal control appropriate to its behavior. Isolated exterior/cell success cannot stand in for whole-root readiness.

The sibling source inventory owns the clean C1 exporter replay and protected-path manifest. This review does not require any dirty C2 generator changes. All future proposed patches must be narrowly rebased onto the latest renderer/viewport, navigation, timestamp, save and audio repairs rather than overwrite whole historical source files.
