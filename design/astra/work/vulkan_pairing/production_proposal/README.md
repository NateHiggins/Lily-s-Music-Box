The proposed patch is prepared outside the live game against commit `9548301873807117a870aeb6e517173e81a4713f`. It has not been applied or run in Godot. Root owns the next integration handoff. `git apply --check` is a read-only applicability check, not runtime acceptance.

`zone_layer_pairing.patch` changes only the two layer assignments in `_zone_toggle` and introduces `_set_zone_layer_mask`. The helper returns immediately for an unchanged mask; actual geometry changes synchronously rebind the instance to its own current World3D scenario before assigning the new mask. Non-geometry VisualInstance3D retains its ordinary layer assignment. The patch does not add a visibility writer, global cache, engine switch, new blocker table or scene reparenting. Both existing blocker owners and the exact saved mask remain authoritative.

The isolated four-case proof supports this operation on primitive MeshInstance3D. It does not establish correctness for all production geometries, visibility dependencies, GI/LOD, or the V1 teardown. The minimal reproduction generated the unpaired-light membership error, but did not generate the separate soft-shadow underflow from the earlier V1 capture. Any residual production underflow remains a failure requiring diagnosis.

After a separate explicit integration handoff, run the following sequence through the unchanged serial runner with fresh APPDATA and complete source/asset hashes. Do not reuse a scene that accumulated stale pairs before the helper was active.

1. Run the existing `res://tests/StreetCoreVisibilityTest.tscn` and `res://tests/PassageVisibilityTest.tscn` with the patch active. Preserve every original assertion: actual core populations, excluded glass/joinery/WindowGlow/neon, late construction ownership, both blocker directions, exact saved-mask restoration and saved-table drain. An engine exit0 with native errors is still red. A headless run alone cannot validate the Forward+ defect.
2. Add focused ownership controls to those real-root tests: an authored nondefault mask such as33; a geometry with its own visible=false; a child under an invisible parent; a geometry whose owner changes its visibility while the zone is blocked; an unrelated neighbor; and an already-zero authored mask. Exercise PASSAGE block → STREET block → PASSAGE release → STREET release, both reverse orders, and repeated unchanged requests. Require unchanged owner properties, exact masks after the last release, no saved entries after complete release, and no change to the neighbor. Use isolated owned test geometry, drain blockers before freeing it, and keep tests' own cleanup explicit.
3. Exercise an isolated SubViewport with its own World3D before broadening the helper claim to those consumers. Its geometry must stay in that viewport, render after restoration and leave the parent world/neighbor unchanged. No main-world scenario shortcut is admissible. Add representative production geometry/visibility dependency controls if the indexed population contains those types; the primitive test does not clear them automatically.
4. Capture a rendered V1 transition sequence with actual root and camera. Existing test coordinates are STREET(-16,0.27,13.5), PASSAGE(14,1,50), ORISON lobby(0,0.27,9), and Harukiya(3,-1.39,34). Include an existing verified F04 corridor camera position from the project's shot harness instead of inventing one. Use STREET → PASSAGE → STREET → ORISON, repeated cycles, and F04/outside-floor owner checks. Advance rendered frames at each transition, inspect geometry population and visible glass/signage/windows, and retire the actual world afterward. Direct `_apply_visibility`/camera teleport is a controlled composition test, not walked traversal.
5. Measure the matched unmodified and candidate transition paths in separate fresh processes using identical content, controlled campaign time, camera path and warmup. Keep both synchronous `_apply_visibility` cost and rendered transition frame distributions, instance/draw populations and p95/max values. The existing Passage test's500-iteration profile advances no frames and measures script work only; it cannot measure deferred bounds/dependency updates from the scenario rebind. Require unchanged-region requests to avoid scenario work, preserve the existing visibility-signature cache contract, and investigate a material transition regression before accepting the helper. The standalone roughly0.65-second36-frame loop is not a production performance result.
6. Run `res://tests/HistoricalRadioLobbyShot.tscn` on the patched actual V1 root, preserving its camera teleport, real PlayerController ray/presenter path and queued world retirement. Inspect all three PNGs and native teardown logs. It must remain composition evidence; its old raw261 underflows/1,252 unpaired-light errors are retained and cannot be retrospectively reclassified.
7. On a fresh equivalent source copy, omit only the helper's pre-change `RenderingServer.instance_set_scenario` call and repeat the decisive rendered transition/retirement consumer. The log-aware gate must go red again while owner/draw controls remain meaningful. Restore the candidate in another fresh run and require clean diagnostics. Keep first failures, exact diffs and full stderr, including host loader errors, and do not disable shadows or switch rendering backends to obtain a green result.

Root owns production integration, the renderer residual-error investigation and the decision based on measured transition cost. This proposal grants no visual/human acceptance, game teardown acceptance or performance claim. The installed engine remains unchanged.
