# ASTRA-SANITIZE-0 debt deduplication

Source basis: **c2dc01771bc25b07f5dcf7a6040102345b8c57d5**. This is a triage record, not acceptance evidence. It groups findings by the missing player outcome and current consumer; it does not turn every historical TODO into a present task. Branch candidates and quarantined work remain governed by `BRANCH_ADOPTION_MATRIX.json`.

## Fresh instrument results

Exact commands, exits, stdout/stderr and hashes: `evidence/base_audits/receipt.json`. No baseline updates were made.

| Instrument | Exit and current meaning |
| --- | --- |
| V2 completeness | **2**. 150 requirements: 52 ABSENT, 3 SHELL_ONLY, 42 PROGRAMMED, 18 SPATIALLY_PROVEN, 34 RUNTIME_PROVEN, 1 HUMAN_ACCEPTED. Scope blockers: first slice 0; golden shift 1; full structure 86; full runtime 45; production cutover 101; v1 retirement 103. This is an honest incomplete-build result. |
| Spatial dependencies | **0**. Reviewed manifest matches this source; this proves no full-world traversal or consumer completeness. |
| Systemic authority | **0** with 57 findings, 35 production and 22 test. Reviewed or nonactionable findings remain visible. The scanner's known heuristic limits prevent treating zero new actionable findings as whole-game ethos approval. |
| Data consumption | **1**, 1,304 blocking records: 1,290 FIELD_UNREAD, 11 FILE_UNREAD, 2 DURABLE_NUMERIC_MONOTONIC_ONLY, 1 MALFORMED. These are scanner records, not 1,304 independently confirmed product defects. |
| Prompt carriers | **1**, including 2 forbidden production strings, 185 baseline-covered legacy occurrences, and 2 dynamic occurrences. |
| Interaction implementors | **0**. Census/manifest agreement, not usability or physical contact proof. |
| Audio emitters `--check` | **0**. Static routing/catalog checks, not audibility or listening acceptance. |
| Existing self-tests | All six existing Python self-test programs exited **0**. Audio emitters has no existing self-test; retain this instrument gap and require a red fixture before relying on it for new release gating. |

Do not compute a completion percentage from these mixed evidence tiers. Do not erase findings to make the aggregate smaller.

## Closed, duplicate and superseded findings

| Debt group | Historical reports | Current proof and disposition |
| --- | --- | --- |
| CLOSED-01 Open Shift coordinator invents knowledge, compensation and packing custody | Open-shift compliance audit, ETHOS1, genetic-memory examples, authority audit | Corrected in **66355da16d6b5e74e7370dcfa731437fe606e212**, reachable from main. `OpenShiftSituation.record_fact()` rejects foreign facts; `NpcObservationLedger` writes observations; `PorterActor` owns its timeline; `MaintenanceInventory` owns packing. Both root composers instantiate the corrected ecosystem. **Already integrated: do not merge the original ethos branch again.** Residual embodiment/presence debt is OPEN-03 below. |
| CLOSED-02 Radiator needs its accepted physical rebuild | Original placeholder and consequence visual reports | **f57289648ba671c3e6a1f25f8fe7fc7989c72731** plus scoped human acceptance **5ed8bf3164785b2db2b4287b86bd44047e58cfca** are main ancestors. Preserve the model, seven surfaces and scoped acceptance. Leakage bubbles, valve size and collars remain grouped art refinements, not grounds to repeat the entire rebuild. |
| CLOSED-03 Runner returns zero on failed suites; all refusals mean busy | Genetic memory §§2.1/2.8, Sept-3 handoff §7 | Handle/exit check landed at **b813b1a962723e7e66edb98f20b3bd60d74b28ca**; split refusals landed at **aeb9f9a1c97e3f3b01d086a354a985ef0725f086**. Main runner distinguishes busy/timeout/cannot-run and permits a requested 180-second bound. Old receipts remain historical; the source fix does not retroactively validate their exits. |
| CLOSED-04 No data-reader gate exists | Genetic memory §§1.7/7, Sept-3 handoff §6 | Current `tools/audit_data_consumption.py` exists, recursively reads data, identifies field absence and monotonic durable values; latest base **c2dc017...** extends it. **Do not build a second reader audit.** Triage its real red output; improve intended-dynamic/build-time contracts where necessary. |
| CLOSED-05 Five ordinary monitors instantiate the unique service terminal | Unused-systems P0 | **334efe7035eddb248d950788258a997520e1cd0c** splits `signal_terminal` from ordinary `monitor`. Main has distinct consumers. Old render remains a before image, not present defect proof. |
| CLOSED-06 Four roof acoustic light nodes are isolated | Unused-systems P0 | Main generator authors `ROOF_ELECTRICAL_RISER`, links F06 and asserts every spawned marker node has a carrier (`art/data/gen_layout.py:10174,10283,10369`). Preserve the invariant. V2 topology derivation is separate remaining work. |
| CLOSED-07 Dead corridor light, clock-dials JSON, runtime decor brief | Unused-systems P2 and genetic-memory repeated examples | **980bee16918b8bad246fcccc145b81cca0aef915** removes the corridor family; **a80577df83fcc776fd206fe3a989404a4967d1d9** removes clock-dials JSON; **0e310598dd6293c67f7024ce890a671e92b1850c** moves decor profiles to design. Paths verified at main. Broader generated-atlas duplication remains OPEN-08. |
| RETRACTED-01 Peter/Cal/Mae missing from an eight-record case file | Early-access C-4 and repeated handoffs | **e81592d81fb91634676c8941259def896dc22576** retracts the truncated census. The full file has 18 case records; Mina alone is enabled. No cast-selection task follows from this claim. The six-case release mandate independently defines the intended campaign. |
| RETRACTED-02 313 systems and missing advance as universal diagnosis | Earlier simulation handoff/citations | Handoff and genetic-memory correction explicitly retract 313. The historical sourced composition census was 99 distinct authorities, with spatial prerequisites dominating. Do not promote either dated number into a new completeness denominator. |
| SUPERSEDED-01 Bus policy/cue catalog/settings absent | Lower historical sections of sound audit | Its implemented checkpoint and current audio audit supersede the absence claim. Keep AudioPolicy, category controls and semantic pool. Listening and caption parity remain OPEN-10. |
| DUPLICATE-01 Missing floors, services, rooms, acoustic support repeated in many reports | Full-rebuild audit, handoffs, room reports, living plan | Use the 150 current completeness requirements as the spatial requirement identities. A single missing space may block many consumers; do not count each mention as separate construction. Preserve consumer-specific proof as dependencies. |

## Genuinely open debt, grouped by decisive outcome

| ID / priority | Player outcome and authoritative owner | Current evidence / deduplicated debt | Next decisive action |
| --- | --- | --- | --- |
| OPEN-01 BLOCKER — canonical production substrate | One continuous V2 world; semantic registry, provider boundary and root composer | Main remains v1-default, V2 scope incomplete. Clean M11 rehearsals and dirty M11C2 have different integration status. Missing structure, F01 cut, street threshold, F03 and whole-world topology belong to one ordered substrate program. | Follow commit-specific adoption register. Keep dirty M11C2 quarantined. Prove reversible production consumer cut and actual route before broader construction. |
| OPEN-02 RELEASE_CRITICAL — campaign date and time | Real local starting time of day in an independently authored 1928 campaign; `CampaignClock` | `campaign_clock.gd:24–34` imports host date, weekday and day-of-year; latest owner ruling forbids that dependency. Multiple older clock fallbacks are related consistency debt, not justification to add another clock. | Add a red fixture varying host calendar while holding time of day; initialize authored date once, preserve simulation advance, midnight/day behavior and saved epoch reconstruction. Audit consumers against the one clock owner. |
| OPEN-03 RELEASE_CRITICAL — believable earned consequences | A resident notices only what they can perceive; porter visibly acts when embodied; ledger, schedules, resident/porter action owners | Corrected Open Shift is integrated. Production v1 `building_root.gd:704` creates the default ledger without presence, whose `_is_present()` returns true. V2 already injects a timetable provider (`orison_v2_runtime_root.gd:162–164,187–208`); its `resident_is_home()` instead confuses wrapped time of day with elapsed campaign minutes (`:223–224`). Porter remains durable actor state/events rather than a general animated navigating inhabitant. Overall four-disposition human gate pending. | Preserve V2's existing provider; fix clock coherence under OPEN-02. Bind truthful v1 observation presence as required for baseline validity, prove absent residents do not learn visible events, preserve offscreen catch-up. Complete and observe Mina plus one case-less resident before scaling the agent architecture. |
| OPEN-04 RELEASE_CRITICAL — durable trustworthy saves | No loss or duplication after interrupt/reload/corruption; `RealityState` and domain owners | Version 4 uses direct `store_string` (`reality_game_state.gd:110–124`), no atomic rename/readback/schema gate. Eight semantic loop proofs are not eleven human route boundaries. Unread durable fields are the same ownership defect tracked OPEN-08, not extra save-reliability counts. | Failing I/O/corruption/future-schema fixtures; validated temp-write/readback/rename and explicit version policy; composed save/destroy/reconstruct through dramatic boundaries. |
| OPEN-05 RELEASE_CRITICAL — complete optional Mina path, then Peter | Discover/infer/procure/repair/converse/dream/wake, with distinct refusal continuations; WorkOrders, case, sleep and dream owners | Old Mina-complete prose and 2B Open Shift tests do not prove a fresh-player Mina shift. Most case records are design content; `activate_case()` checks existence without consuming `enabled`, and maintenance data still names missing `lena_visible_patch`. These two latent schema/activation defects are distinct from the retracted missing-case claim. | Enforce authored case containment without adding a Lena chapter; reconcile dangling job references with current service-round intent; then full unaided Mina route and boundary proof. Peter is second under the existing mandate. |
| OPEN-06 RELEASE_CRITICAL — carrier-neutral and tactile interaction | Immediate semantic acts through keyboard/controller; controller and prop owners | Current prompt audit finds two forbidden `Hold E` strings in `clock_prop.gd`; 185 legacy and two dynamic occurrences have different classification. Implementor census green does not prove reach, contact, cancellation, NPC use or controller completion. | Red proof for forbidden copy, smallest semantic correction, targeted audit and live prompt verification. Broader interaction migration remains grouped by real playable mechanism/outcome. |
| OPEN-07 MAJOR — parsed content reaches its real owner | Trivia can load the authored catalog; trivia data owner | Current data audit reports `game/data/trivia_darts.json` malformed: invalid control character at line 63, column 70. This is one parsing defect, not the same class as unread fields. | Inspect intended text/source, reproduce strict parse failure, repair content without inventing facts, verify its actual loader. |
| OPEN-08 MAJOR — authored data changes the game | Named build/runtime consumers and bounded durable facts | 1,304 scan records group into contract families. Confirmed self-only persistent `building_stability`/`reality_coherence` and declaration-only `discovered_documents` remain. Notice/mail atlas manifests have writers but runtime maps are hardcoded. Schedule `with/route/outfit` remain unconsumed by `ScheduleDirector`; existing navigation methods do not prove these payload fields are consumed. | Classify every family before altering it. Bind one real consumer or retain explicit archival/future/build-only provenance. No blanket baseline, deletion, or token read inserted to fake use. Prove the scene changes, and second instance is data work. |
| OPEN-09 RELEASE_CRITICAL — extraordinary ecology in the actual Orison | Lamp irradiance/history influences intact cell anatomy and colony decisions; separate simulation/material/light owners | Dream/voxel/lamp branches contain accepted components, failed S2J/C1D gates and isolated-scene evidence. Main references or material compilation alone do not establish production integration. | Recover bounded accepted components by provenance. Reproduce failed cases and prove actual lamp → actual field → actual world-coordinate material sample in furnished production composition, then motion/performance/human review. |
| OPEN-10 RELEASE_CRITICAL — sound teaches cause and direction | Audible source/state/refusal with truthful captions; AudioPolicy and physical/acoustic sources | Bus routing fixed; fresh human listening/masking, complete critical cue migration, room/portal transmission, surface footsteps/contact Foley and caption coverage are not proven. The audio audit has no existing self-test. | Red fixture for gating reliability; uninterrupted route and controlled mix comparisons, ordinary speakers/headphones, blinded direction/state tests. Keep caption claims scoped to actual catalog coverage. |
| OPEN-11 RELEASE_CRITICAL — stable composed runtime | Complete content imports/boots/streams/saves/exits within target budget | Historical v1 and sparse V2 numbers are different scopes. Current high shadow/MSAA config cannot be priced from static source. Existing engine teardown claims require untouched-build reproduction. | Profile named root/content/hardware/renderer/resolution/time/quality/warmth with import separated. Red teardown/growth fixtures and second-cycle ownership checks. Establish target hardware before release claims. |
| OPEN-12 RELEASE_CRITICAL — trustworthy release evidence | Player can navigate/input/recover without development knowledge | Rough historical screenshot-suite counts are diagnostic leads. Instrument self-tests do not certify all captures. Human visual, first-encounter, controller-only, listening, ethics and accessibility gates are distinct. | Consolidate canonical runners and receipts by release claim; prove red before green; deliver the clean playable baseline and ask only for concrete human receipts at their actual boundary. |

## Data triage: do not mistake textual absence for dead assets

The fresh file-absence list contains eleven paths: `apartment_life_profiles.json`, `creature_index.json`, `fixture_light_map.json`, `lobby_notices.json`, `mailbank_cards.json`, `material_catalog.json`, `move_repertoire.json`, `resident_animation_profiles.json`, `resident_hero_models.json`, `runtime_material_sets.json`, and `songbook/last_train_home.json` under `game/data/`.

Some have real non-runtime consumers. `art/blender/scripts/convert_dump_characters.py:30–32` reads hero mappings and animation profiles; `bake_model_moves.py:62` reads the mapping; `build_orison.py:45` reads the material catalog; `art/tools/generate_runtime_materials.py` reads source materials and writes the runtime manifest. Therefore a FILE_UNREAD result is accurately a missing production-path reader under this scanner, but it is **not proof those source assets are unused**. Trace the exact source/mirror/build output/runtime chain and encode intended build-time provenance before disposition. Deleting those files from this result alone would destroy real authoring inputs.

The schedule families need their own contracts: wardrobe must preserve one active model and canonical export scale; co-presence may be derivable from already-consumed timetables rather than a second editable truth; authored route polylines must not bypass ResidentNav collision validation. A token reader added for audit satisfaction proves none of these behaviors.

The unassigned creature index and orphan B1 Room 0 threshold are preserved-in-place candidates for a creative/dependency review, not automatic release feature work. The Bible says the creatures' purpose is unassigned; it says Room 0 exists while the hum does. Main still authors `B1_ROOM0_DOOR` in `gen_layout.py:2839` while the actual `Room0` instance binds to the F04 anomaly (`building_root.gd:707–711`). Establish the intended entrance before changing fiction; leave recoverable assets/history intact. No automatic deletion or new monster is authorized by an unread-data finding.

## False positives, underclaims and acceptance scope

1. Systemic authority's negative test writes and injected test timelines can match production-prohibited shapes; retain test-tier provenance. The ecosystem `_process` proximity heuristic cannot by itself judge deterministic catch-up. Conversely the scanner does not prove truthful resident presence; OPEN-03 is visible in current code even with exit 0. An initial draft incorrectly generalized v1's missing presence injection to V2 by reading an incomplete setup call; that finding is withdrawn. V2's existing timetable provider must be retained and its time-domain mismatch addressed.
2. Completeness reports can underclaim checkpointed/accepted spaces when the accepted identifier is absent from recognized structured evidence. Fix the intake/receipt with independently traceable proof; never rename a room to appease a matcher. An older handoff's specific watch/vestibule work order is not permission to mint a new acceptance receipt without checking current line and original scope.
3. `clock_dials.json` removal closed that duplicate file, not the larger generated-atlas pattern. `lobby_notices` and `mailbank_cards` are one shared consumption problem with two instances, not a justification to resurrect the deleted source.
4. The radiator's recorded leakage bubbles, slightly oversized valve and penetration collars are **accepted nonblocking art debt for that visual gate**. The full finished-game art pass may refine them, but they do not demote the traceable acceptance or justify redoing the branch.
5. Historical screenshot/performance/exit reports remain preserved records. Their counts must be rerun when that consumer becomes the active release gate; they do not become a large undifferentiated TODO queue today.

## Closure rule

Each master-ledger row should point to one OPEN group or a more specific current completeness requirement. Closure requires a correct owner and actual consumer, evidence appropriate to the claim, a real path to red, production composition, and human review when perception matters. A narrower fix can close its own defect while the experiential row remains open. This review does not claim any current gameplay feature release-complete.
