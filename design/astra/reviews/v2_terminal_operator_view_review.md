# V2 terminal operator diagnostic remains red

The single run [terminal_operator_view_v2_01](../evidence/vulkan_composed/runs/terminal_operator_view_v2_01/result.json) completed **19/21 checks, native exit 1, strict gate 1**. Its two failures are the actual terminal facing and absent DeskZone. Both normal captures, complete diagnostic payload, and actual shell/world retirement passed. The source-bound [review JSON](v2_terminal_operator_view_review.json) retains all facts and verification results.

The exact old bedside ray is 8.1871 m and hits `OrisonV2Blockout/F04_B_PRIVATE_HALL/WallNorth_00/Collision`. The new operator-camera ray to the actual SignalScope is 1.23135 m and physically clear. The mounted terminal front is approximately world +Z; the operator direction is -X. Their dot is `-7.54979012640433e-08`, so the instrument faces sideways. Actual DeskZone count is zero; the terminal has no `interact` method and no Area3D. This is a gameplay accessibility defect, not a passing interaction test.

Both images were directly inspected. The [1280×720 operator capture](../evidence/vulkan_composed/runs/terminal_operator_view_v2_01/frames/v2_actual_f04_semantic_stance.png) now shows the terminal's top and side mechanisms, with rails and valves, but no readable instrument face. The [320×240 phone capture](../evidence/vulkan_composed/runs/terminal_operator_view_v2_01/frames/v2_actual_f04_phone.png) is brighter and still shows the top/side. Perceptual acceptance remains red. The use and stance proxies are both visible, camera-mask eligible, noncolliding StandardMaterial3D meshes with alpha approximately 0.32. The camera is outside their recorded AABBs; these facts do not establish that transparent geometry is visually harmless.

All **16 actual artifact-map entries** were rehashed, with complete map coverage of the 17 nonprofile files including the result receipt itself. The anticipated 46 entries do not apply after the foundation commit: only this fixture now needs before/after source copies. Both 4,045-entry source maps and their diffs match; the sole overlay path is `game/tests/vulkan_composed_root_test.gd`, and both copied fixture hashes equal candidate `4607eca50a8e9758bcfcd9a2c218cbda6755ac81a1b3468eba46a92fab054b38`. All 21 unique ordered labels and the 21/2 footer agree between raw stdout and JSON. Recomputing the unchanged gate gives the exact stored result.

There are zero light-unpair errors, soft-shadow underflows, retention findings or unknown error headers. Raw logs retain five known host-loader ERROR headers, two loader warnings and 39 RGB8 conversion warnings. This is not warning-free release evidence. The runner interval is 6.235 s; the external command including source/engine snapshots is 13.917 s. Neither is performance acceptance.

The first [preflight refusal](../evidence/vulkan_composed/invocations/terminal_operator_view_v2_01/preflight_refusal.json) launched no engine and installed no fixture because an independently opened Project Manager existed. Root gracefully closed that empty manager. A fresh [invocation02](../evidence/vulkan_composed/invocations/terminal_operator_view_v2_02/invocation.json) then ran the one authorized diagnostic and restored exact original `5ec63c36c6181b33c02bd2255039e9bf4e8db0c4465a45ac641d1481f422d6d3` in `finally`, after an empty process census. The scene remains `b950…`, owners remain `1a1b…` and `cde44…`, and tracked/untracked game/tools changes are empty. The final census was empty and the engine/source lane was explicitly returned to root.

This uses controlled teleport and a fixture-mounted production PhoneCamera. It does not prove player traversal, body-clear stance admission, ray interaction, call entry/exit or human owner acceptance. The original raw receipts and prepared source packet remain unchanged.
