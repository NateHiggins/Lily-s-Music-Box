The reviewed same-scenario helper removes both native error classes from the actual V1 historical-radio lobby capture: the fresh baseline emits 261 soft-shadow underflows and 1,252 unpaired-light errors; the candidate emits 0 of each, completes all three captures and retires its queued world without retention diagnostics. **This is a clean bounded V1 capture/retirement smoke, not general transition, performance or human acceptance. StreetCore still fails its existing Harukiya population threshold.**

The batch started from clean game/tools at commit `e72320288d256e4d20386e8f28c116fe962788fc`. Only the reviewed `building_root.gd` helper was applied to production. Later, with root authorization, the two existing fixtures received an explicit frozen 03:00 CampaignClock setup and diagnostic prints. No original assertion, threshold or wait duration was changed. No engine, renderer, shadow setting, asset, production clock or serial-runner source was changed. No staging or commit was performed by this worker.

Every run used a fresh APPDATA, windowed 1280Ã—720, the unchanged serial runner and actual installed Godot 4.7.1 `a13da4feb` on Vulkan 1.4.341 / Forward+ / RTX 4080. Both installed engine binaries, the wrapper/parser/runner, the canonical runtime text inputs, and all 3,887 tracked/nonignored game files were hashed before and after each run. Every run retained stable bytes. The broader manifest includes asset bytes separately from the canonical text digest; imported `.godot` caches are excluded. PIDs, exact commands, profiles, selected test/root source copies, diffs, stdout and stderr are retained in each run directory.

| Actual run | Native exit | Diagnostic gate | Assertion/capture result | Underflow / unpaired light |
| --- | --- | --- | --- | --- |
| [baseline_lobby_01](../evidence/vulkan_composed_smoke/baseline_lobby_01/result.json) | 0 | 1 | 3/3 captures | 261 / 1,252 |
| [candidate_street_01](../evidence/vulkan_composed_smoke/candidate_street_01/result.json) | 1 | 1 | 27 pass / 1 fail | 0 / 0 |
| [candidate_passage_01](../evidence/vulkan_composed_smoke/candidate_passage_01/result.json) | 2 | 1 | 32 pass / 2 fail | 0 / 0 |
| [candidate_lobby_01](../evidence/vulkan_composed_smoke/candidate_lobby_01/result.json) | 0 | 0 | 3/3 captures | 0 / 0 |
| [baseline_street_01](../evidence/vulkan_composed_smoke/baseline_street_01/result.json) | 1 | 1 | 27 pass / 1 fail | 0 / 1,246 |
| [baseline_passage_01](../evidence/vulkan_composed_smoke/baseline_passage_01/result.json) | 2 | 1 | 32 pass / 2 fail | 0 / 1,246 |
| [candidate_street_clock_01](../evidence/vulkan_composed_smoke/candidate_street_clock_01/result.json) | 1 | 1 | 28 pass / 1 fail | 0 / 0 |
| [candidate_passage_clock_01](../evidence/vulkan_composed_smoke/candidate_passage_clock_01/result.json) | 0 | 0 | 35/35 | 0 / 0 |

The matched baseline visibility runs restored the exact prechange building file, used unchanged original fixtures and new profiles, then restored the actual candidate bytes in `finally`. The [restoration receipt](../evidence/vulkan_composed_smoke/matched_baseline_restore/restoration.json) confirms restoration. Both unmodified fixtures repeat the same functional failures seen with the helper. That establishes that those observed failures were not introduced by the helper; it does not make them acceptable.

Passage's old comment claimed `DAYNIGHT=0` pinned 03:00. With the shared authored CampaignClock, that flag only stops legacy dispatch; it does not choose civil time. The original run logged `PASSAGE HOURS OPEN`, then failed the expected 03:00 grille composition and late-interior visibility. The fixture now explicitly configures 1928-11-10 at 180 minutes and freezes CampaignTime before constructing the root. Its original 1.6-second wait and all 34 assertions remain intact; one clock-setup assertion was added. It passes 35/35, with 134 late interior geometries and an empty hidden-node diagnostic list. The original red runs and test copies remain unchanged in evidence. This fixes the fixture's time premise; production clock behavior is unchanged.

StreetCore's clock-pinned run remains red at its original `harukiya_core.size() > 250` check. Its total core index contains 1,785 draws; the Harukiya subset contains 247 measured eligible geometries, all 247 indexed. The [exact census](../evidence/vulkan_composed_smoke/fixture_clock_repair/harukiya_0300_census.json) retains every path, type, AABB, layer and visibility state: 243 MeshInstance3D and 4 Label3D, with zero currently eligible omissions. No threshold was lowered. The original 1.5-second wait remains. This snapshot does not establish whether later construction would add a family, or whether an earlier authored population changed. The threshold first appears in `f9e9fc9`; `40cbe01` later claims 28/28 without changing that threshold. Those historical claims do not replace a current passing run. Root and the richer fixture owner will compare post-settle populations and source families before deciding on a correction.

All six baseline/candidate lobby PNGs were inspected locally. The physical hand-copied notice, prompt, surrounding board/wall/phone composition and complete three-line field-copy text remain visible. The field copy reports the pre-change and effective WEAF/WJZ/WOR assignments and the dated boundary. No new composition or legibility defect was observed in the candidate. The baseline/candidate images are not byte-identical and are not presented as a zero-noise visual equivalence proof. The scene uses a teleported camera and the actual PlayerController interaction ray/presenter; it is not walked traversal. Dummy audio was selected, so no listening acceptance is claimed.

Each run preserves five inherited host Vulkan manifest errors, two loader warnings, 965 RGB8 conversion warnings and one existing found-art warning. These remain in raw logs and are classified separately; unknown errors and failed suite completions still turn the external gate red. There are no retention diagnostics in this batch. The corrected compact and spaced soft-shadow parser signatures are counted separately from light-unpair errors. Seven wrapper controls include a read-only replay of the prior actual V1 log, correctly identifying 261 underflows and 1,252 unpaired-light errors.

The initial application-byte check failed because `git apply` produced CRLF checkout bytes from the LF reviewed proposal. The normalized contents are identical, the Git diff is exactly 17 insertions and 2 deletions in the one production file, and the actual CRLF bytes were frozen throughout candidate runs. The [application binding](../evidence/vulkan_composed_smoke/applied_binding.json) retains that failure and both hashes. No mid-run normalization was performed.

The actual handed-off source SHA256 values are:

| File | SHA256 |
| --- | --- |
| `game/scripts/building/building_root.gd` | `cd174d254420dade5388fbe11745a38836d1bb3bfad44f85fdfc4e860aecb7a5` |
| `game/tests/street_core_visibility_test.gd` | `4df374d912a91bc5c8d251621f9ef6048db3db8eebf2d201d1cb3125544d2b45` |
| `game/tests/passage_visibility_test.gd` | `00cbc73c28a61ab13623d7ba7c1dae212ec7ce3e8bef83afdf2e51bc8586d732` |

The candidate runtime text digest is `d58daf80c2a317bd57f56979a4c3d9f9746aa8bf1e628923cade36e3ec39f65d`. At handoff, a read-only process census found no Godot process. The lane and three files were explicitly released to branch for richer ownership/performance attribution. This worker will make no further game edits or Godot runs. Duration and the legacy 500-iteration synchronous scan profile are descriptive only; neither measures the helper's rendered transition cost. Earlier standalone reviews remain historical records of the then-unapplied proposal, while this batch records its current uncommitted application.
