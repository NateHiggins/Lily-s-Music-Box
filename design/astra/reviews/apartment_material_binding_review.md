# Focused material ownership repair

The current repair is `game/scripts/reality/apartment_encroachment.gd` SHA256 `7c54c18154ccf8de8c3bce13a356329344dec19411adb2f1237425ae99eb90db`. It uses `_bind_living` as the sole registration route, rebuilds the existing array in place from live material slots plus active root-level case rows, preserves the first installed case material identity, and refuses foreign viewport/resident/player branches. SurfacePass first-owner surface cache references remain intact. No save owner, layout, asset, selector, baseline, or one-census traversal optimization changed.

The preserved original c9892f completed the unchanged 36 substantive assertions with 18 failures. Its failures cover duplicate registration and repeated growth, source-copy reuse, foreign material ownership, inactive overridden surfaces, private/dynamic exclusions, first installed floor-case material identity and changed-intensity refresh, governor replacement/retirement, and field-rebind registration. Root-level initial refresh/lifecycle already passed on the original; they are preservation controls, not retrospectively claimed defects.

| Run | Actual serial/game result | Diagnostic / control | Meaning |
| --- | --- | --- | --- |
| original_01 | process 1; incomplete | 1 / 1 | Fixture nullable-bool error, then 45-second timeout; no completed game exit. |
| original_02 | process 1; no launch | 1 / 1 | Independent project's active Godot tests caused lane refusal. |
| original_03 | process 1; no launch | 1 / 1 | PowerShell advanced ExtraArgs binding refused invocation. |
| original_04 | game 18; 18/36 | 1 / 0 | Complete intended original-source regression red; no unrelated diagnostics. |
| candidate_01 | game 0; 36/36 | 1 / 1 | 73 missing optional-metadata errors; rejected, despite passed assertions. |
| candidate_02 | game 0; 36/36 | 0 / 0 | Corrected optional metadata guard; clean focused green. |
| duplicate_registration_01 | game 8; 28/36 | 1 / 0 | Duplicate-slot control rejected. |
| case_owner_marker_01 | game 3; 33/36 | 1 / 0 | Installed row/first refresh identity control rejected. |
| private_world_guard_01 | game 4; 32/36 | 1 / 0 | Cabinet-world ownership control rejected. |
| root_row_registration_01 | game 5; 31/36 | 1 / 0 | Root-level refresh registry/lifecycle control rejected. |
| restored_01 | game 0; 36/36 | 0 / 0 | Exact 7c54c restoration; clean final focused green. |

Original_04, corrected candidate, all four omission controls and restored candidate have stable before/after production/fixture/source-manifest and installed-engine bindings. Their only failure output is the named intended assertions in red runs. The final restored run has empty Godot stderr and no warning lines. The final lane handoff census found zero Godot processes. No older receipt was edited or assigned a stronger result.

`design/astra/evidence/apartment_material_binding/validation.json` checks 1087 retained artifact hashes across 11 receipts with zero mismatches, verifies each copied source and both 1199-entry runtime manifests, and retains raw stream hashes, exact diagnostics, fail labels and engine hashes. The manifest includes 35 shader/include sources. It covers source/configuration plus the installed binaries, not every binary art/import-cache input. The separate omission driver hash is recorded after execution; each actually executed mutated production source is independently copied and bound by its own runtime receipt.

The wrapper failures are concrete harness lessons. A non-null default check is required for unset ShaderMaterial parameters; direct `bool(null)` is invalid. Outer PowerShell Stop interrupted the unchanged serial runner's `Write-Error` catch, so initial timeout/refusal process 1 was not relabeled native 124 or assertion 1. Direct CLI array forwarding also differed under CmdletBinding. The current bridge reads structured JSON, splats the actual array, scopes Continue around the unchanged runner call, refuses missing exit codes, and preserves native 124/5 in matching non-Godot controls. All 18 parser/process-boundary tests pass. Diagnostic failure remains 1 even when the controller accepts an intentional red.

The remaining boundaries are precise. The focused fixture hand-constructs finish metadata; actual `build()` guards and all six live case finish/prop rows still require the real-root material invariants. Existing ApartmentEncroachmentTest, DreamArchitectureLifecycleLiveTest, actual CampaignShell teardown and measured frame/callback runs were not run in this lane. No callback-cost reduction is claimed. SurfacePass's pre-existing budget propagation limitation for copied materials remains visible, and tier-OFF does not gain a new callback; retired registry entries are removed on the next real callback or sweep. The lane was returned to root for the timestamp/hearing checkpoint and navigation work before these broader checks.
