# Floor admission and observer: independent static review

No definite GDScript type, API or teardown blocker was found in the sealed floor proposal or revised observer. This review did not run Godot, edit live files or mutate either package. The proposed focused proof still needs its serialized runtime results, followed by the actual full build.

Reviewed bindings:

- `work/apartment_floor_ownership_01/preparation.json`: `dcdf5036fa98fe0fe6f616b6ac13d8bf930f1a47c0d31e4288209b3cac0bf956`; all nine declared file hashes match.
- Admission owner: `2a440504ab80404f4d9f4ab16138df7db9255801ca879ff91aabafe3c7f4d0ca`; focused fixture: `8003596b3b6e0b39ab15cded32cff21aeeaf55202d2a971a75a8ebc299b184be`.
- Stronger observer in `work/composed_material_invariants/revisions/all_registry_lifecycle_01`: fixture `d67ce1ac8b19d51f6d4ccc88bc3fe11599dd6068afaccea6abb1d9ec50712508`, helper `aec59c518d9c08714b30a69b5f9682dd8cc213e17d495719602d5635d815530c`.

The admission check uses actual floor-node identity before both the existing-case fast path and geometric first claim. That prevents the known F03/3A, F05/5A and F06/6B wardrobe leaves from entering the lower cases in a fresh build, so the unchanged F02, F04, F06, F05, F03 binder order no longer has those competing rows. The complete provider includes floors without cases. The helper neither changes material metadata nor guesses floor names.

True root-level props still use the existing bounds and metadata rules. Same-floor batched draws retain the original broad bounds in source; the focused fixture does not construct such batches. This is a narrow admission repair, not a change to horizontal case priority or shared-source material policy.

The proposed 63-check fixture uses actual owner `build()`, authored room and wardrobe records, real FurnitureInteractionPass/BakedFurnitureInteraction/MatLib, and both normal and directly drained queued SurfacePass callbacks. Its synthetic floor roots are valid providers: the real producer installs its world-coordinate mechanisms beneath them. Setup supplies six case units, five fields and all eight providers. Explicit overlapping draws beneath B1/F01/ROOF test provider authority; root-level inside/outside draws test fallback bounds. Callback, identity and facts checks are meaningful. Teardown clears external surface references and then destroys the actual producer root after bounded yields.

The queued portion calls production queue methods synchronously; it does not test automatic GPU governor thresholds or physics-frame scheduling. There are no glTF finishes, real batches, field ticks, immediate refresh/lifecycle checks or visual/performance claims in this focus. Root retirement does not independently count every material's release. These limits match the intended next actual-root proof.

The stronger observer fixes the short-circuit accounting issue: each row's identity and state and each lifecycle result is evaluated before combining with the accumulated result. It snapshots the complete live registered lifecycle write set immediately before pushing; overlaps with already poisoned case materials use saved original values. It restores the union of registered and case-only targets, then compares the complete registry snapshot as well as the existing case state, facts, force and intensity snapshots. Production `refresh()` writes case state rather than lifecycle, so that restoration order is coherent. Returned observations contain scalar values and IDs, not strong material references.

One policy remains deliberately unresolved. If an already tagged draw is copied or reparented across floors, admission can remove the former row while retaining the old case tag and state bounds. The physical binder may update its living field, but the destination case does not automatically acquire it. The unchanged binder can also still conflict with manually supplied stale foreign rows outside a fresh admission rebuild. Neither condition is proved fixed by this package, and neither is required to prevent the static wardrobe claims demonstrated in full02.

Accept only the exact original/omission red and candidate/restored 63-label green with clean diagnostics, then the existing 36-check regression and the actual full-root observer under the final bound owner. Preserve the full02 red and all earlier instrument failures. This review adds no runtime acceptance or tier promotion.
