# Full V1 renderer foundation candidate review

`candidate_v1_foundations_01` completed with 305/305 checks,18/18 captures, engine0/gate0 in 80.919 seconds. Runtime inputs were stable and the exact BuildingRoot candidate was restored. Native unpair0, soft-shadow underflow0 and retention0; no route or arrival diagnostic was emitted. This does not establish an actual F01 guest arrival/dwell/return lifecycle.

Source-bound receipt: `../evidence/vulkan_composed/runs/candidate_v1_foundations_01/result.json`. All 18 directly inspected image hashes/notes: `../evidence/vulkan_composed/candidate_v1_foundations_visual_review.json`. Scoped distributions: `../evidence/vulkan_composed/candidate_v1_foundations_cost.json`.

The actual cabinet viewport renders a legible Operations attract scene before and after the Passage sequence and return. Its visible geometry remains present. Full-root arcade observations record no ownership/world/mask issues. The separate-world target is present, restored, then absent when blocked; the actual mirror/reflection and all four phone captures contain scene content. No new gross visibility defect was observed in these views.

Existing inspection limits remain: Harukiya is too dark to assess room apparatus in the broad view; the street stance has a large central panel obstruction; the carried device obscures the right foreground in main-camera compositions. The direct cabinet images resolve the specific private-world content question, not these broader presentation debts. No human acceptance is inferred.

Diagnostics retain5 exact inherited missing-loader-manifest errors,2 loader warnings,1038 RGB8 conversion warnings and1 no-legal-wall warning for cam_noel_witches. These warnings are preserved, not recast as an error-free release.

The measured set is36 transitions with144 sampled frames, separate from warmup, capture, standalone controls and teardown. This single candidate has no matched omission comparator yet;80.919 seconds is process walltime, not transition cost or a release budget. Native logging, live actors, render populations and recorded adaptive SurfacePass state constrain any later comparison.

The unchanged legacy Harukiya >250 assertion is not weakened or declared resolved by this305-check fixture. Its actual eligible/indexed census remains separately attributable.

A material current transition-cost concern was reported before further engine runs: Street to Passage synchronous apply median622.336ms (n6), Passage to Street615.593ms (n6). Grouping all Street arrivals gives324.912ms and hides the two direction populations. The measurement contains only the synchronous `_apply_visibility` call at fixture176-178; frame waits/captures are outside it. The prior failed helper-only candidate already recorded comparable roughly611ms Passage cost, so this is not demonstrated as a new foundation regression. Raw source crashed without usable timing arrays; attribution to the helper remains unresolved until a completed matched control.

Separate unclosed source/log finding: ApartmentEncroachment storey-material slots grow across sweeps because the caller appends after _bind_living already registers the material. The 305 renderer checks do not measure unique material ownership or establish zero second-cycle growth. A per-floor slot/unique-ID census is pending; this lifecycle/performance debt remains open.
