# Matched paired-bookkeeping and transition-cost review

`foundations_pair_01` establishes the separately declared negative contract in the actual V1 `root_retirement` scope. Omitting only the helper operation produced 1174 stale-light-set errors and 2316 soft-shadow underflows at the predeclared native sites; the process exited 0 with 296/296 checks,15/15 captures and actual shell/world retirement. The strict gate remains1 and its unpair-only classification remains false. Exact restoration then produced engine0/gate0,296/296 checks and 15/15 captures with both native signatures0 and retention0. Other source/assets, engines and wrappers matched; original candidate bytes were restored exactly. This observed source-only negative is not candidate clearance or an exception for arbitrary underflows.

Immutable sequence: `../evidence/vulkan_composed/pair_bookkeeping_sequences/foundations_pair_01/receipt.json`. Both source-bound child results are under `../evidence/vulkan_composed/runs/`. The broader initial raw crash remains failed mixed-cause evidence; this smaller completed scope explicitly excludes the separate-world deletion case. The preceding full candidate separately completed 305 checks and 18 captures, including that owned case.

All 30 paired images were directly inspected; their hashes/notes are in `../evidence/vulkan_composed/foundations_pair_01_visual_review.json`. The actual cabinet renders the Operations scene before/after in both runs. Main geometry, phone views and mirror/reflection contain scene content. No gross disappearance was observed. Dark Harukiya, the central street obstruction and held-device occlusion remain inspection limits. A live resident appears in the restored reflection, demonstrating why exact pixel equality and static population are not claimed.

| Direction | Omission median ms | Restored median ms | Samples each |
| --- | ---: | ---: | ---: |
| Street to Passage |755.713|617.063|6|
| Passage to Street |617.259|610.729|6|
| Street to Orison |49.019|39.754|6|
| Orison to F04 |625.742|4.037|6|
| F04 to Orison |3.016|2.950|6|
| Orison to Street |34.822|36.683|5|

Exact per-direction and rendered-frame distributions are retained in `../evidence/vulkan_composed/foundations_pair_01_cost.json`. The 36-transition/144-frame signatures match. The approximately 610ms Passage-to-Street stall persists when the helper operation is absent; this is evidence against assigning that entire stall to the helper. Native diagnostic logging heavily confounds other differences, particularly the failing Orison-to-F04 transition. There is no isolated helper-overhead estimate or shipping performance claim. Warmup, captures, standalone controls and teardown are outside the timing sample; process walltimes 88.233s/79.384s are not transition costs.

Read-only source/log review found separate unclosed lifecycle bookkeeping growth: ApartmentEncroachment._bind_storey reuses the existing array, calls _bind_living which already registers/deduplicates, then unconditionally appends the same material. Existing logs grow by roughly 118/126 slots per F02/F04 sweep. These passing renderer checks do not measure unique material ownership or prove zero second-cycle growth. A diagnostic phase/material-ID census is next; no material or performance source fix is included here.

Independent wrapper review found damaged artifact metadata could omit consumed log/probe entries. The original execution receipt is preserved. The hardened offline verifier now requires those entries and the complete immutable file census, plus source-copy/hash-map agreement; both real child receipts have all 75 artifacts and reproduce their strict gates. This is recorded in `../evidence/vulkan_composed/artifact_map_hardening/receipt.json`, with 57 pure controls passing. No engine rerun or rewritten historical claim was used.
