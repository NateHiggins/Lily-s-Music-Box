The installed Vulkan Forward+ engine reproduces the unpaired-light defect, and the isolated pre-change same-scenario rebind removes it. Omitting that single operation brings the failure back. This establishes a scoped engine workaround on primitive geometry. **The live game has not received the patch, and its V1 teardown and performance remain unresolved.**

All runs used the unchanged repository serial runner, a new standalone project copy and APPDATA, 1280×720, Godot4.7.1 `a13da4feb8d8aefc283c3763d33a2f170a18d541`, Vulkan1.4.341 / Forward+ / NVIDIA RTX4080. After an initial wrapper identity lookup correction, both installed launcher/GUI hashes and all fixture/runner inputs were identical before and after each run. Root's save/title commit during this work is unrelated to the copied project sources.

| Run | Actual engine exit | Diagnostic gate exit | Functional checks | Pairing diagnostics | Interpretation |
| --- | --- | --- | --- | --- | --- |
| [static_01](../evidence/vulkan_layer_pairing/runs/static_01/result.json) | 0 | 1 | 17/17 | 0 | Preserved wrapper-binding failure; excluded from accepted four-case control set |
| [static_02](../evidence/vulkan_layer_pairing/runs/static_02/result.json) | 0 | 0 | 17/17 | 0 | Clean ordinary drawing and light deletion |
| [raw_01](../evidence/vulkan_layer_pairing/runs/raw_01/result.json) | 0 | 1 | 21/21 | 2 | Expected real native diagnostic red |
| [candidate_01](../evidence/vulkan_layer_pairing/runs/candidate_01/result.json) | 0 | 0 | 21/21 | 0 | Scoped candidate green |
| [omission_01](../evidence/vulkan_layer_pairing/runs/omission_01/result.json) | 0 | 1 | 21/21 | 2 | Same native red after omitting only the rebind operation/counter |

The red diagnostic is `BUG, indexing did not unpair geometries from light.` Raw and omission each emitted it twice. Neither generated the separate soft-shadow underflow from the earlier V1 capture; resolving that production signature remains unproven. All five runs had zero retention diagnostics. Each also retained five inherited host Vulkan layer-manifest errors and two host loader warnings. Those are classified separately, with complete stderr retained. Unknown errors still fail the external gate.

The first run exposed a binding defect in this new wrapper: Python's Windows executable search selected a console launcher copy in the workspace, whereas the unchanged serial runner's PowerShell `Get-Command` selected the installed launcher and sibling GUI executable. The wrapper refused the incomplete two-binary binding; it initially labeled that refusal generically as a source/runner change. Inspection showed the actual scene source was unchanged. `static_01` and its original message remain intact. The corrected wrapper uses the same PowerShell lookup as the runner and reports engine binding failures separately. `static_02` was a fresh actual rerun. Ten synthetic Python gate controls now include this failure, pairing errors despite engine exit0, malformed red controls, missing receipts/captures, source drift and unknown Vulkan errors.

In each dynamic case there were15 actual layer changes and10 unchanged requests. The candidate performed exactly15 rebinds; raw and omission performed0. Independent PASSAGE/STREET blockers compose: releasing the first cannot reveal the object; releasing the last restores its authored mask1. Six further hide/restore cycles advance rendered frames. The helper never changes the node's visible property, and the neighboring cube retains its own mask and visible state.

All20 actual local PNGs were inspected with the image viewer, including the initial wrapper-failure run. In both static runs the orange object and cyan neighbor remain visible in all four frames; deleting the light removes direct illumination and shadows. In raw/candidate/omission the orange object is visible initially, absent while blocked, restored, then absent when the light is deleted in the blocked state. The cyan neighbor stays visible throughout. Target samples are2,236 orange pixels when visible and0 while hidden; the cyan neighbor remains1,229 sampled pixels. There was no visible stray target or lost neighbor in these frames. This is Codex inspection of primitive fixtures, not human approval or a V1 game image review. The aggregate binds every reviewed image to its SHA256.

The raw and candidate36-frame repeated-cycle stages took646,066 and648,985 microseconds respectively. These single smoke timings include frame waits and do not establish a production performance result. The full-game layer gate controls thousands of instances, and same-scenario rebinding queues bounds/dependency work beyond the upstream five-line fix.

The [source review](vulkan_layer_pairing_review.md) retains the primary [Godot issue](https://github.com/godotengine/godot/issues/121989), [merged fix](https://github.com/godotengine/godot/pull/122064) and exact matching official source. The next minimal production patch is [prepared outside the live game](../work/vulkan_pairing/production_proposal/zone_layer_pairing.patch), based on commit `9548301873807117a870aeb6e517173e81a4713f`. It changes `_zone_toggle`'s two assignments and adds a synchronous changed-only helper. Blocker tables, saved masks, `.visible`, scene hierarchy and renderer selection are preserved. A read-only patch applicability check passes; Godot has not run that proposed production file.

The [decisive production test plan](../work/vulkan_pairing/production_proposal/README.md) names StreetCoreVisibilityTest, PassageVisibilityTest, overlapping/hidden-owner/nondefault-mask/SubViewport controls, rendered STREET/PASSAGE/ORISON/F04 transitions, performance attribution, actual V1 historical-notice capture and a same-consumer omission red. Root owns that integration handoff and any residual renderer errors. No live game file, engine installation or Git state was changed by this reproduction work. The Godot lane was released to branch for its last save regressions after the four cases completed.
