# Full02 material ownership: independent binder review

The eight material observation failures are real. The run completed 314 checks: all 305 original renderer checks and material/root retirement passed, while eight added ownership observations failed. Exit was 1. The separate signed-ObjectID parser defect cannot explain these GDScript failures.

Receipt: `evidence/vulkan_composed/runs/material_ownership_7c54_v1_full_02/result.json`, SHA256 `d51ec57d10aa785875b1dfdf609d7e4c4d20eaabe21ca8036c5864002409dcdf`. All 58 retained artifact hashes match. Both 4,043-entry source maps and diff hashes agree; all 28 retained source copies match their bound manifest. The actual typed fixture was `c2642de6f0c099a18e8d7c0f740f94a84f0d5bc90cc255973dee5ab6ad2ee6b5`; the current fixture was restored to `5ec63c36…`. Production owner7c54c, SurfacePass2e7fc and rootcde44 match the run. Engine manifests agree; this review did not rehash engine binaries or the live asset tree.

The raw footer at stdout4640 reports314 checks/8 failures. There are18 retained PNGs, not visually reviewed here. Raw stderr contains the five known loader errors, two loader warnings,1,038 RGB8 conversion warnings and one found-art warning. It contains no project/script error, native pairing error or retention report; wrapper streams are empty. These retained warnings are not a quiet-run claim.

## Causal chain

1. `apartment_encroachment.gd:898` trusts existing case metadata before ownership or spatial checks. For unclaimed props, line906 accepts geometry reaching up to `floor_y +3.6`. Actual case rows include four F03/3A wardrobe leaves under Mina2A, four F05/5A leaves under Peter4A and four F06/6B leaves under Cal5B. Dream's separate producer review establishes authored3.2m spacing and leaf lower bounds. The leaf/knob bounds distinction belongs to that producer evidence; no new cache collision is needed to explain this claim.
2. `build():172–230` inserts fields in case order; raw binding logs confirm **F02,F04,F06,F05,F03**. `_bind_storey():755–778` assigns the physical floor's material, then its supposedly root-level fallback at781–794 processes every case row below BuildingRoot. It does not restrict itself to root-level props or reject a different known floor ancestor.
3. F02/F04 register lower-case materials for foreign wardrobe leaves. Later F03/F05 binders install copies for their physical floors, leaving lower rows and registries stale. Probe IDs establish that each F02/F04 extra set equals its four stale row material IDs exactly. `_apply_prop_states():956` then skips those rows, so the actual draw misses the case refresh.
4. F06 is processed before F05. A later Cal5B row can overwrite its already registered F06 draw back to an F05 copy. Early observations contain four F06 registry extras and four missing installed IDs. At the final observation the draw is instead correctly bound toF06 but Cal's four rows are stale; the F06 registry then matches. This alternating manifestation follows the stale-row guard and metadata reuse, and is not a contradiction in the probe.
5. `_storey_material():798–808` copies a foreign material while retaining its case metadata. Case-owned copies are deliberately excluded from prior alias seeding at743. Thus the wrong case identity remains available for another callback and copies can churn. The local clear/rebuild at751–754 cannot promise global registry equality while a later floor is allowed to mutate the same draw.

The registry is unique and has zero invalid entries throughout. This remaining issue is competing ownership, not recurrence of the original duplicate-array growth. SurfacePass sharing alone is not a demonstrated defect: `apply_props():326–334` supplies a shared layered source and invokes the real callback, and BuildingRoot's actual passage transition calls it at2783. These repeated callbacks do not require a governor tier replacement. Retained cache consumers remain live and match the governor budget.

## Narrow correction and proof boundary

Use the existing actual `floor_nodes` provider by node identity before both case-tag reuse and spatial first claim. A recognized foreign floor must not be captured by the previous case's broad bounds. Preserve spatial fallback for genuinely root-level props, the existing boundary guards, and batched draws. Prevent the root-level row fallback from rewriting a draw under a different recognized floor ancestor. Array pruning, globally disabling source sharing, reordering floors or shrinking the height threshold would hide parts of the conflict without establishing ownership.

Declare and test any handling of deliberately seeded wrong-case metadata; a fresh-root correction alone does not prove in-place repair of those tags. The focused control should construct real adjacent-floor wardrobe leaves through their actual producer, cover both floor processing orders, assert semantic case membership independently of metadata, and check current draw/row/registry identity plus immediate refresh and lifecycle after repeated, late and queued callbacks. Keep true root-level props, same-floor material sharing, cache consumers and private/actor boundaries covered.

The first and final actual refresh probes failed their consumer checks while recording restored force, intensity, facts and sampled material state. Middle observations omit that mutation. The final ownership observation has a different issue set from the first seven; the JSON review preserves every exact issue and ID difference. The earlier36-check focused green remains valid for its declared constructed-fixture scope, which explicitly did not prove actual build paths. No new runtime, visual or performance acceptance is claimed here.
