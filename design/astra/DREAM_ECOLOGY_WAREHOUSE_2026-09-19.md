# Dream ecology warehouse — local integration

Evidence class: **INERT** — implementation and pause handoff; promotes no space, milestone, art acceptance, selector or release.

The owner redirected the paused reconciliation task to integrate voxel light and exhibit all dream ecology critters with their current behaviors. This checkpoint delivers the **warehouse debug integration**. Ordinary apartment RG8 ownership and floor-coordinate integration remain separate unfinished work. Main was fetched during this pass and remains **acdb4be42e5d973df623c3545aacde95855a1d07**. This local branch remains **codex/astra-reconcile-20260919**; its prior checkpoint was **f916240eab6ebe40a7716ea52daa509506caf7fc**. No push, merge, selector change or protected-asset edit.

## Open and use

In a DEBUG building launch, open **F1 → GO — teleports → Dream ecology** (alongside Prop warehouse). The catalogue is created lazily beyond the existing shed, using its actual transform and a safety-net exemption matching the new collision floor. All sixteen authored species appear together in two existing eight-animal batches. Select any species for a following camera, right-drag to orbit, and use the wheel outside the controls to zoom. F1 or Leave camera returns to the prior camera and controls. Reopening reuses the same exhibit.

The direct launcher is **tools/open_dream_ecology.ps1**. Run it with PowerShell from this repository. It imports twice and opens the same exhibit without constructing the campaign, through the shared serial Godot lane. Escape or closing the window ends the standalone inspection; the long runner has its normal 1,500-second ceiling. The launcher writes unique logs under LOCALAPPDATA/Orison/ecology-debug. Its native window occupies the shared lane until closed.

Lamp toggles current illumination. Pause suspends actual ecology clocks and field updates. Food residue uses the existing residue owner. Pulse / touch emits the existing mechanical packet for the listener; Noctiluca uses the accepted harness's explicitly labeled transient debug stimulus. Reset creates fresh one-hour specimens and preserves light history. Existing species behavior and identity rules are unchanged; no directional Euglena phototaxis was added. The real margin, palp and hero owners are present, but this pass does not claim every habitat interaction was demonstrated.

## Light and ownership

The exhibit owns one existing **DreamExposureField**, one RG8 **ImageTexture3D**, and one transient **DreamCritterVoxelBinding**. R retains exposure and G tracks reversible irradiance. The actual inspection lamp pose advances that owner and updates the same texture. Two fauna draws each retain their existing shared material and 84,000-triangle buffer; they bind the exact same texture. There is no per-animal field, texture or material. Separate batch materials are required because their eight-animal uniform arrays differ.

The binding helper validates RG8 dimensions and uses weak controller references plus explicit weak owner identity. It refuses competing live bindings. Its same-texture transfer regression proves a stale owner cannot release or overwrite a replacement owner's binding. Teardown clears retained material samplers. This is a transient presentation binding, not save state.

The accepted field wraps X/Z over 96 metres and clamps world Y to 0–4 metres. The warehouse exhibit occupies that vertical domain. ApartmentEncroachment actually creates accepted critters and LivingField/DreamFieldController owners, but contains no DreamExposureField owner. The earlier pause document's sentence claiming a floor-local RG8 owner there was inaccurate. DreamMazeRoot owns the accepted RG8 field but currently uses its different fauna director. The carried lamp's RGBA16F volume is a different contract. None was silently substituted or falsely reported as ordinary-play integration.

All debug geometry uses render layer 20, and the exhibit's lights illuminate only that layer. The inspection restores prior player processing, physics, unhandled input, mouse capture and camera processing. The existing debug menu is hidden during inspection so its wheel handler cannot intercept zoom. Camera framing keeps controls beside the animals; the Lacrymaria station accounts for its shader's 6.4-body-length neck. Some listener feeler tips can extend beyond the default close-up; wheel zoom and orbit remain available. Existing dark-membrane/colony legibility debt is not reclassified as accepted art.

## Measured evidence

Evidence folder: **C:/ov/astra-ecology-20260919/**. Native runs use the warmed diagnostic checkout **C:/ov/astra-r1-runtime-55e99dc**, with exact named current task sources copied from production. This is a modified diagnostic tree, not pristine 55e99dc, a fresh committed candidate, or a claim that unrelated source in the two trees matches. **tested_sources.json** records LF-normalized task hashes. The wrapper receipts bind the diagnostic tree; none is a schema-2 runtime_contract.

- Two imports completed with empty stderr.
- **DreamCritterVoxelBindingTest_checkpoint.log.receipt.json**: 34 checks, zero failures, empty stderr. Includes shared GPU readback, R retention/G decay, replacement, departure, weak references and same-texture ownership transfer.
- **DreamEcologyWarehouseTest_checkpoint.log.receipt.json**: 105/105, empty stderr. All sixteen actual packed shader kinds, one-hour lives, real six-centimetre grazer panel/twin, 32 seconds of differentiated law observations, lamp R/G response, pause/resume, reset without resource growth, retained-material teardown and twenty native captures. All sixteen close-ups and the overview were visually inspected across the native review passes.
- **DreamEcologyEntryTest_checkpoint.log.receipt.json**: 13/13 through the actual building DEBUG button, ninety composed physics frames, wheel event, physical F1 event, player/camera restoration and reuse. **Stderr is not clean:** one inherited found-art placement warning and three resident-route errors (F01, F03 and F05) remain; the preceding composed runs also exposed F04 refusals. No ecology script/shader error or exit resource leak was observed. These route errors are consistent with the unfinished nav checkpoint and are not waived by the passing exhibit assertions.
- **DreamCritterVoxelTest.log.receipt.json**: 2,111 checks, zero failures, empty stderr, on the earlier task snapshot. No accepted shader/generator/motion-law changes followed that run; later changes concerned exhibit presentation and the new input fixture. The receipt was verified as binding before subsequent diagnostic source changes.

Retained failures: the initial binding test did not parse because a WeakRef needed an explicit type; its 120-second timeout is no suite verdict. The initial composed input fixture reported 10/13: it omitted physical_keycode for the project's physical F1 mapping and injected mouse input through the host rather than directly through the tested viewport. The corrected fixture routes real events through the viewport's UI/input dispatch and checks restored state; it does not call the inspection input handler directly. Final diff review also caught Windows code-page corruption in pre-existing debug-menu labels and the earlier pause note; their original UTF-8 characters were restored before the checkpoint runs. Earlier captures showed the Lacrymaria head out of frame and controls extending below 720p. Reviewed captures retain the full neck and reachable controls. Original logs/captures were preserved.

The full working board is **board_working/board.json**: all tools suites passed except spatial at that snapshot, because it exposed two new raw debug camera coordinates. Those were repaired with the warehouse origin authority and a named overview marker. A subsequent live spatial audit is clean (zero new failing/class changes/vanished targets/save failures), and all 56 spatial tests pass. Nine incidental fixture records remain reported, seven inherited from the paused nav work and two from this task; no manifest or baseline was changed. Reader reports zero new unread fields. The full working board is not a final fresh candidate verifier; existing ledger blockers remain 7/8/136/50/159/161 across its six scopes, with 204 requirements. New debug-boundary systemic REVIEW rows remain visible; zero new actionable rows.

## Resume boundary

This is a local debug-feature checkpoint. The next production integration must resolve RG8 field ownership and storey mapping, then test rebuild/save/teardown and player light in composed V2. Do not manufacture an additional field solely to bridge the different existing optical systems. Keep selector v1 and all seventeen protected assets unchanged.

The earlier F06 opening-door route failure, newly observed F01/F03/F04/F05 route refusals, final fresh committed verification, inspection registrations and Blender provider/family work remain unfinished. Preserve **design/astra/ASTRA_PAUSE_2026-09-19.md**, its external prepared patches and all existing worktrees; its old spatial-pending statement is refined by the clean current scan above. The external inspection patch touches prop_warehouse.gd and must be reconciled with this exhibit addition before adoption. No blocked-candidate push or main merge is claimed.
