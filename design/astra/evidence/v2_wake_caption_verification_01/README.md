# Wake-room and acoustic-caption runtime verification

Base 710a150. This executes the checks left pending by the occupied Godot lane
in the preceding three source checkpoints. Every engine invocation used the
unchanged serial runner, Forward+ windowed rendering and the 180-second ceiling.

## Earned Dream boundary and 4B

boundary_01 exposed a test setup failure: the standalone boundary scene never
captured the mouse, so the player's normal interaction gate hid its prompt
although the ray hit the switch. The physical wake and six egress waypoints
passed. This failed run is retained. The test now captures input at startup,
as the existing ordinary route drivers do. The staged bedside camera also
targets the bed's global position rather than assuming an unrotated layout.
No production interaction, wake placement or orientation was changed.

boundary_02 passes 33 checks with empty stderr. It loads the previously earned
Dream-pending save, replaces V2 with the real Dream root and returns through
the public transaction. It verifies world/subject release, the bed's floor
elevation, six walking waypoints through both alcove/hall openings and back,
two additional waypoints for the real switch ray/prompt and E presses,
fixture power toggle/restoration, factual wake residue and disk reload.
The Dream transaction is driven directly; this is not a played Dream maze.

## Case presentation

captions_01 loads boundary_02's completed-wake save, walks the existing 20-point
kitchen route and passes 65 checks with empty stderr. The factual refrigerator
caption follows the food door through opening and closure. Explicit subsequent
case-stage and acoustic-signal controls check local and remote captions,
mounted-parent placement, missing/semantic subjects, weak/unrelated arrivals,
recurrence text replacement, duplicate reuse and unchanged case facts.
Injected arrivals are presentation controls, not proof of acoustic topology.

## Visual scope and remaining work

Inspected the actual earned_wake, staged bedside_review and reopened case
captures. The bed is present and lit, and the sofa claim remains readable.
The default wake view and surrounding enclosure still show unfinished building
presentation, including exposed sky at the side of the view. This is technical
verification of furnishing, controls and case displays, not room art acceptance.
V2 remains unfinished; V1 stays default and S2J remains open/failed.

The full earned-case regression result is recorded in summary.json alongside
the final log and route. Next work is 4B enclosure/wake presentation, remaining
apartment work and actual played Dream traversal before broader V2 acceptance.
