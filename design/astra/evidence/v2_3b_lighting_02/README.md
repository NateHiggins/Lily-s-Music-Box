# 3B room lighting and player switch targeting

The candidate mounts four production room fixtures, Omar's existing bench lamp,
four physical wall switches and the shared LightRig in the canonical V2 runtime.
The main switch moved from behind tool storage to the opposite doorway jamb.
No fixture energy increase was needed after inspecting actual light output.

`final.log`: 374 checks, zero failures, empty `final.log.stderr`. The unchanged
exclusive Godot runner executed the connected-world scene in a rendered window.
Two reconstructions exercise clear standing capsules, actual player ray/prompt,
input-driven off/on, emitted light output, circuit isolation, independent task
lamp power and looking-away negative controls. These are controlled standing
poses, not a continuous walkthrough. Input is injected at process-frame boundaries
because timer callbacks occur after the production just-pressed poll.

`final/` contains source SHA256 hashes and five native player-camera images at
November 10, 1928, 20:00, with the carried lamp disabled. Bath and sleeping-area
views are now readable. Blockout finishes and incomplete dressing still prevent
full visual acceptance. Earlier directories preserve failed placement and test
timing diagnostics; only `final/` is the final candidate receipt.

Remaining: continuous domestic routes with real door leaves, domestic targeting,
disk persistence, wider V2 acceptance and cutover. This does not authorize or
claim selector cutover; V1 remains the default.
