The admission-only floor repair is proven in the focused actual-producer contract. Original and omitted-guard runs both pass 43/63 with native exit 1 and diagnostic gate 1. Candidate and exact restored candidate pass 63/63 with native/gate 0. The existing ApartmentMaterialBindingTest passes 36/36. All five expected-control acceptance exits are 0; the red diagnostic results remain red.

The executed omission is not a byte-minimal two-line change. It also has CRLF line endings and 18 full-line comments with UTF-8 text misdecoded through Windows-1252. `postrun_validation.json` records every changed comment and proves all executable lines equal the candidate after the intended ancestry-guard removal and unused-local rename. Candidate2a440504 preserves original7c54 Unicode bytes. The omission's raw85d9 bytes, hashes, logs and receipts remain unchanged. This qualification supersedes earlier shorthand describing the omission as only the guard/local change. No engine rerun is needed for a comment-only packaging difference.

| Run | Passed | Native exit | Diagnostic gate | Expected-control acceptance |
|---|---:|---:|---:|---:|
| 01_original | 43/63 | 1 | 1 | 0 |
| 02_candidate | 63/63 | 0 | 0 | 0 |
| 03_omission | 43/63 | 1 | 1 | 0 |
| 04_restored | 63/63 | 0 | 0 | 0 |
| 05_binding36 | 36/36 | 0 | 0 | 0 |

Each `runs/<run>/result.json` binds the native result, diagnostic assessment, full before/after source manifests, selected raw source copies, actual engine identity and observed process ancestry. The four producer receipts are `runs/<run>/shots/wardrobe_floor.json`; each records two callbacks and actual root retirement. Existing36 has its established raw assertion/footer contract and no producer JSON; this review does not invent a matching retirement field for it. All Godot stderr files are empty and no native/script/warning/retention headers appear in the accepted raw streams. Outer Git snapshot commands did emit line-ending advice, so this is not a claim that all tooling was silent.

The actual authored storey spacing is 3.2m. FurnitureInteractionPass parents wardrobe mechanisms to the supplied floor node. Four low leaves of each selected upper-floor wardrobe overlap the old previous-floor +3.6m AABB window. The original and omission recover exactly LeftLeaf/FramedLeaf, LeftLeaf/RaisedPanel, RightLeaf/FramedLeaf and RightLeaf/RaisedPanel for 3A_w0_wardrobe,5A_w0_wardrobe and6B_aw_wardrobe in both normal and queued observations. This is the admission defect reproduced here. The repair collects the actual build provider's floor identities once per reach, checks the nearest recognized ancestor before existing case metadata/AABB admission, and retains bounded root-level spatial fallback. Providers include B1,F01 andROOF even though they have no resident case; existing explicit unit-floor fixtures remain supported.

The focused original also retains stale case material rows, extra F02/F04 registry IDs, synthetic no-case leakage and repeat-copy instability. All five physical field checks already pass in this focused original. Therefore this fixture does not reproduce or clear the full02 F06 wrong-field observation. The stronger root-owned full V1 d67 observer is separate composed evidence; its results are not imported here.

`postrun_validation.json` rehashed all269 artifact-map entries and five run receipts, recomputed20 frozen manifest digests, replayed all five assessments and verified equal before/after state. Each complete manifest has3909 entries; the only cross-run changed game path is apartment_encroachment.gd. The artifacts contain20 raw logs and140 selected source copies with16 distinct content hashes. Duplicate source bytes are intentional before/after copies, not additional independent proofs. No live asset tree or engine was rehashed for this final evidence review.

The final handoff receipt `final_state.json` records an empty Godot census and exact candidate source. `omission_restoration.json` records exact restoration, followed by a distinct restored-candidate run and the unchanged36 regression. Root owns the engine/source lane after that handoff. The original parent head during all five runs is da68962aaeaabf56851abb7190dda14d7ff5675f; the source manifests bind the explicit working overlay.

The producer uses real FurnitureInteractionPass/BakedFurnitureInteraction, actual owner build, SurfacePass and LivingField under synthetic floor roots. Queued methods are directly drained synchronously; this proves the callback seam, not GPU-governor scheduling. No finish/glTF, batched-mesh, full-building, visual, performance or collision acceptance follows. The no-case overlapping children are synthetic membership controls. Carried/reparented pre-tagged cross-floor materials and stale tags remain an unresolved policy; the extra binder row pass is unchanged. The successful171 one-census package remains sealed to7c54 and unapplied to production; it requires a rebase against this repaired owner.

Final owned source hashes at handoff:

- `game/scripts/reality/apartment_encroachment.gd`: `2a440504ab80404f4d9f4ab16138df7db9255801ca879ff91aabafe3c7f4d0ca`
- `game/tests/WardrobeFloorAdmissionTest.tscn`: `31bd226c4d6c4c4e0a7070689fad3eefd1d944656c442c64e2b8d6fbd24c2846`
- `game/tests/wardrobe_floor_admission_test.gd`: `8003596b3b6e0b39ab15cded32cff21aeeaf55202d2a971a75a8ebc299b184be`

Aggregate: `aggregate.json`, SHA256 `ba8a613e8c3b0dc7622bd9b607435ea4ce4188ef5514e8caa4f189a476237ebd`. Post-run validation: `postrun_validation.json`, SHA256 `19fe81b16f386b3f4c13f13b99dde5606979a01c847478880eb6783dbd872fcb`. The preparation package remains frozen at `design/astra/work/apartment_floor_ownership_01`; its READY_ENGINE_UNRUN status is a historical preparation state, now followed by these runtime receipts.
