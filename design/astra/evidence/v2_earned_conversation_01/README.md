# Physical first repair into earned conversation

`OrisonV2EarnedConversationRouteTest` continues the existing actual-input repair route without seeding repaired state or bypassing procurement. The player returns to physical Mina and follows the authored first-stable dialogue. The interpretation flag persists, the panel closes and movement unlocks, while the temporary repair remains unresolved with recurrence pending.

`route_02.log`: 76 waypoints, zero failures, empty stderr, on the relocated subway composition. Captures include the complaint and earned conversation.

The retained first run failed its closure assertion because the fixture checked the always-present CanvasLayer instead of the actual dialogue panel. The corrected fixture checks the panel throughout. No dialogue production change was needed. This proves the first earned conversation only; second visit, manifestation, integration, dream and wake remain open.
