# V2 earned recurrence and dream boundary

Base 435153e, canonical Astra worktree. The route begins with the existing
physical complaint, listening-head inspection, hardware purchase and repair,
then Mina's first conversation. No repaired or resolved case facts are seeded.

The second visit uses ordinary player input at the real time clock, returns
through the stairs to 2A, selects factual captions on all three evidence
objects, and uses the existing calibrator. The room's existing geometry,
resident timetable, electrical state and case rules remain authoritative.

The clock advances to 23:43, when Mina's Saturday timetable sends her to the
bodega. The integration route must approach the actual moving actor, not
assume she remains at her desk. Its approach follows the same authored route
graph through ordinary player movement. Dialogue choices use the authored
panel API after physical interaction opens each exchange.

The harness uses 2x simulation time and 120 physics ticks per second, preserving
60 physics ticks per simulated second. It restores both settings on completion.
All engine runs use the unchanged exclusive runner and 180-second ceiling.

## Earlier controls

- baseline: test-script type inference error; not a passing run.
- baseline_02: incorrect test route tried crossing the watch-room wall.
- route_03: 110 continuous waypoints, zero failures; physically earned second
  calibration. Final stderr empty for this scoped endpoint.
- integration_04: correctly failed because Mina was away on her timetable.
- integration_05: approach used an actor's transient ramp height as a standing
  point; the exact-height test rejected the capsule's supported slope stance.
  The corrected approach follows authored stair nodes.

## Scope

The existing V1 caption manifestation is not mounted in V2. Its SOFA, WINDOW
and refrigerator display positions refer to V1 furniture/layout. V2 needs
actual room furnishings and display anchors before these can truthfully be
placed. This packet does not treat copied legacy world coordinates as a fix.

The earned dream boundary test loads the route's actual serialized state,
constructs the real CampaignShell and DreamMazeRoot, drives the public dream
entry/outcome transaction, and reconstructs V2 at its bedside anchor. This is
world-transition proof, not a played Dream maze/chase or visual acceptance.
The V1 selector default remains unchanged.

## Final results and repair

- integration_06: **138 continuous waypoints, zero failures**, real-talk and
  integration earned from the first physical complaint. Exactly one dream
  request; the actual save owner writes earned_dream_pending.json.
- boundary_before: test WeakRef inference parse error; no result claimed.
- boundary_before_02: actual Dream/V2 replacement and bedside return passed,
  but the factual Mina residue was absent (14 checks, one failure).
- boundary_after: **17/17**, including released old world WeakRefs, exclusive
  real Dream/V2 composition, bedside return, resolved job, factual residue,
  idempotent binding, and exact residue retention through a real disk reload.
- case_regression: existing Mina authored-dialogue/gameplay suite passes.

Final run stderr files are empty. The complete physical route ran before the
wake-only production repair; the subsequent boundary test consumed its exact
save against the repaired sources. Source and input hashes are in summary.json.

V2 omitted the wake callback because it was attached only to V1's caption
manifestation. The unchanged factual record operation is now shared, and V2
binds it through its existing MinaCaseGameplay owner. A completed-wake reload
reconciles the record; repeated binding cannot add duplicate callbacks. V1's
existing display still invokes the same operation and refreshes its labels.
This adds no caption geometry or new case/clock authority.

All save artifacts originate from the test campaign. The boundary test writes
to its evidence output after reading the earned input. The player's save path
is never written. The saved residue uses the existing identity and anchor/socket
contract; this does **not** claim that V2 already renders its refrigerator label.

Next: finish 2A's real room furnishings and attach the manifestation to their
actual display anchors, then complete the waking bedside presentation and the
played dream path. Full-game performance, spatial/visual acceptance and V2
selector cutover remain open.
