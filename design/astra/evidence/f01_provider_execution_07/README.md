# F01 provider admission and integration

Baseline: canonical `37f0307`. The exact sealed optional provider is admitted by fresh candidate / deliberate omission / restored candidate runs: 32/32, exactly 30/32 (the two exterior exemption checks), 32/32. All three restore the original production root exactly and pass process lifetime isolation and diagnostic classification. Known Vulkan loader, RGB texture conversion and V1 found-art warnings remain; this is not warning-free product acceptance.

Matched composed captures: `../vulkan_composed/runs/f01_provider_legacy_03` and `f01_provider_owner_first_cells_04`, each 305 checks and 18 images. All six paired sheets in `../f01_provider_visual_review_01` were visually inspected: matching geometry, material placement, Passage/arcade/F04 composition and separate-world obstruction behavior. Rain, lighting and presentation phase differences remain. This establishes provider parity, not final art or performance acceptance.

Legacy capture's original process gate falsely rejected a known command line becoming unavailable at process retirement. The original result remains unchanged; its invocation folder contains an additive lifetime reassessment binding the raw samples and corrected classifier. Ten classifier controls include unrelated PID reuse, conflicting known commands, missing timestamps and foreign Blender. Earlier parse failures and the execution-06 PID reuse failure remain unmodified.

Windows pack export: `../f01_provider_export_01/export.log`, native exit 0, empty stderr; pack size 1,191,092,676 bytes. The actual pack was then loaded with `--main-pack` from an empty project directory, using an external verification script: 17 scenes, 597 mesh/material instances, all 609 exact imported targets including 12 collision-only targets, 102 nonempty imported albedo textures, zero failures and empty stderr (`verify_corrected.log`). The first verifier incorrectly expected 609 meshes; its failed log is retained. This is resource packaging proof, not an exported gameplay playthrough.

`integration.json` records the exact named files adopted by `work/f01_provider_execution_07/integrate.py`. The BuildingRoot consumer now supports the optional provider; legacy remains its default. All 17 cells are still resident. V2 streaming, exterior/Passage composition, resource/performance acceptance and full V2 completion remain open. The game selector is unchanged.
