# Resumed disk-save and world reconstruction proof

The initial four-direction matrix passed its 30 assertions but emitted a real
FunctionalProp error: an autoload acoustic delivery reached a detached fridge
and attempted to create a SceneTree timer. Detached/deleting props now reject
network/reality events; a generation check also rejects an old delayed event
after a remove/re-add cycle. The focused lifetime fixture passes four checks
with empty stderr and confirms current live events still arrive.

The next matrix run exposed a harness flaw: it reused fixed save filenames and
deleted only primaries, leaving crash-recovery sidecars. The production writer
correctly refused these incomplete prior sets. Runs now use random private test
namespaces, retain their primary and sidecars, and print the evidence paths.
No player save or existing recovery artifact was removed or overwritten.

`matrix_final.log`: 30 checks pass, all four V1/V2 save/reconstruction directions
and all four exact calendar round trips pass, production layout hash unchanged.
No ERROR or SCRIPT ERROR remains. Stderr contains the intentional invalid-selector
warning and the existing V1 `cam_noel_witches` found-art placement warning.
The unchanged serial runner executed all native runs exclusively.

This proves the matrix's semantic first-shift/core-loop facts and November 10,
1928 clock across midnight through actual disk writes and CampaignShell roots.
It does not prove every domestic transient state, every dramatic boundary or
full V2 cutover. Earlier failed runs are retained as diagnostics.
