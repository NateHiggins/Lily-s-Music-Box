# Connected V2 Vantry Arcade and hardware acquisition

The real V2 runtime now mounts the separately extracted entrance, Passage shell and eleven shop cells in the normalized front-door frame. It composes the existing surface pass, 49 authored light fixtures, eleven doors, shop lettering, grilles, hours director and three handcarts. The shared maintenance service mounts its hardware counter from the original furniture anchor, transformed by the same frame. These are 13 resident geometry cells; streaming is not claimed.

Integration exposed an existing V2 composition defect: the root created and injected MaintenanceShopService without calling setup. The exterior correctly treated an injected service as already configured, leaving stock and inventory/WorkOrders references empty. The root now initializes that sole service before injection. The bodega route fixture explicitly checks those references and the authored capsule stock, preventing an empty service from satisfying its refusal check.

## Actual player route

`route_04.log` passes 36 continuous waypoints with empty stderr: interior, street, both kerbs, arcade entrance, throat, nave, hardware doorway/counter and return indoors. Ordinary player ray/prompt/input opens and closes the door, buys the capsule, rejects a duplicate purchase and returns with the same item facts. The fixture seeds the awaiting-part job through public WorkOrders methods; this is a physical procurement route, not the entire opening golden shift.

The initial counter approach hit fitted geometry. Closing the inward leaf did not clear the long-front approach, so both failed routes remain. The final route uses the public end of the counter from the doorway turning area. No collision, furniture or capsule was reduced to make it pass. Both route_03 (before lettering) and route_04 pass. The final nave and purchase-prompt images were inspected: the shops are readable and the counter interaction is physically situated. Presentation remains short of final art acceptance; the shop is unstaffed, the streetscape is unfinished, and the enclosing world still needs composition work.

## Reconstruction and regression

`save_matrix_actual.log` passes 34 checks and all four V1/V2 directions. The existing actual-disk CampaignShell test now also acquires a capsule through the public production transaction, saves it, reconstructs either root, compares exact provenance/timestamps and rejects another acquisition. Calendar and existing shift/return facts also survive. The continuous physical-input acquisition is independently demonstrated by the route above.

The matrix retains the deliberate invalid-selector warning and existing V1 found-art placement warning. It also found stale texture UID references in the generated F01/F04 scene caches. Only those two generated `.godot/imported/*.scn` files were moved into `cache_before` while the native lane was empty; the unchanged source assets were reimported. `cache_verify.log` loads both refreshed scenes with empty stderr. The original matrix and caches remain preserved. This cache repair is not an authored asset modification.

`composition.log` passes all 374 rendered connected-world checks with empty stderr, including two root reconstructions and the 3B physical switches. The first arcade invocation failed because the new global script class had not yet been imported; the verified owned hung process was stopped and `import.log` completed successfully. The first matrix command used a nonexistent scene spelling; its error remains in `save_matrix.log`. Neither failure is counted as a pass.

## Remaining

The construction shed/world edges, actual asynchronous residency, durable streamed physical-state reconstruction, full golden shift, NPC shop service, all remaining floors/apartments, derived acoustic topology, full resource/performance review and human acceptance remain open. The newly mounted fixtures are not presented as a completed derived acoustic graph. V1 remains the default selector; no release or complete V2 claim.
