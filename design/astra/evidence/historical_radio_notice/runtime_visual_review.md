The final historical notice passes its bounded V1 inspection check. All three images in `capture_03/shots` were opened individually: the fixed paper and semantic interaction prompt are visible, and the real player ray presents the complete three-line field copy before and at November 11, 1928, 03:00 EST. The prior `capture_02` images preserve the clipped fourth line. Only this notice's body and condition were shortened; the global TelegramHud remains unchanged.

This is a teleported inspection in the actual V1 production root. It proves neither a walked route nor human acceptance. V2 placement, resident document-reading behavior and authentic historical broadcasting remain absent. The notice is a fictional tenant hand-copy of sourced license information. The paper retains both columns; the field copy reads CampaignClock civil time and explicitly says programme hours are not given.

Fresh-profile results through the unchanged serial runner:

| Check | Final result | Preserved negative control |
| --- | --- | --- |
| CampaignCalendar | `calendar_05_restored`: exit0, 50/50, no diagnostics | `calendar_clamp_omitted`: exit1, 49/50. Removing the final seconds clamp produces hour24 at the preceding representable double. |
| AdminPrereq | `admin_01`: exit0, 29/29 | Future dream revision999 is deliberately rejected with its expected warning. |
| Historical notice | `focused_09`: exit0, 17/17, no diagnostics | `notice_owner_cleanup_omitted`: exit1, 16/17. The final owner's stream remains referenced when its cleanup is omitted. |
| Actual V1 notice capture | `capture_03`: exit0, 3/3 frames, all inspected | `capture_02`: exit0, 3/3 frames, last body line visibly clipped. |

The board now stops and detaches only its paper-tap player, releases only its matching recorded-stream cache entry, frees that player and retires its local tween on exit. A live neighboring board keeps its stream and playback. The focused harness completes scene removal and permits the mixer to drain before checking release; synchronous observer helpers prevent its own coroutine temporaries from retaining the observed stream. A same-frame debounce was investigated, proved unnecessary and removed. All intermediate parse failures, premature-shutdown logs and observer-retention diagnostics remain preserved with exact source copies; they are distinguished from product omission failures in `runtime_visual_validation.json`.

The first calendar edge literal rounded up to midnight inside Godot before reaching the clock. The corrected fixture constructs the previous representable double explicitly. Both the genuine clamp-omission failure and restored passing run are retained. Omission scripts restore the original source byte-for-byte in `finally` blocks, with mutation receipts proving restoration.

The V1 windowed run is **not a clean runtime acceptance**. The final capture still reports 261 `geom->softshadow_count==0 - BUG!` errors and 1,252 geometry/light unpair errors. Queued world retirement, two rendered frames and a brief mixer drain did not resolve them. Five host Vulkan layer-manifest errors, two host layer warnings, 965 RGB8-conversion warnings and the existing `cam_noel_witches` placement warning are also preserved. The final capture has no ObjectDB/resource-retention diagnostics. No renderer-wide workaround, disabled light system, registry edit or diagnostic suppression was applied.

`runtime_visual_validation.json` binds the 21 preserved runs, six inspected images, final selected-source copies, classifications and remaining limitations. `canonical_runtime_bindings.json` supplies audit-compatible runtime text hashes derived from each frozen manifest: the first custom runner used path/NUL/hash/newline framing, while the production audit uses compact JSON pairs. Historical receipts are unchanged. The final audit-compatible runtime digest is `950b6ea1c323083064b18306adc8c520dacea988e20bcbbf4dfd8cc48eeeb533`. Texture/audio asset identity remains separate evidence; these composition receipts do not promote structured runtime requirements.

The Godot lane was released to the root agent after all processes finished. No staging, commit or live-ledger update was performed here.
