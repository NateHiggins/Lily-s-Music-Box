# Historical wireless notice: source-ready, runtime proof pending

The owner selected November 10, 1928 as the opening date. The new consumer is a fictional tenant hand-copy of published license information, physically mounted beside the existing V1 `LobbyBulletinBoard`. It has its own inspection area and uses `PropControlArea` and the existing `TelegramHud`/service-set printing path. Both assignment columns remain printed on the paper. The dated field copy chooses the applicable column from an injected `CampaignClock.day_info()` snapshot; no timer rewrites the paper or distributes NPC beliefs.

The FRC's October 25 transmittal makes the revised licenses effective November 11 at 03:00 Eastern Standard Time. Its table supports the revised WEAF 660, WJZ 760, WOR 710 and WNYC/WMCA shared 570 assignments. The transmittal and relevant table rows were checked directly in [this primary-document transcription](https://www.thebdr.net/wp-content/uploads/PDF/Profiles/Broadcast-History/the-great-frequency-move-of-1928.pdf). Earlier assignments retain the primary-source references and access limits recorded in `reviews/campaign_date_research.md`; source identities are embedded in `game/data/historical_radio_reallocation.json`.

`before.json` preserves the old consumer census: no dated wireless control or data existed. `source_validation.json` records data/connection checks and exact source hashes. Neither file claims a Godot run. The authored focused scene is `res://tests/historical_radio_notice_test.tscn`; its execution and red fixture proof await the parent's serial Godot lane. Visual inspection in the actual V1 lobby is also pending.

The focused test covers the minute boundary, legacy schedule-key independence, late reconstruction, an unavailable civil date, the actual physical ray owner and control-to-presenter result, unchanged printed text, unchanged campaign/knowledge state, and the absence of a new broadcast emitter.

V2 placement remains absent. NPC document-reading behavior remains absent. No historical speech, program schedule, 1931 recording, ghost-radio change, or authentic broadcast is claimed.
