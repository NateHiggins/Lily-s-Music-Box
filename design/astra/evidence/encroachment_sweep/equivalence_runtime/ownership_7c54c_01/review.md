# Focused one-census method equivalence — completed

All four source-stable headless runs completed the exact 171-row contract, both authored case orders and actual retirement. The live production owner remained 7c54c; only six separately loaded test files were installed. The one-census production proposal b5b977 remains unapplied.

| Selected copy | Checks passed | Actual native exit | Diagnostic gate | Selective controller |
|---|---:|---:|---:|---:|
| Candidate | 171/171 | 0 | 0 | 0 |
| Reversed priority | 157/171 | 1 | 1 | 0 |
| Cached first census | 155/171 | 1 | 1 | 0 |
| Exact restored candidate | 171/171 | 0 | 0 | 0 |

Priority produced exactly two named overlap-owner failures plus all twelve differing phase comparisons. Drop-late produced exactly four named failures in each order (membership count, new draw discovery, new owner alias and immediate new-draw state) plus eight later-phase differences. Its four initial/repeated comparisons stayed equal. No baseline, unrelated assertion, native/script/warning or retention error occurred. Red stderr contains only the recorded named assertion failures. Both greens have empty Godot/runner stderr. Git diff emitted outer line-ending advice; it is separate from engine diagnostics.

All four before/after complete game, runtime-text, instrumentation and installed-engine bindings match. Actual wrapper→console→engine ancestry was observed. The final current fourteen selected files were rechecked, the process census was empty, and the six successful tests were left installed. Root owns the engine/source lane again. Raw receipts are under runs/01_candidate, 02_priority, 03_drop_late and 04_restored; aggregate.json binds them, install.json and final_state.json. All 220 recorded artifacts were rehashed with zero missing files or mismatches; no raw run files were rewritten.

This establishes the bounded actual-method/material equivalence against the repaired owner and meaningful red controls. It does not establish production build ownership, glTF composition, queued governor callbacks, shader pixels or performance. The fixture creates SurfacePass materials and calls the real method/field consumers on authored synthetic bounds; the separate 36-check material-binding proof owns callback behavior. The approximately two-second serial process durations are not reach_props timings. Real-root material, rendering and sampled transition measurements remain separate work.

Prepared package and 25 offline controls remain sealed at runtime_plan_01. Its isolated omission proof retains the two intended raw-assertion admission failures. No fixture or instrument correction was needed during this four-run sequence.
