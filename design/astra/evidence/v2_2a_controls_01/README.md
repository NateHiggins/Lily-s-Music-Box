# 2A main-room light control

The existing Mina work-room pendant now belongs to the shared semantic room-lighting owner. Its position, energy and range remain unchanged. A physical bakelite/porcelain wall switch at the main-room east wall controls that circuit. The case-object placement owner no longer constructs a separate unbound light.

The rendered route passes 25 continuous player-input waypoints with empty stderr, including both switch actions, circuit isolation against 3B, supported evidence and calibrator interaction. The carried lamp is off. Captures are taken before the action: case_room_switch_false.png shows the powered room before switching off; case_room_switch_true.png shows the dark room before switching on. Both were visually inspected: plate is reachable and the room responds, but apartment surfaces/fit-out still require substantial finish work.

The composed rendered suite passes 439 checks with empty stderr. Its old F03-only stance height now derives each plate floor from its semantic record, allowing real F02 and F03 input/capsule checks. This adds runtime light control, not saved switch state, full apartment completion or human acceptance. V1 remains the default.
