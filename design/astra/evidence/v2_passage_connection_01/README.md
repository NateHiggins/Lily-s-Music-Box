# V2 street section and isolated arcade entrance

The local V2 roadway was 3.1 m wide. `tools/build_v2_street_section.py` now reads the original generator's numeric street-section expressions without executing the generator, registers them against the original front-door leaf center, and updates only the isolated V2 geometry and spatial records. The clear roadway is 9.144 m; north/south kerb faces are z=4.955/14.099 and the arcade building line is z=18.521. The far pavement reaches that line. Curb furniture, wet patches and sidewalk joints follow the corrected north edge. The pavement surface center/extent and region boundary follow the new geometry; compensating UV offsets preserve existing world-space bodega and route placements. Service authority is unchanged. Both generated outputs are byte-idempotent. The V1 generator, layout and assets are unchanged.

The actual connected V2 runtime passes the rendered 18-waypoint building/bodega/return route with door and counter targeting, shared inventory and wrong-shop refusal (`connected_route_rendered.log`, empty stderr). The first headless route reached the door but failed the ray/prompt check; its original failed log is retained, and the fixture now prints interaction diagnostics on failure. It is not relabeled a pass.

`OrisonV2StreetCrossingTest` performs the normal controller's entire 21-waypoint interior/street/road/far-pavement/return walk, including both kerbs, without relocation after initial placement. `street_crossing.log` passes with empty stderr. This proves the new street's collision route, not an arcade interior connection.

The additional `street_crossing_review.log` also passes 21/0 with empty stderr and captures the actual street from the arcade building line. That image was inspected: the street is continuous, but the frontage is still visibly unfinished and the scene is dark. Final spatial-record composition passes 374/0 in `semantic_composition_rendered.log`, with empty stderr and two world reconstructions. The earlier headless composition fails exactly eight prompt checks: PlayerController intentionally suppresses desktop prompts when mouse capture is unavailable. That failed evidence remains. Subsequent source formatting changes were independently checked to preserve every parsed JSON value; no geometry or spatial value changed after final validation.

The native spatial inspection establishes why the full street owner cell cannot simply be overlaid on V2: it includes broad city meshes as well as the entrance. `tools/build_v2_passage_gateway.py` extracts exactly 12 authored entrance roots into the approved V2 asset home, preserving their transforms, meshes, materials and collision suffixes. It references the unchanged admitted buffer/textures and records source/resource hashes. `gateway_import.log` and `gateway_parity.log` pass with empty stderr. The latter compares 34 actual imported nodes, surface-array hashes, material/texture identities, transforms and collision faces against the original street cell. No missing or extra nodes.

The first spatial inspector failed because its output directory did not exist. Only its verified owned hung engine was stopped; the corrected inspector creates/checks that directory and exits on output failure. Both logs are retained.

The extracted entrance is not yet mounted in V2. Next: register that entrance and the existing Passage hall/shop cells in the same normalized frame, complete the construction-shed/portal connection, compose existing lights/doors/shop services, then prove the continuous arcade hardware route and actual residency/reconstruction. The street extension remains a structural surface, not finished streetscape art. No streaming, completed golden shift, final visual acceptance or V2 selector cutover is claimed.
