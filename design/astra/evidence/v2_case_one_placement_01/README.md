# V2 case-one placement and caption reconstruction

The production V2 root now places Mina's three evidence objects, calibrator, shift clock, recurrence letter and voice source relative to named V2 owner reservations. MinaCaseGameplay accepts the complete transform set atomically; legacy composition retains its existing placements. Two physical tables support the evidence and calibrator. A main-room pendant lights the work area. The V2 evidence models are stacked cards, a bound book and a pencil, with matching small collision bodies and the existing handling response.

`route_03.log` passes 21 continuous player-controller waypoints with empty stderr: core, stairs, public approach, 2A work area and return. It checks physical support, actual ray/prompt/input on every evidence object and the calibrator, all three factual caption choices, the home-threshold letter position, and refusal of an incomplete placement set without partial mutation. Complaint activation uses the public case owner and drains its authored dialogue; this is not a physical resident encounter or the complete golden shift.

`route_01` passed placement with generic evidence bodies and an unlit work area. Its capture prompted the models and pendant. `route_02` correctly failed a stronger assertion because the fixture assumed the first caption choice was factual for every object. `route_03` uses each authored choice sequence through ordinary input. Captures were inspected: the three objects are distinct, supported and lit. The calibrator and clock still need finished physical presentation, and 2A is not a completed apartment.

The existing 34-check disk-save matrix passes in `save_matrix.log`. Extending it exposed an unsaved local caption-choice index: a saved CARDS selection restarted at WAITING instead of advancing to FAILURE. The case owner now derives the next index from its durable selected caption. `case_save_matrix_02.log` passes 38 checks and all four V1/V2 directions, retaining exact calendar, inventory provenance, factual captions and correct next-choice behavior. The earlier expanded fixture parse error remains in `case_save_matrix.log`; only its verified owned hung engine was stopped. Final matrix stderr contains the deliberate invalid-selector control and known V1 found-art warning, with no new errors.

`composition.log` records the final connected-world regression. Geometry and case mechanics are not conflated: this packet does not prove the complete fault/repair, physical Mina encounter, recurrence or wake-return sequence. The V2 manifestation, remaining domestic fittings and controls, full acoustic/resource/performance review, other floors and human acceptance remain open. V1 remains default.
