# T4-2 — Juno's temporal listening bed

Approved production-root proof, 2026-08-24. Juno's existing delayed-feedback
partition now carries a fixed twenty-one-receptor sensory bed in three response
groups. It is not a future appliance: the production `DreamRoomBuilder` grows
speaker-cloth tympanum, cochlear delay, 1920s radio feedback and later phased-
array observation into one non-owning organ.

The being's misreading is specific: every channel received is treated as a
request to answer. Juno's ruled truth remains **Connection requires an open
channel**. The existing open-edge delay congeals the organ; sustained delivered
signal reopens the joint and removes it. No new clock, topology, collision,
navigation, pursuit, hazard, save fact, screen, brand or waking technology was
added.

## Frames

- `00_open_channel_before.png` — fixed production joint before its delayed echo.
- `00b_open_channel_control.png` — frozen A/A at the same pose and state.
- `01_delayed_echo_partition.png` — the delayed reply: speaker-cloth membrane,
  four brass echo traces and three spatially delayed receptor fans.
- `02_partition_lamp_off.png` — the self-readable organ with the unreliable
  lamp disabled; no anatomical state changes.
- `03_sustained_channel_reopens.png` — the same joint after sustained delivery
  releases the oldest partition.

Whole-frame normalized RMSE:

| Pair | RMSE |
|---|---:|
| control A → control repeat | 0.000000 |
| control repeat → temporal receptor partition | 0.0969389 |
| partition → lamp off | 0.000000 |
| partition → sustained release | 0.0969470 |

The capture freezes engine time only while the stills are taken, then restores
it before exit. This prices topology and anatomy rather than sixty frames of
the intentionally animated molten body.

## Executable proof

- `DreamProfileTest.tscn`: **65/65 PASS**. The reciprocal joint still seals and
  releases through the shared grammar; the visible batch contains exactly 21
  nonzero fixed receptors spanning three lengths.
- `DreamIncarnationTest.tscn`: **66/66 PASS**. Juno retains one immutable
  seventeen-map bundle, her exact truth, empty foreign-hazard allowlist and no
  incarnation runtime owner. All six roots retain identical gameplay facts.
- Production capture: **5 frames, 0 findings**, Forward+, 2560×1440.

During proof, Godot 4.7 exposed a shader-language defect independent of Juno:
Mina's local scalar `ink` shadowed the top-level architecture `ink` uniform and
compiled as an illegal uniform assignment. Renaming that local mask to
`caption_ink` restores the full production Dream material without changing its
math; the clean capture reports no shader compile failure.

## Reproduction

```powershell
$env:SHOT_DIR='C:\PleaseRemainOnTheLine\art\renders\dream_temporal_juno_t4'
.\Godot_v4.7.1-stable_win64_console.exe --path game --resolution 2560x1440 res://tests/JunoProfileShot.tscn

.\Godot_v4.7.1-stable_win64_console.exe --headless --path game res://tests/DreamProfileTest.tscn
.\Godot_v4.7.1-stable_win64_console.exe --headless --path game res://tests/DreamIncarnationTest.tscn
```

This is a bounded Dream sensory/anatomy extension through the already-approved
downstream profile seam. It does not complete Juno's waking fault, repair,
conversation, recurrence or coordinator loop.
