# N13 — Mae survives two histories

The production root uses seed `f123456789abcdef`, slot 4 and one ordinary
remembered junction. Two separately resolved reciprocal child routes return to
that junction through different doors. Both approaches mutate one room record
and one stable `contradictory_object_id`; no portal or duplicate room exists.

## Frames

- `00_one_object_before.png` and `00b_one_object_control.png` are the unchanged
  A/A pair.
- `01_first_provenance.png` adds the first record and the one allowed Tenant
  attention event.
- `02_same_object_two_histories.png` retains the same object and adds the
  incompatible second provenance without replacing the first.
- `03_contradiction_lamp_off.png` preserves the contradiction in darkness.

Normalized mean absolute deltas are `0.00640734` for A/A, `0.07140363` for the
first account and `0.20516475` for the second account. The contradiction is
therefore well above the live shader/animation floor.

`DreamProfileTest.tscn` passes **46/46** and proves distinct physical branches,
one stable object identity, two retained provenance values, one attention event,
no repeat on the second account or revisits, no hazards and earlier-profile
no-ops. `DreamRoomBuilderTest.tscn` remains **175/175**. The production capture
saves five 2560x1440 frames with zero findings.

This proves the shared downstream dream-profile seam only. It does not build
Mae's waking fault, repair, conversation, recurrence or coordinator path.
