# Dream fauna F1 — production proof

FA1 lands only the owner-ruled minimum slice: Gilder's Buttons and
Tessellates. `DreamFaunaDirector` is one presentation-only owner under the
production `DreamMazeRoot`. At a fixed 3 Hz it advances bounded nutrient,
grazer, predator and detritus densities; predator stays zero in harmless F1.
It reads live rooms, Atlas lineage, decay, the authoritative exposure field,
the real service-lamp pool and Tenant position. It writes none of them.

`DreamLineageBody` publishes its landed open birth-frame floor anchors as
metadata. Tessellates emerge from those real anchors, spread toward the crop,
and return below the frame when the Tenant hushes the room. Both families are
one shadowless `MultiMeshInstance3D` apiece with shader gait. They own no
collision, light, hazard, per-creature node or save record.

## Frames

- `00_control_a.png` and `00_control_a_repeat.png` are the equal-interval A/A
  control with fauna hidden. They price the live dream shader noise floor.
- `01_birth_frame_low_exposure.png` is the real birth-frame at its initial
  derived density.
- `02_lamp_gathering_ab.png` holds the production lamp on the authoritative
  field and advances ten fixed ecosystem ticks. The larger grazer realization
  is the A/B result.
- `03_tenant_hush_reabsorption.png` places the real shadows-only Tenant at the
  frame. Nearby grazers submerge; the low Gilder crop remains harmless.

Pillow RGB mean absolute differences at 2560 x 1440 are **0.4063** for A/A,
**0.5926** for control/low, **32.3924** for low/high feeding and **2.9621**
for high/hush. The two claims therefore clear the measured live-animation
noise floor rather than relying on a single changing frame.

## Measured limits

`DreamFaunaTest.tscn` passes **14/14**. It proves deterministic census and
slot realization, the 96-instance ceiling, four bounded harmless densities,
unfed death/reabsorption, real birth-frame publication, forgetting with no
retained record, identical per-room re-derivation, Tenant hush, embrace freeze,
R6 shared-world visibility, zero collision/light/shadow/per-creature nodes,
and byte-stable plan, hazards,
exposure and campaign save across a fauna refresh.

Final same-build `PERF_DREAM=1` control uses
`PERF_DREAM_FAUNA_OFF=1` after selecting the same worst yaw. At the stable deep
pocket station, fauna add exactly **2 draw calls** and **4,152 primitives**:

| Station | F1 off | F1 on | Delta |
|---|---:|---:|---:|
| deep pocket, lamp off | 133 calls / 2.10 ms | 135 calls / 2.10 ms | +2 / 0.00 ms |
| deep pocket, lamp on | 172 calls / 2.23 ms | 174 calls / 2.24 ms | +2 / +0.01 ms |

This clears the ruled +13-draw and +0.5 ms ceilings. The waking-room yaw sweep
was not used for delta evidence because its submitted architecture varied
between runs; the deep station remained exact. All four production stations
still pass the 16.6 ms frame budget.

Fresh shared regressions: `DreamProfileTest` 46/46, `DreamRoomBuilderTest`
175/175, `DreamPursuitTest` 39/39 and `DreamHazardTest` 42/42.

## Scope

FA1 does not license the Loupe, later families, audio, or dangerous fauna.
FA2–FA4 remain owner-gated. This is a downstream dream presentation system,
not a completed waking case loop.
