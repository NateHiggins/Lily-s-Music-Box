# Cast repopulation evidence — 2026-08-14

Selected frames from the ClipSheet passes (full 147-frame sheets are
regenerable per model: CLIP_MODEL + CLIP_LIB=1, personal library loads
automatically). What they establish:

- peter/cal walk vs <slug>_Walk: the shared walk baked upright onto
  each rig, and each model's own raw gait visibly distinct — no corset,
  no lean, proportions intact.
- rhea idle_12 vs evelyn_control idle_12: the forward-inclined idle is
  the SOURCE clip's content — the reference body (Evelyn, runtime
  graft, no bake) holds the same posture. Degree differences across
  bodies are proportion, not defect.
- Numeric control (stronger than any render): today's mina_vale rebake
  is byte-identical to the owner-approved efaf55e original on every
  shared clip hash-checked (idle_12, idle_6, formal_bow, walk,
  casual_walk). The own-gait extension changed nothing upstream.
