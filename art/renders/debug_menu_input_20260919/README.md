# Debug pointer and warehouse entry

Evidence class: **INERT**. Native diagnostic captures and suite wrappers, not runtime contracts.

F1 opens the debug controls with the pointer. Open GO, then Dream ecology.
F1 or Escape exits the exhibit to the debug menu; another closes debug and resumes play.
Escape in ordinary play opens Building Services.

**debug_go.png** shows the real clickable route at 1280×720. **debug_after_ecology.png** shows the restored debug menu.
**entry.log.receipt.json** records 28/28 real-input checks. **sources.json** binds the five changed runtime/test sources with LF-normalized hashes and lists the separate regression runs.

Attempts 01 and 02 retain the false assumption exposed by actual input: an empty caption container covered GO. Attempt 03 fixes its hit testing. Full logs and stderr remain intact, including existing resident navigation refusals and found-art placement warnings. They are not a clean whole-building or V2 verdict.
