# DREAM PRODUCTION RENDERING R5 — ATTENTION LEAVES A WOUND

Production capture, 2026-08-20. Governing target:
`art/reference/dream_surface/stage4_room_colonisation.png`. R5 completes the
licensed exposure/phase/warp slice and the governed reflected-gold light. It
does not add a portal, another room, enterable space, an embrace state or
RECURSION.

## One state owner

`DreamExposureField` remains the only state. Both the Orison skin and the body
under it include `dream_phase_transition.gdshaderinc` and read the same ordered
continuum:

- infection / architectural conversion: **0.10 → 0.34**;
- rupture / breach reveal: **0.48 → 0.78**;
- flesh-to-living-gold transmutation: **0.70 → 0.92**.

Those values are accumulated dwell, not the current lamp cone. The selected
target voxel measures 0.0196 when inherited, 0.6745 after a 5.2 s governed hold
and 1.0000 after an 8 s hold. The field remains unchanged when the lamp writer
stops. Frost, liquid-metal and the final embrace remain unlicensed because no
narrative trigger or accessibility proof exists for them yet.

## Continuous wound and bounded warp

The R4 breach geometry is submitted at every state. A stable ordered material
coverage function opens from its centre and reaches plaster, living rim, lath
and rubble last; it never toggles a node, mesh, surface, collision or hazard.
The intact Atlas wall already behind the wound is therefore the genuine latent
state. The shader's anatomy channel now distinguishes the living rim while
keeping aperture, plaster, timber and the rest of the dangerous body in the
same one `ArrayMesh` surface.

The false interior receives one aperture-local rotational field. Its support
falls to zero at the authored tear, its maximum is 0.085 local UV, and its slow
breathing term has an approximately 70 s period. It never touches the camera,
screen, ordinary Orison outside the wound, navigation or hazard contact.
`DREAM_PHASE_DEBUG=1` shows infection/rupture/gold state. `=2` isolates the
bounded warp support.

## Governed reflected gold

At retained exposure 0.78 → 0.96, one production `OmniLight3D` fixed just
inside the dominant wound rises continuously from zero to **0.42 energy**.
It is warm, shadowless, limited to **3.2 m**, and remains at zero below the
threshold rather than appearing/disappearing as a node. It reads no current
lamp state. When the pocket forgets the owner room the ordinary R4 rebuild
retires it with that wound.

The maximum-light performance A/B uses the same deterministic target pocket
on both sides. `PERF_DREAM_EXPOSED=1` writes the real field to 1.000 and proves
the real light at 0.420 / 3.20 m / shadows false. Control versus exposed kept
every object/call row identical (114/114, 134/134, 133/133, 154/154); measured
times were 2.25/2.03/1.72/1.89 ms versus 2.02/2.03/1.73/1.91 ms. The added
world light is inside run noise and all eight rows remain below 16.6 ms.

## Frames

All captures use the production room, furnishing, player inspection lamp,
durable field, hazard body, reflected light and Compatibility renderer. No
showcase light, wall, hazard, portal or furniture is added.

- `00_latent/`: fresh process, dwell 0, measured exposure 0.0196. The wound is
  materially absent and the historic wall is the control.
- `01_rupture/`: fresh process, dwell 5.2 s, exposure 0.6745. The irregular
  centre has opened while the complete torn perimeter has not.
- `02_living_gold/`: fresh process, dwell 8 s, exposure 1.0000. The complete
  wound, living-gold phase and governed world light are active.
- `03_phase_debug/`: the high state with the shared phase diagnostic.
- `04_warp_debug/`: the high state with only aperture-local warp support shown.
- `05_final_1440p/`: accepted high-state production beauty at requested
  2560 × 1440 (Windows yields a 2560 × 1421 client viewport). Its lamp-off
  frame proves the faint interior and returned room light remain after the
  inspection beam leaves.

Shader `TIME` and the body continue moving, so these are production evidence,
not pixel-identical A/B frames.

## Corrected readings

1. Alpha blending would have moved the entire batched danger surface into the
   transparent pass and made its many overlapping limbs sort incorrectly. The
   accepted reveal is stable ordered material coverage on the existing opaque
   surface: gradual in area, temporally fixed and draw-neutral.
2. The first performance attempt used the ordinary spawn pocket, which did not
   own the governed breach and correctly printed `no governed breach`. It was
   not counted. The retained A/B stages the same deterministic target pocket
   for both sides and confirms field, light energy, range and shadow state.
3. A first staged performance control left the autonomous pocket follower
   running after teleport and caused a truthful exposure-volume overflow
   warning. The corrected diagnostic freezes topology exactly as the target
   shot does and feeds the static production lamp pose explicitly.
4. The first dark high-state lattice remained technically present but visually
   unreadable under ACES. Its local biological/otherworld afterglow was raised
   only on the wound's own pixels; the governed 0.42 light remains the sole
   source returning illumination to surrounding architecture.

## Proof

- `DreamSurfaceTargetTest.tscn`: **71/71 PASS**. It proves exact phase order,
  shared architecture/anatomy controller, durable monotonic field, persistence
  with the lamp off, unchanged one-surface geometry, bounded warp and afterglow,
  and one room-local shadowless reflected light which rises only at high
  retained exposure.
- `DreamRoomBuilderTest.tscn`: **175/175 PASS**.
- `DreamHazardTest.tscn`: **42/42 PASS**.
- `DreamLineageTest.tscn`: **21/21 PASS**.
- Fresh real-window 2560 × 1440 target-pocket perf A/B: **PASS**, 0/8 rows over
  16.6 ms, identical objects and calls as itemised above.
- `WalkTest.tscn` FAST and FULL x8/480: **PASS**. The inherited null-layout,
  resident-route, headless broadcast-texture and non-finite diagnostics remain
  in the log; R5 owns none of those systems.

## Honest boundary

R5 makes attention persist as a wound, gives the wound one bounded spatial
distortion and permits one measured room-local light at the highest retained
state. It still shows only a shader-authored impossible interior. R6 is the
first gate allowed to propose a view-only portal; it must not infer enterable
space or RECURSION from this result.
