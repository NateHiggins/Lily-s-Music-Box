# Clockwork Waltz title screen

Production 1280 × 720 captures, 2026-08-15.

- `00_before.png` is commit `07607f4`: the placeholder `REALTY MAINTENANCE`
  menu over the sales plate, with `three_am_radiator.ogg` as its looping theme.
- `01_escapement_failure.png` is the new opening face: the actual game title,
  the cold vertical stairwell, quiet clockwork diagram and full returned master.
- `02_original_session.png` is the selectable second face: the same menu over
  the period sales plate and the full untouched Clockwork Waltz.

The images are deterministic silent renders. `TitleScreenAudioTest.tscn`
separately proves real playback and the finished-stream handoff.
